# The port's divergence register

This page holds the `Parity with ja4plus-go` section of the port's specification. The
port is the Python implementation at `Crank-Git/ja4plus`. FR-reference-17 and
FR-reference-18 of `docs/specs/features/11-foxio-reference.md` require the copy, so that a
reader checks a parity row without a clone of the port.

| Record | Value |
|---|---|
| Repository | <https://github.com/Crank-Git/ja4plus> |
| File | `docs/specs/spec.md`, line 255 to line 320 |
| Version | `v1.1.0` |
| Commit | `21299645366591331eb93155355b65a76a3729f3` |
| Retrieved | 2026-08-11 |
| Bytes | 36445 |
| SHA-256 | `f5ec7502b46cea632ac8d291f32acf3b97fcf3471a5ee8a4a8ea0ac6aba45f4b` |
| Rows | 31 |

**The copy below is verbatim, and no reader edits it.** `.claude/rules/ste.md` bars a
rewording of copied text, because a reworded quotation is no longer evidence.

**The port writes `ja4plus-go` for this repository, and `ja4plus` for itself.** This
repository writes `port` for the Python side. A sentence of the copy that reads "the
port" therefore names this repository. `docs/specs/spec.md` `## Terms` states both words.

**This page records a source, and it decides no value.** The register of this project is
`testdata/deviations.json`, and `docs/specs/spec.md` `## Parity with ja4plus` holds the
rows this project acts on.

The command below reproduces the copy and the hash.

```console
gh api "repos/Crank-Git/ja4plus/contents/docs/specs/spec.md?ref=21299645366591331eb93155355b65a76a3729f3" \
  --jq '.content' | base64 -d | sed -n '255,320p' | shasum -a 256
```

**The `Rows` record counts the body rows of the `### Divergence register` table of the
copy.** The port adds a row when it records a new divergence, so a count that differs
reports a stale copy. The counting rule is this, and a reader who counts differently
reports a drift the copy does not hold.

- The count starts at the heading `### Divergence register`, and it ends at the end of the
  copy.
- A row is one line that starts with `|` under that heading.
- The header row and a separator row are not body rows.
- The port writes a blank line inside the table, and a row after a blank line is still a
  body row.

`port_register_drift_test.go` holds the rule and the comparison. FR-parity-58 and
FR-parity-60 of `docs/specs/features/08-python-parity.md` state the requirement.

**The command below writes no end marker, because a second copy of that marker would stop
the marker search of `deviations_test.go` at the wrong line.** It reads to the end of the
file instead, and the trailer of the copy holds no table row.

```console
sed -n '/^### Divergence register/,$p' docs/specs/foxio/port-register.md \
  | grep '^|' | grep -v -e '^| Item |' -e '^|---' | wc -l
```

<!-- port-register:begin -->
## Parity with ja4plus-go

A Go port of this project exists at `Crank-Git/ja4plus-go`. `ja4plus/processor.py`
already states that it mirrors that port. The two must not drift apart, because a
user who runs both must get one answer.

Three rules govern parity.

1. **FoxIO decides behaviour.** Where FoxIO specifies the output, the vectors
   decide. This rule outranks the port. Where the port disagrees with a vector, the
   port is wrong.
2. **The port decides interface.** Where FoxIO specifies nothing — a field name, a
   subcommand name, a default — the port has already shipped a choice. This project
   adopts that choice rather than inventing a second one.
3. **The gate is the shared vector set.** Both repositories read the same 37 FoxIO
   vectors, pinned to the same upstream commit. No test in this repository builds,
   runs, or imports the port. A cross-language test rig couples two repositories
   that move at different speeds, and it fails for reasons that have nothing to do
   with the change under test.

This spec plans changes to this repository only. It records what the port should
change, and files nothing there.

### Divergence register

| Item | This project today | The port today | Rule | Action |
|---|---|---|---|---|
| Result type | `dict` with 8 keys. | `FingerprintResult` struct with 9 fields. | 2 | Adopt the port's field set, including `Timestamp`. Epic 4. |
| Results per packet | One fingerprint string. | A slice of results. | 2 | Return a list. Epic 4. |
| Parse failures | Swallowed, logged at `DEBUG`. | Returned as a second value. | 2 | Expose the errors the processor collected. Epic 4. |
| Remote lookup | The command-line flag `--lookup` is opt-in, but `JA4DBClient.lookup` then calls `ja4db.com` on every miss with no second opt-in. | The remote fallback is a separate opt-in. | 2 | Separate the local lookup from the remote lookup. Epic 7. |
| Mapping file refresh | `db update` and `db info` already exist. | The same two subcommands. | — | Already at parity. Keep a test that proves it. |
| Shard key format | `tcp:ip:port->ip:port`. | The same format. | — | Already at parity. Keep a test that proves it. |
| JA4SSH window | Emits at `packet_count`, which defaults to 200. | Emits at `min(packetCount, 10)`. | 1 | Fixed here by #28. The port carries the defect at `ja4ssh.go:176`. |
| JA4SSH results the reference produces and this project declines | Reads the mode field from the window alone, emits a value only for a window that holds SSH packets, and emits the trailing window for every connection that closes. | Not measured. | 1 | Three defects of the FoxIO reference produce output that describes the capture and not the connection, so rule 1 does not settle them and a person decided each one. #96, #97 and #105 hold the ruling and the measurement. The port must adopt all three, or the two implementations disagree on JA4SSH. |
| The window a connection holds open at the end of a capture | Emits it. `close_open_windows` reads every connection of the state table, and the caller runs it when the packet source ends. | Not measured. The port exposes no equivalent method. | 1 | The specification states no rule that closes the last window, so this is not a case rule 1 settles. `docs/specs/foxio/JA4SSH.md` R11 holds the gap, and #199 proved that the deleted `technical_details/JA4SSH.md` states nothing about it. The references split two against two: `python/ja4.py:554-556` and `wireshark/source/packet-ja4.c:1399-1404` close the last window on a FIN+ACK packet, and `rust/ja4/src/ssh.rs:45-55` and `zeek/ja4ssh/main.zeek:160-164` close it at the end of the capture. `python/ja4.py:610` runs no end-of-capture step, because the line is commented out. The user decided on 2026-08-08 that `ja4plus` follows the Rust reference and the Zeek package, and the ruling is reversible. Two FoxIO references hold the value: `tests/foxio_vectors/rust_expected/ja4__insta@ssh2.pcapng.snap:215-217` and the Zeek baseline both give `c36s52_c42s76_c51s2` as the second value of `ssh2.pcapng`, and the `ja4plus` counters already held it. Six JA4SSH comparisons moved and no other value moved. The register lost `ssh2.pcapng/JA4SSH` and gained five entries under #214. #97 stays declined, so a window that holds no SSH packet still emits nothing. #214 holds the ruling and the measurement. The port must add the same call, or the two implementations disagree on JA4SSH. |
| The method that emits the window at the end of a capture | `close_open_windows()` on `BaseFingerprinter`, on every fingerprinter and on `Processor`. It returns the entries it appended. | No equivalent method. | 2 | The port has shipped no choice here, so rule 2 names no interface to adopt and this project chose one. The name follows `_close_window`, which `ja4plus/fingerprinters/ja4ssh.py` already held. The port must adopt the name, or the two implementations expose two names for one behaviour. |
| Parity test | 6 tests that assert names exist. | Not applicable. | 3 | Replace with the shared vector suite. Epic 1. |
| JA4 ALPN value for a byte that is not alphanumeric | Writes `99`. | Not measured. | 1 | The FoxIO prose and the FoxIO implementations disagree, so rule 1 settles the value on the vector. #127 holds the ruling and the measurement. The port must adopt `99`, or the two implementations disagree on JA4 and on JA4S. #141 owns the condition that triggers the value, which no vector separates. |
| JA4 ALPN value for a byte outside `0x20-0x7E` in a position other than the first | Writes `99`. | Not measured. | 1 | The two FoxIO implementations disagree with each other, so this is a reference split and rule 1 does not settle it. The user decided on 2026-08-07 that `99` stays. Two reasons support the reading. The FoxIO Rust result is an artifact. `rust/ja4/src/tls.rs` reads the tshark escape text, so it reads `h\x1f` as the five characters `h`, `\`, `x`, `1` and `f`. The FoxIO Python result puts `U+FFFD` into a fingerprint, which is a character no byte of the packet holds. #141 holds the measurement, #162 holds the ruling, and the ruling is reversible. `tests/foxio_vectors/alpn-condition.pcap` streams 2, 4 and 5 carry `h\xab`, `h\x1f` and `h\x0a`, and the register holds twelve entries that state the measured value of both implementations. The port must write `99`, or the two implementations disagree on JA4 and on JA4S. |
| JA4 ALPN value for a first ALPN value of one byte | Repeats the byte and writes `hh`. | Not measured. | 1 | All three sources differ on the input `h`: FoxIO Python writes `h`, FoxIO Rust writes `h0`, and no vector separates them. The FoxIO Python value holds one character, which cannot fill a two-character field. `ja4plus` writes a value that is well formed and stable, and the user decided on 2026-08-07 that it stays. #141 holds the measurement, #162 holds the ruling, and the ruling is reversible. `tests/foxio_vectors/alpn-condition.pcap` stream 6 carries the input, and the register holds four entries that state the measured value of both implementations. The port must repeat the byte, or the two implementations disagree on JA4 and on JA4S. |
| The JA4 ALPN form that matches no FoxIO implementation | Writes `99` for a first ALPN value that is not alphanumeric, and `hh` for a first ALPN value of one byte. | Not measured. | 1 | **The user ruled on 2026-08-10 that the form of `ja4plus` stands**. The conformance audit of the same date named this the one condition where `ja4plus` matches no FoxIO implementation. The two references disagree with each other, and each one reads its own tooling rather than the packet. `ja4plus` reads the packet bytes, and it writes `99` or `hh`. FoxIO Python writes `U+FFFD`, which is a replacement character that no packet byte holds. FoxIO Rust writes `h9`, which is the escape text of `tshark`. **No FoxIO reading is available to adopt here**. A match with either reference copies an artifact of that reference tool into the fingerprint, and the user declines that. This row moves no fingerprint and it repairs no defect. #141 holds the measurement of the disputed inputs, #162 records the readings of 2026-08-07, and #522 records this ruling. The two rows above state those readings, and this row states the ruling that stands over both. The sixteen entries of `tests/foxio_deviations.json` that name #162 stand, and `tests/test_alpn_ruling_register.py` reads this row and that count. The ruling is reversible. The port must write `99` and `hh`, or the two implementations disagree on JA4 and on JA4S. |
| JA4L client value on the return path of `process_packet` | Returns a client value for every packet that moves the client measurement point. Across the committed vectors the return path reports 105 client values, and the stored list holds 60. | Not measured. | 1 | The reference reports one client value for one connection, and the stored list of `ja4plus` holds one. `tests/conformance_index.py` reads the stored list, so every conformance case measures the correct value and no fingerprint moves. The return path repeats the value, because the reference measures to the last packet that carries both relative numbers. A reader knows which packet is last only when the connection ends. #156 measured three candidate end-of-connection points against the 60 client values. A FIN or a RST reports 26 and loses 34, because 34 connections never close inside their capture. `macos_tcp_flags.pcap` holds no FIN packet and no RST packet at all. Every measurement point read reports 105, which is the behaviour today. A read of the stored list at the end of a capture reports all 60, and that is not a `process_packet` return. That candidate needs a new flush call, and a live capture never ends, so `ja4plus --live` would then report no client value. The user decided on 2026-08-07 to record the divergence and to leave the return path as it is. #156 holds the ruling and the measurement, and the ruling is reversible. The register holds no entry for this divergence, because the conformance harness reads the stored list and a strict entry would report `XPASS`. `tests/test_ja4l_return_path.py` holds both counts instead, so the divergence is a comparison that runs. The port must report one client value for one connection, or the two implementations disagree on JA4L. |
| The JA4L part count | Writes two timing parts on every connection. A QUIC connection carries a protocol marker as a third part, and no TCP connection carries a third part. | Not measured. | 1 | The specification and two FoxIO implementations write three parts, and two FoxIO implementations write two. `technical_details/JA4L.png` labels three parts. `wireshark/test/testdata/` holds 44 `ja4.ja4l` values and 44 `ja4.ja4ls` values, and every one of the 88 holds three parts. `zeek/tests/Traces/` writes three. `python/test/testdata/` writes two, and `rust/ja4/src/snapshots/` writes two. This is a reference split, so rule 1 does not settle it. **The user decided on 2026-08-08 that the two timing parts stay**, and the reason is the conformance evidence. Every one of the 114 JA4L values in `tests/foxio_vectors/*.json` holds two parts, and `tests/foxio_vectors/wireshark_expected/` holds two files that carry no JA4L key. A three-part form therefore deviates from all 114 comparisons, which moves the register from 116 entries to 230 and leaves no case that measures any JA4L value. **20 of the 114 sit on 10 captures that the Wireshark dissector publishes no JA4L value for**, so those 20 would carry a third part that no reference verifies. A second reading blocks the adoption. The Wireshark part c reads measurement points D, E and F, which `packet-ja4.c:1328`, `packet-ja4.c:1343` and `packet-ja4.c:1366` set on application packets. `ja4l.py` emits the server value on the SYN-ACK and the client value on the first packet that carries both relative numbers, and both points lead D. Adopting part c is therefore an emission-model change and not a format change. #225 holds the ruling and the measurement, and the ruling is reversible. The port must write two timing parts, or the two implementations disagree on JA4L. |
| The JA4L protocol marker on a QUIC connection | Writes `quic` as the third part of `JA4L-C` and `JA4L-S` on a QUIC connection. 36 values across four captures carry it. | Not measured. | 1 | The FoxIO references disagree, so this is a reference split and rule 1 does not settle it. `packet-ja4.c:1441` and `packet-ja4.c:1447` write `"%d_%d_quic"`, and `wireshark/test/testdata/` publishes the marker on 9 of the 44 `ja4.ja4l` values and on 9 of the 44 `ja4.ja4ls` values. `zeek/ja4l/main.zeek:233` and `zeek/ja4l/main.zeek:252` append the literal `q`. `python/ja4.py:165` writes two parts, and `rust/ja4/src/time.rs` writes two parts. The user decided on 2026-08-08 that a QUIC connection carries the marker, because a published reference value verifies it. **The spelling follows Wireshark.** `.claude/rules/external-apis.md:95` reads "Read no JA4L or JA4LS value of a Zeek baseline as a reference value", and `.claude/rules/external-apis.md:101` names the `q` marker as one of three Zeek divergences. The rule declines the Zeek spelling, so the Wireshark spelling is the one published reading that remains. **The `tcp` literal is not adopted.** `packet-ja4.c:1348` and `packet-ja4.c:1354` write it on an HTTP connection, and none of the 88 published Wireshark values carries it. #225 holds the ruling and the measurement, the register holds 16 entries that state the measured value of the Python reference, and the ruling is reversible. The port must write the marker on a QUIC connection, or the two implementations disagree on JA4L. |
| The JA4L server value on a retransmitted SYN-ACK | Emits one server value for one connection. A retransmitted SYN-ACK finds point `B` set, and it gives no value. | Not measured. | 1 | The reference publishes one server value for one connection, and a repeat is a defect under `.claude/rules/conformance.md`. `ja4plus` returned the value on every SYN-ACK, so a retransmitted SYN-ACK repeated the value the first one gave. **The repeat described no second measurement, because a retransmitted SYN-ACK moves neither point `B` nor the server TTL.** `ssh2.pcapng` stream 15 is the one vector that reaches the rule: the expected-output file holds `JA4L-S=6252_58` once, and `ja4plus` produced it twice. **A replay of the 38 committed captures moved exactly one value**, and that value is the second `6252_58` of `ssh2.pcapng` stream 15. No other JA4L value moved, on that capture or on any other. The register lost `ssh2.pcapng/JA4L-S` and fell from 135 keys to 134, and the conformance suite moved from 135 `xfailed` to 134. `tests/test_ja4l_connection_state.py::test_a_retransmitted_syn_ack_reports_no_second_server_value` holds the rule, and it fails when the guard goes. #272 holds the ruling and the measurement. The port must emit one server value for one connection, or the two implementations disagree on JA4L. |
| The JA4L comparison on a reference file that holds no JA4L key | Produces a JA4L value on `CVE-2018-6794.pcap`, on `https-connect.pcap` and on `tls-handshake.pcapng`. The expected-output file of each of the three holds no JA4L key. | Not measured. | 1 | **This project was never emitting more than the reference. The reference published nothing to compare.** The comparison is unreachable, not satisfied. `python/ja4.py:339` runs `delete_keys(['JA4L-S','JA4L-C'], final)` when the generating run names another method, so the method filter removed the key from the file. The evidence is `python/ja4.py:339` at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. A count of the JA4L keys of the 38 expected-output files reports 0 for those three files and a value for 22 others, so the deletion names the three and not the method. #200 measured the mechanism and #272 decided the decline on 2026-08-08. **Five register entries carry the decline**, and each one records a value decline rather than a capability decline. This row changes no fingerprint, so the port needs no change for it. |
| JA4X on a stream that a proxy tunnel carries | Reads the record layer without regard to the tunnel protocol that carries it. Produces three JA4X values on the SOCKS4 tunnel of `socks4-https.pcap` on port 9901, and no FoxIO implementation holds one. | Not measured. | 1 | No FoxIO implementation holds a value, so this is unanimity and not a reference split, and rule 1 does not settle it. The user decided on 2026-08-07 that the three values stay. The same behaviour produces the `https-connect.pcap` values that the FoxIO Rust snapshot and the Wireshark dissector both hold. A gate on the record-layer scan would risk that case. #138 holds the ruling and the measurement, the register records the divergence, and the ruling is reversible. The port must read the record layer the same way, or the two implementations disagree on JA4X. |
| The JA4H method code for a method outside the nine the Rust reference names | Reads the first two characters of any method token. `PROPFIND` writes `pr`, and `MKCOL` writes `mk`. | Not measured. | 1 | The three FoxIO references disagree, so this is a reference split and rule 1 does not settle it. `python/ja4h.py:9` reads `method.lower()[:2]` and names no method. `rust/ja4/src/http.rs:342` rejects a method outside `co`, `de`, `ge`, `he`, `op`, `pa`, `po`, `pu` and `tr`. `wireshark/source/packet-ja4.c:1087` holds a 40-entry map, so it reads a method the Rust reference rejects and it produces nothing for a method its map omits. The user decided on 2026-08-08 that `ja4plus` follows the Python reference, which is the primary reference for every method in this project. A method absent from the Wireshark map therefore writes a two-character code rather than nothing. The image caption ends with `etc`, so the image closes no method list. No vector in this repository carries a method outside the nine, so no vector separates the three readings and no register entry names this reading. #219 holds the ruling and the measurement, and the ruling is reversible. `tests/test_ja4h_part_a_readings.py` holds six constructed method cases instead, so the divergence is a comparison that runs. The port must read the first two characters of any method, or the two implementations disagree on JA4H. |

| Part e of JA4TS, the time since the last SYN-ACK | Writes part e. A connection the server answered twice or more carries the delay of each SYN-ACK, in whole seconds, and a connection the server answered once omits part e. | Not measured. | 1 | The FoxIO image is the one source that keeps part e off JA4TS: its caption reads `TCP Retransmission Timings (only on JA4TScan)`. Three FoxIO sources contradict it, and nothing ever corrected them. The deleted `technical_details/JA4T.md` heads its form `__JA4TS and JA4TScan Fingerprint formats:__` and writes `WindowSize_TCPOptions_MSSValue_WindowScale_TimeSinceLastSYNACK`; `b6f3ff4` deleted the file on 2024-02-22 and no commit restored it. `wireshark/source/packet-ja4.c:1595` writes the `JA4TS` field through `ja4t()` with a connection, which appends part e at line 684. `zeek/ja4t/main.zeek:229-235` appends the delay list to `c$conn$ja4ts`. The image's own example value `1-2-4-8-R6` carries the `R` suffix its caption denies. This is a reference split, so rule 1 does not settle it, and the user decided on 2026-08-08 that JA4TS carries part e. **The ruling reverses D6 and D7 of #215 of the same day, which followed the image.** #226 holds the measurement, and the ruling is reversible. **The reference value this reading departs from is the Zeek baseline, and the departure measured zero.** The three `conn.log` baselines hold 10 JA4TS values, `Scripts.ja4-conn/conn.log` holds `ja4ts 65535_00_00_00`, and #198 measured 9 of 10 matching. **After part e the reading is 9 of 10, unchanged, and no `ja4plus` value carries part e on any of the ten rows.** Each of the ten baseline connections holds one SYN-ACK, and the deleted file states that a fingerprint omits part e when it sees no retransmission. Across the 38 captures of `tests/foxio_vectors/`, 803 values are produced before and 803 after, and exactly 1 differs: `ssh2.pcapng` packet index 372, which is frame 373 in a one-based reader, moves from `64240_2-1-1-4-1-3_1460_7` to `64240_2-1-1-4-1-3_1460_7_0`. The register holds 116 keys before and after, and the conformance suite reports 116 `xfailed` before and after, because no conformance case compares a JA4TS value. `docs/specs/foxio/JA4T.md` states the rule as R12 and `docs/specs/foxio/zeek.md` records the re-measurement. The RST value of JA4TS separates from part e, and the row below holds it. The port must write part e, or the two implementations disagree on JA4TS. |

| The JA4TS value that a RST produces | Appends `-R` and the delay of the RST to part e, on a connection that already holds a delay. That value reads part a through part d from the first SYN-ACK of the connection. A RST on a connection with no delay produces no value, and a client RST produces no value. | Not measured. | 1 | The deleted `technical_details/JA4T.md` is the primary source and it states the rule: "the final TCP packet, a RST packet, should be appended to the last JA4TS denoted with 'R' and its delay." Its example ends `65535_2-1-3-1-1-4_65495_8_1-2-4-8-R6`, and it states the reason the value reads the stored connection: "Note that RST packets do not contain TCP options or window sizes, as such the program will need to be aware of the previous JA4TS." `b6f3ff4` deleted the file on 2024-02-22 and no commit restored it, so the statement stands as FoxIO-authored under the rule #221 established. **Two implementations corroborate the suffix and each disagrees with the other on one point, so rule 1 does not settle those two points and a person decided each.** `wireshark/source/packet-ja4.c:1608` writes a second `JA4TS` value on the RST packet, and lines 1601 to 1607 restore `window_scale`, `window_size`, `mss_val` and `tcp_options` from the connection. `zeek/ja4t/main.zeek:233` appends `-R%d` to the delay list. Both nest the RST branch inside the delay branch: `wireshark/source/packet-ja4.c:693` reads `rst_time` only when `syn_ack_count > 1`, and `zeek/ja4t/main.zeek:232` reads `rst_ts` only when the delay list holds a value. **The first disagreement is the flag combination, and `ja4plus` reads the RST bit.** `zeek/ja4t/main.zeek:167` tests `rph$tcp$flags & TH_RST`, and `wireshark/source/packet-ja4.c:1296` tests `tcp_flags == 0x004` for equality, so the dissector reads no RST that also carries ACK. The deleted file writes "a RST packet" and names no flag combination, so the prose and the Zeek script agree and the dissector reads one case fewer. A RST that carries ACK is common on real traffic. **The second disagreement is the RST that carries no delay, and `ja4plus` emits nothing.** `wireshark/source/packet-ja4.c:1599` writes the `JA4TS` field whenever `syn == 3`, so the dissector emits the four parts a second time on such a RST. That second value repeats the value the SYN-ACK already produced and describes no packet of its own, which `.claude/rules/conformance.md` declines under its second shape. `zeek/ja4t/main.zeek` writes one JA4TS value for one connection and never a second, so the Zeek package corroborates the decline. **A client RST produces no value**, because every SYN-ACK travels from the server and the connection key names the server first. `zeek/ja4t/main.zeek:189` sets the packet threshold with `F`, so the Zeek script reads responder packets alone, and `wireshark/source/packet-ja4.c:1296` holds no direction guard. **No vector reaches the rule and no value moved.** `tests/foxio_vectors/` holds 10 RST packets across its 38 captures, and none of the 10 sits on a connection with more than one SYN-ACK. A replay of the 38 captures produces 797 values before the change and 797 after, and 0 differ. The register holds 137 keys before and after, and the conformance suite reports 137 `xfailed` before and after, because no conformance case compares a JA4TS value. `tests/build_ja4ts_rst.py` writes the capture from the six capture times the deleted file lists, and `tests/test_ja4ts_reset.py` reads it back. #246 holds the measurement, `docs/specs/foxio/JA4T.md` states the rule as R13, and the ruling is reversible. The port must append the RST suffix, or the two implementations disagree on JA4TS. |

| The empty option list, the absent maximum segment size and the zero window scale of JA4T and JA4TS | Writes the two-digit form. An empty option list writes `00`, part c writes two digits, and a window scale of zero writes `00`. `gre-erspan-vxlan.pcap` therefore gives `8192_00_00_00`. | Not measured. | 1 | **Three FoxIO forms exist and rule 1 settles none of them, so this is a reference split and the user decided it.** `rust/ja4/src/tcp.rs` joins an empty option list to the empty string and prints part c and part d with no padding, so its snapshot holds `8192__0_0`. `wireshark/source/packet-ja4.c:664` writes `00` for an empty option list, prints part c with `%02d`, and prints part d with `%02d` when the window scale is zero. `zeek/ja4t/main.zeek:195-211` repeats the Wireshark rule for JA4T and for JA4TS. Two of the three forms agree, and the user followed them on 2026-08-08. The deleted `technical_details/JA4T.md` states the rule in prose and corroborates the form a third time: it reads "If any field does not exist, then the output is 00." and gives `JA4T = 1024_00_00_00`. **The prose and the two implementations part on one input, and `ja4plus` follows the implementations.** The prose reads `00` as the mark of an absent field, and both implementations test the value instead, because each stores the window scale as an integer that defaults to zero. A packet that carries a Window Scale option of value 0 therefore writes `00` in both implementations and `0` under the prose alone, and 7 of the 12 moved values are of that shape. A fingerprint exists so that one tool's output can be compared against another tool's output, so `ja4plus` writes the value the two executable references write. **This diverges from the FoxIO Rust implementation, which is the reference the parity harness compares JA4T against, and the cost was measured before the change.** 12 values across 6 captures take the new form: `CVE-2018-6794.pcap` gives three JA4TS values, and `gre-erspan-vxlan.pcap`, `gre-sample.pcap`, `sshv1.pcap` and `v6.pcap` each give one JA4T value and one JA4TS value, and `ssh2.pcapng` gives one JA4TS value. 5 of the 12 carry an empty option list, and the other 7 move only because the window scale is zero. **Only 1 of the 12 reaches a FoxIO reference comparison**, because this repository holds a Rust snapshot for 7 captures and no reference file holds a JA4TS value. That one is `gre-erspan-vxlan.pcap/0:65174/JA4T.1`, which stays in `tests/foxio_deviations.json` as a decided divergence and keeps its `xfail(strict=True)` marker. The register therefore gains no entry from this reading. #215 holds the ruling and the measurement, and the ruling is reversible. **D2, D3, D4 and D5 of #215 gain no row of their own, and the reason is stated rather than left out.** Each one moves `ja4plus` towards the FoxIO references and departs from none of them. D2 is the one that needs a word: the reader counts one entry for each End of Option List byte, which `rust/ja4/src/tcp.rs` and `wireshark/source/packet-ja4.c:1458` both do, and `zeek/ja4t/main.zeek:95-98` leaves the option loop at kind 0 and records nothing. Two references against one settles it under rule 1, the Zeek package outranks nothing, and `ja4plus` already recorded one kind 0 entry before #215, so the departure from the Zeek script is older than this change. `docs/specs/foxio/zeek.md` records it. The port must write the two-digit form, or the two implementations disagree on JA4T and on JA4TS. |
| Subfield 2 of JA4D when a message repeats option 57 | Writes the first Maximum DHCP Message Size, in four digits. | Not measured. | 1 | **This declines a proven defect of the dissector.** `wireshark/source/packet-ja4.c:1508-1512` appends `"%04d"` to the same buffer on each occurrence, so a message that repeats option 57 gives an eight-digit subfield and a part a of fifteen characters. R2 of `docs/specs/foxio/JA4D.md` gives part a eleven characters and subfield 2 four of them, and the specification decides schema. Read at the width the image states, the dissector's buffer holds the first occurrence, so this project keeps the first. `zeek/ja4d/main.zeek:55-63` reads one value. **No vector carries a repeated option 57**, so the decline costs zero unmatched cases; `tests/test_ja4d_decisions.py::test_ja4d_keeps_the_first_maximum_message_size` holds the separating packet. The user decided D4 of #231 on 2026-08-08, and the ruling is reversible. |
| JA4D on a BOOTP message that carries no option 53 | Emits no value. | Not measured. | 1 | **Two FoxIO references against one.** `wireshark/source/packet-ja4.c:1498` sets the emit flag inside the option 53 block alone, so the dissector emits nothing, and the `00000` default at `wireshark/source/packet-ja4.c:705` stays unreachable for DHCP. `zeek/ja4d/main.zeek:43-45` returns `"00000"`, and `zeek/ja4d/main.zeek:81` records the author's own doubt: `Not sure this is actually possible since you need at least option 53 to be DHCP and not just BOOTP`. **No vector carries a BOOTP message**, so this project stays unmatched against the Zeek script on zero cases; `tests/test_ja4d_decisions.py::test_ja4d_emits_nothing_for_a_bootp_message_that_holds_no_message_type` holds the separating packet. The user decided D2 of #231 on 2026-08-08, and the ruling is reversible. |
| Subfield 1 of JA4D6 on a relay message | Writes the outer DHCPv6 message type alone, in five characters, so part a holds eleven characters. | Not measured. | 1 | **This declines a proven defect of the dissector.** `wireshark/source/packet-ja4.c:1537-1546` appends a five-character name for every `dhcpv6.msgtype` field, and a relay message holds two: the outer type and the inner type. The dissector therefore writes `rlayfsolct` and a part a of sixteen characters. R2 of `docs/specs/foxio/JA4D.md` gives part a eleven characters and gives subfield 1 five of them, and the specification decides schema. A consumer that reads part a by position keeps working against the eleven-character form and breaks against the sixteen-character one. **This row departs from the only executable implementation.** No FoxIO implementation other than the Wireshark dissector writes a JA4D6 value. `zeek/README.md:15` states `JA4D6 &rarr; ja4d.log (awaiting Zeek DHCPv6 suppport)`. FoxIO ships no Python and no Rust for the method. No deleted text file covers the method. **No vector carries a relay message, so the decline costs zero unmatched cases.** `tests/foxio_vectors/dhcpv6.pcap` holds no relay message, and all six reference values match before the change and after it. `tests/test_ja4d_decisions.py::test_ja4d6_writes_the_outer_message_type_alone_in_subfield_1` holds the separating packet. A reversal of the ruling fails 5 cases. The user decided D3 of #271 on 2026-08-08, and the ruling is reversible. **D1 and D2 of #271 carry no row, because each follows the dissector.** Part a reads the Client DUID, the IA_TA option and the Client FQDN at any nesting depth. Part c reads the Option Request List the same way. |

| The methods the FoxIO License 1.1 covers | The README names each method it implements under the license, and that list holds ten names. It states that FoxIO's list is wider, and it cites `License FAQ.md` at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. **The ten stand as a subset**, and the README asserts no equality with FoxIO's list. | Not measured. | — | Record the contradiction, and change the README nowhere. **Three FoxIO records at that commit name a different set of methods**, so no statement of equality with FoxIO's list can be true. `License FAQ.md:5` names twelve methods, and it reads `__JA4S, JA4L, JA4LS, JA4H, JA4X, JA4SSH, JA4T, JA4TS, JA4TScan, JA4Scan, JA4D, JA4D6 and all future additions, (collectively referred to as JA4+)__`. The FoxIO `README.md:293` names nine methods, and it reads `**JA4S, JA4L, JA4LS, JA4H, JA4X, JA4SSH, JA4T, JA4TS, JA4TScan and all future additions, (collectively referred to as JA4+)**`. `LICENSE:3` names thirteen methods, and it reads `Software: JA4S, JA4H, JA4L, JA4LS, JA4X, JA4T, JA4TS, JA4TScan, JA4D, JA4D6, JA4SScan, JA4E, and JA4SSH (Collectively referred to as JA4+)`. **That third record spells the scanner `JA4SScan`, and it names a `JA4E` that neither other record names.** **Each quotation above is verbatim**, because `.claude/rules/ste.md` bars a rewording of text copied from the FoxIO material. **This is a candidate reference defect outside the two shapes `.claude/rules/conformance.md` declines.** It moves no fingerprint, and it makes no fingerprint describe traffic this project never read. That rule sends such a case to the user. **The user ruled on 2026-08-10 that the ten stand and the README does not change.** **The parity rules settle nothing here**, because they govern behaviour and interface, and a license list states another party's terms. #388 measured the three counts on 2026-08-09, and #466 read the three lines again at the same commit on 2026-08-10. `tests/test_foxio_license_register.py` holds this row. The ruling is reversible. |
| JA4Scan | Not implemented. **This project has recorded no ruling about JA4Scan.** The absence is neither a decline nor a plan. `License FAQ.md:5` names `JA4Scan`, `LICENSE:3` spells it `JA4SScan`, and the FoxIO `README.md:293` names neither spelling. | Not measured. | — | Record the state, and write no reason for a decline. **This project decided nothing about JA4Scan, so a stated reason would assert a ruling that no round holds.** The `Non-goals` section above holds the decline of JA4TScan, which is a separate method and a separate ruling. `README.md:42` states that reason, and that reason is unrelated to the license. The user ruled on 2026-08-10, and #466 holds the reading. `tests/test_foxio_license_register.py` holds this row. |
| A structurally valid ClientHello whose body describes no real client | Produces a fingerprint. `ja4plus` holds no plausibility guard, so the content of the body changes no outcome. | Not measured. | 1 | **This row records a property of `ja4plus` rather than a divergence from a named source.** The specification decides intent and the vectors decide the exact bytes where intent runs out. No FoxIO material and no vector settles which byte strings are a ClientHello, so rule 1 leaves the question open. **A guard here would be a rule this project invented.** `ja4plus` would then answer differently from every FoxIO implementation on the same bytes. A fingerprint exists so that one tool's output can be compared against another tool's output. **The measurement decides the cost, and #338 produced it and #343 re-measured it.** `tests/measure_random_client_hello.py` draws 5000 random bodies of 256 bytes behind a valid TLS record header and a valid handshake header. All 5000 produce a fingerprint and all 5000 are well formed. 4998 carry the version token `00`. 720 carry neither a cipher suite nor an extension. **The same script reads every committed capture and finds 168 ClientHellos. 0 of the 168 carry the version token `00`, and 0 carry neither a cipher suite nor an extension.** A guard on those two conditions therefore moves no vector. #343 measured that directly. With the guard live the conformance suite reported the same 1531 passed, 143 skipped and 135 xfailed. **It also closes nothing.** 2 of the 5000 draws pass both conditions. The guard therefore costs a permanent divergence and leaves about one fabricated value in twenty-five hundred. **The user decided on 2026-08-08 that `ja4plus` adds no guard, and the ruling is reversible.** `tests/fuzz/test_structural_validity.py` holds the ruling as four cases that a guard makes fail, and #343 holds the ruling and the measurement. The port must produce a fingerprint for the same bytes, or the two implementations disagree on JA4. |

Verified against: https://github.com/Crank-Git/ja4plus-go (`types.go`,
`processor.go`, `lookup.go`, `ja4ssh.go`, retrieved 2026-08-06, default branch
`master` at 2026-05-09).
<!-- port-register:end -->
