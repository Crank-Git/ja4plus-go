---
name: ja4plus-go v1.0.0
slug: ja4plus-go
repo: Crank-Git/ja4plus-go
status: approved
spec_version: 3
created: 2026-08-06
approved: 2026-08-11
html_generated: 2026-08-11
branch_model: dev-and-live
features:
  - id: foundation
    file: features/00-foundation.md
  - id: licensing
    file: features/01-licensing.md
  - id: correctness-audit
    file: features/02-correctness-audit.md
  - id: concurrency
    file: features/03-concurrency.md
  - id: conformance-harness
    file: features/04-conformance-harness.md
  - id: conformance-gaps
    file: features/05-conformance-gaps.md
  - id: fuzz-testing
    file: features/06-fuzz-testing.md
  - id: supply-chain
    file: features/07-supply-chain.md
  - id: python-parity
    file: features/08-python-parity.md
  - id: database-lookup
    file: features/09-database-lookup.md
  - id: release
    file: features/10-release.md
  - id: foxio-reference
    file: features/11-foxio-reference.md
  - id: ja4ls
    file: features/12-ja4ls.md
  - id: live-capture
    file: features/13-live-capture.md
  - id: documentation
    file: features/14-documentation.md
  - id: mutation-sweep
    file: features/15-mutation-sweep.md
  - id: pre-release-validation
    file: features/16-pre-release-validation.md
---

# ja4plus-go v1.0.0

## Overview

`ja4plus-go` is a Go library and command-line program for JA4+ network fingerprinting.
FoxIO publishes the JA4+ standard. This project is an independent implementation of that
standard. The library is at version `v0.3.0`.

**FoxIO names twelve methods. This project implements eleven of them.** The twelfth is
JA4TScan. `Non-goals` holds the reason, and the reason is that FoxIO publishes no format
specification and no reference implementation for it.

**`JA4LFingerprinter` writes both JA4L and JA4LS, so ten fingerprinters carry eleven
methods.** Read the ten as a count of fingerprinters, and never as a count of methods.
The port settled this wording in its issue #387, and `features/12-ja4ls.md` builds the
eleventh method here.

This spec describes the work that takes the library from `v0.3.0` to `v1.0.0`. Version
`v1.0.0` is an API freeze. After the freeze, the exported names and signatures stay
stable for the whole `v1` series. A freeze is only safe when four things are true.

1. The implementation produces the fingerprint that the FoxIO reference produces.
2. The concurrency contract is explicit and correct.
3. The license states the terms that FoxIO applies to the JA4+ methods.
4. This project and the port produce the same fingerprint for the same packet.

None of the four is true today. The reader of this spec is an engineer or an agent who
works on `ja4plus-go`. The user of `ja4plus-go` is a Go developer who embeds the library
in a network monitor, and a network analyst who runs the command-line program.

### What round 3 of this spec changed, and why

Rounds 1 and 2 were written on 2026-08-06. **The port shipped `v1.0.0` on 2026-08-10 and
`v1.1.0` on 2026-08-11, and it recorded about thirty rulings between those dates.** Each
ruling settled a question that this project must answer the same way, and about twenty of
them end with a sentence of the form "The port must X, or the two implementations
disagree on Y". In that sentence, "the port" names this project.

Round 3 therefore reads the port first and plans this repository second. `Parity with
ja4plus` below holds the register that carries every such row. The Changelog records the
round.

## Terms

This table is the controlled vocabulary of the project. **Where a term also appears in
the port's own table, the meaning here is the same meaning.** One concept keeps one word
across both repositories, because a reader moves between them.

| Term | Part of speech | Meaning in this project | Do not use |
|---|---|---|---|
| method | noun | One named JA4+ algorithm, for example JA4 or JA4SSH. FoxIO names twelve of them, and this project implements eleven. | algorithm, type, scheme |
| method name | noun | The lower-case token that names one method, for example `ja4h`. `FingerprintResult.Type` holds one. | method key, type name, token |
| fingerprinter | noun | The Go type that implements one method or two. `JA4LFingerprinter` implements JA4L and JA4LS, so ten fingerprinters carry eleven methods. | engine, module, handler |
| fingerprint | noun | The output string of one method for one connection. | hash, signature, ID |
| processor | noun | The `Processor` type, which runs every fingerprinter over one packet. | aggregator, pipeline, dispatcher |
| dispatch | noun | The type assertion that selects each fingerprinter of one optional interface, and the call the processor then makes to each selected fingerprinter. It never names the `Processor` type. | discovery, routing, fan-out |
| capability | noun | One optional interface that a fingerprinter implements, which the processor discovers with a type assertion. `WindowCloser` is one. The `capability` field of a register entry is a separate meaning, and row `capability decline` above states it. | feature, trait, ability |
| capture | noun | A packet capture file in `pcap` or `pcapng` format. | trace, dump, pcap file |
| key log | noun | The list of TLS secrets of one or more connections, in the NSS key log format that `draft-ietf-tls-keylogfile` specifies. | keylog file, secrets file, SSLKEYLOGFILE |
| secret | noun | One TLS traffic secret of one connection. The library derives the QUIC packet protection keys from it. | key, key material |
| decryption secrets block | noun | The pcapng block that carries a key log inside a capture. The IETF pcapng draft states its block type. | DSB, secrets block |
| vector | noun | One FoxIO capture plus the expected-output file that FoxIO publishes for it. | fixture, golden file, sample |
| corpus | noun | The whole set of captures and vectors that the project tests against. | dataset, suite, test data |
| stream number | noun | The value that the `stream` field of a per-stream vector entry holds. It names one connection of one capture. | stream ID, flow number |
| frame number | noun | The position of one packet in a capture, counted from 1. A per-packet vector record names one in the `frame.number` field. | packet number, packet index |
| endpoint key | noun | The two endpoints of one connection, sorted and written `<src>:<srcport>-<dst>:<dstport>`. A per-stream comparison names a connection with it where no vector entry names that connection. | flow key, tuple string |
| middle part | noun | The second of the three parts of a register key. It holds a stream number, an endpoint key or a frame number. | second field, stream field |
| conformance | noun | The property that the library output equals the FoxIO vector exactly. | compliance, correctness, accuracy |
| conformance suite | noun | The test suite that compares library output against every vector. | validation tests, spec tests |
| deviation | noun | A recorded difference between the library output and a FoxIO vector. | mismatch, failure, exception |
| reference | noun | The FoxIO repository at the pinned commit. It decides every disputed fingerprint. | upstream, source, spec repo |
| reference split | noun | The state where two FoxIO implementations produce different values for one input. The reference then decides nothing, and a person decides. | disagreement, conflict |
| reading | noun | One recorded conclusion about what a source states, with the evidence that supports it. | interpretation, take, finding |
| ruling | noun | One determination the maintainer makes where no source settles the question. A ruling is the choice of a person, and a reading is a conclusion about a source. | decision, call, verdict |
| maintainer | noun | The one person who makes a ruling for this project. `.claude/rules/rulings.md` and `.claude/rules/parity.md` each name the maintainer, and nobody else. | owner, lead, reviewer |
| project manager | noun | The session the maintainer delegates work to. `.claude/rules/rulings.md` states that it makes a delegated ruling, and no other ruling. | PM, orchestrator, coordinator |
| schema violation | noun | One published FoxIO value that a published FoxIO rule forbids, where every FoxIO implementation enforces that rule. `.claude/rules/rulings.md` states the five conditions. | schema breach, spec violation, invalid value |
| delegated ruling | noun | One ruling the project manager makes under a delegation that the maintainer granted. `.claude/rules/rulings.md` states the narrow delegation of 2026-08-12, which reaches a schema violation under five conditions. The same file states the widened delegation of 2026-08-13, which reaches the four open questions #289, #300, #346 and #369. A question that fails the five narrow conditions can still meet a decisive case. A genuine reference split fails both delegations, and it belongs to the maintainer. A delegated ruling stays provisional until the maintainer confirms it, and a later reader reads an unconfirmed one as unconfirmed. | automatic ruling, agent ruling, provisional decision |
| delegation | noun | One ruling authority that the maintainer gives to the project manager, under conditions that a grant states. `.claude/rules/rulings.md` `## What a delegated session may rule` states the two this project holds, and the word names no handover of a task. | task handoff, mandate |
| narrow delegation | noun | The one delegation that the maintainer granted on 2026-08-12, which reaches a schema violation and nothing else. `.claude/rules/rulings.md` `### The narrow delegation of 2026-08-12` states its five narrow conditions, and #246 holds the grant. | first delegation, limited delegation, original delegation |
| widened delegation | noun | The one delegation that the maintainer granted on 2026-08-13, which reaches the four open questions #289, #300, #346 and #369 under three decisive cases. `.claude/rules/rulings.md` `### The widened delegation of 2026-08-13` quotes the grant, and comment 5276145707 of #108 holds it. | broad delegation, extended delegation, second delegation |
| grant | noun | The recorded statement in which the maintainer gives the project manager one delegation. The grant states its own scope, and `.claude/rules/rulings.md` `## What a delegated session may rule` names where each one lives. The permission sentence of a license is a separate meaning, and `LICENSE` carries that sense. | authorization, warrant, charter |
| narrow condition | noun | One of the five conditions that a delegated ruling meets under the narrow delegation of 2026-08-12. `.claude/rules/rulings.md` `### The narrow delegation of 2026-08-12` numbers them, and a question that fails one leaves that delegation. | delegation condition, precondition |
| decisive case | noun | One of the three cases of the widened delegation under which the project manager settles an open question. `.claude/rules/rulings.md` `### The widened delegation of 2026-08-13` quotes all three, and the grant states that nothing else is decisive. | decisive condition, sufficient case |
| register | noun | The tracked file `testdata/deviations.json`, which holds one entry for each accepted difference from a FoxIO value. | deviation list, exception file |
| value decline | noun | One register entry that records a disagreement with a FoxIO value. An implementation change on either side could close it. The entry carries `"capability": false`. | value dispute, defect entry |
| capability decline | noun | One register entry that records a capability this project chose not to build. No implementation change closes it. The entry carries `"capability": true`. | scope decline, boundary decline |
| stale entry | noun | One register entry whose recorded `ours` value differs from the value the run produces. The entry then accepts a comparison it does not describe, and the conformance suite fails. | drifted entry, outdated entry |
| orphan entry | noun | One register entry whose key no comparison of the run reaches. The entry then accepts a difference the run never measures, and the conformance suite fails. A stale entry is reached and records the wrong value, and an orphan entry is never reached. | dangling entry, unreached entry |
| exclusion | noun | A capture or a stream that the conformance suite makes no comparison for. | exception, skip, ignore |
| transcription | noun | This project's own prose form of one FoxIO image, held under `docs/specs/foxio/`. | translation, copy, write-up |
| corroboration | noun | One FoxIO-authored source, other than the image, that states the same rule. | confirmation, second opinion |
| baseline | noun | One expected-output file of the FoxIO Zeek package, held under `zeek/tests/Traces/`. | golden file, expected log |
| port | noun | The Python implementation at `Crank-Git/ja4plus`. **The port's own spec uses this word for this repository. In this repository it names the Python side.** | Python version, sibling, twin |
| parity | noun | The state where this project and the port expose the same interface and produce the same fingerprint. | alignment, sync, consistency |
| audit | noun | A read-only review of the code that produces findings. | review, sweep, pass |
| finding | noun | One defect that the audit reports, with a file and a line. | issue, bug, problem |
| added file | noun | One `.go` file that a later issue adds to the repository root, to `internal/parser/` or to `cmd/ja4plus/`. No audit reads it, and `docs/audit/findings.md` names the issue that added it. | new file, unaudited file |
| gate | noun | A CI job that fails the build when its check fails. | check, guard, step |
| contract | noun | A documented rule that a caller must obey. | guarantee, promise, invariant |
| connection | noun | One network flow, identified by a five-tuple or by a QUIC connection identifier. | session, stream, flow |
| endpoint | noun | One address and one port. | host, peer, side |
| state table | noun | The map a fingerprinter uses to hold per-connection data. | cache, store, registry |
| freeze | noun | The `v1.0.0` commitment that exported names and signatures stay stable. | lock, stabilization |
| core package | noun | The package `ja4plus`, which the Go files of the repository root hold. It imports no HTTP client, so it performs no network input and no network output. `docs/audit/network-boundary.md` holds the ruling of 2026-08-14. | main package, root package, library package |
| remote lookup | noun | The request that `LookupFingerprintRemote` of the package `github.com/Crank-Git/ja4plus-go/ja4db` makes to the `ja4db.com` service for the record of one fingerprint. | online lookup, network lookup, API call |
| window | noun | The count of packets JA4SSH reads before it produces a fingerprint. | interval, batch, sample |
| open window | noun | One JA4SSH window that a connection holds when the packet source ends. | partial window, trailing window |
| bare ACK | noun | One TCP packet whose flags equal `0x0010` and whose payload is empty. `ACK` names the TCP acknowledgment flag. `docs/specs/foxio/JA4SSH.md` R16 holds the reading. | bare acknowledgment, empty ACK, pure ACK |
| FIN+ACK packet | noun | One TCP packet that carries the FIN flag and the ACK flag. It closes the connection, and JA4SSH emits the open window on it. `FIN` names the TCP finish flag. | FIN-ACK, close packet, teardown packet |
| part c | noun | The third part of a JA4SSH value. It holds the count of bare ACKs from each side. `docs/specs/foxio/JA4SSH.md` R15 holds the reading. | ACK part, third part |
| raw form | noun | The unhashed form of a fingerprint, for example `JA4_r`. | expanded form, debug form |
| zero sentinel | noun | The literal value `000000000000` that a hash field carries when its list holds no value. | zero marker, zero hash |
| plausibility guard | noun | A check that rejects input a method reads, because its content describes no real client. | sanity check, heuristic, filter |
| hop count | noun | The count of routers a packet crossed, read as the initial time-to-live minus the observed time-to-live. | hop distance, TTL delta |
| propagation factor | noun | The factor that the FoxIO hop-count table gives for one hop count. **It reaches the distance formula alone, and no fingerprint value reads it.** The JA4L divisor is the constant 2. | propagation delay, terrain factor |
| measurement point | noun | One packet timestamp that a JA4L value measures from or to. | anchor, marker |
| emission | noun | One value that a fingerprinter reports for one packet. One connection can reach more than one. | output, report, event |
| bare key | noun | One per-stream vector key that carries no occurrence number, for example `JA4S`. | plain key, unnumbered key |
| occurrence number | noun | The whole number of 1 or more that a per-stream vector key carries after the method name, for example the `2` of `JA4X.2`. | index, sequence number |
| surplus value | noun | One value the library produces that no vector key of the stream names. The conformance suite reports it as a deviation. | extra value, spurious value |
| protocol marker | noun | The third part that a JA4L or JA4LS value carries on a QUIC connection. Its value is `quic`. | protocol part, suffix |
| part a | noun | The first part of a JA4L or JA4LS value. It holds the one-way latency in microseconds, which is half of the interval between the two measurement points. `docs/specs/foxio/JA4L.md` R5 and R6 hold the reading. | latency part, first part |
| part e | noun | The fifth part of a JA4TS value. It holds the delay of each SYN-ACK after the first, in whole seconds. | retransmission part, timing part |
| object identifier | noun | One ASN.1 identifier, written as a dotted string such as `2.5.4.3`. JA4X hashes its hex form. | OID, identifier, tag |
| tunnel | noun | One connection that carries another protocol inside its payload, for example a SOCKS4 proxy connection. | proxy, wrapper, encapsulation |
| reported key | noun | The four address fields a `FingerprintResult` carries. It holds the outer address pair and the inner port pair. | output key, result key |
| grouping key | noun | The address pair and the port pair that collect packets into one connection. Both read the inner layer. | connection key, state key |
| index | noun | The map that pairs the reported key of a connection with the grouping key of it. `CleanupConnection` reads it. | lookup, reverse map, mapping |
| outer layer | noun | The address layer of the tunnel. A `FingerprintResult` reports it, and the time-to-live reads it. | tunnel header, outer header |
| inner layer | noun | The address layer of the packet the tunnel carries. The grouping key reads it. | inner header, payload header |
| monitor | noun | The `ja4plus watch` command, which reads packets from an interface until the operator stops it. | daemon, sniffer, listener |
| capture handle | noun | The object the monitor opens on one interface to read packets. | socket, listener, handle |
| capture backend | noun | The code that a build selects to open a capture handle. This project holds two. | driver, provider |
| pure-Go backend | noun | The capture backend that `pcapgo.NewEthernetHandle` provides. It builds on Linux and needs no cgo. | native backend, default backend |
| libpcap backend | noun | The capture backend that the `libpcap` build tag selects. It links libpcap through cgo and builds on macOS. | cgo backend, pcap backend |
| capture filter | noun | The Berkeley Packet Filter expression the `--bpf` option passes to the capture backend. | BPF, packet filter |
| drop count | noun | The count of packets the capture backend dropped. | loss, misses, drops |
| statistics line | noun | The one line the monitor writes to standard error to report its counts. | stats output, status line |
| stop request | noun | The flag that a termination signal sets and that the monitor reads after each packet. | shutdown flag, kill switch |
| documentation site | noun | The MkDocs site that GitHub Pages serves at `https://crank-git.github.io/ja4plus-go/`. | docs, website, manual |
| mutation | noun | One change that a sweep applies to one expression of one package under this module. The sweep runs the suite against the change and then reverts it. | mutant, tweak |
| sweep | noun | One run of the mutation tool over a named package set. It records the result of every mutation it applies. **The word names a mutation run alone, and it never names a fuzz run.** | campaign, mutation testing |
| fuzz target | noun | One `func FuzzX(f *testing.F)` that drives one entry point with an input the fuzz engine builds. `docs/specs/features/06-fuzz-testing.md` states the requirements. **The tree holds 15**, measured on 2026-08-14. | fuzzer, harness, fuzz test |
| seed | noun | One input of a fuzz target that this repository holds. `go test` replays every seed with no fuzz time, so a seed is a permanent regression guard. | sample, case, corpus entry |
| seed corpus | noun | The whole set of seeds of one fuzz target. It lives at `testdata/fuzz/<TargetName>/` under the directory of the package that holds the target, so this repository holds two such directories. | corpus, seed set, fixtures |
| property | noun | One rule that every fuzz target asserts for every input. `internal/fuzzprop` carries the five that FR-fuzz-14 through FR-fuzz-18 state. **This row bars `assertion` as a name for one of the five, and it bars no other use of that word.** A Go type assertion keeps its name, because the Go specification states it. | invariant, assertion, check |
| nightly fuzz run | noun | One run of `.github/workflows/fuzz.yml`, which fuzzes each target for 10 minutes in a matrix job of its own. **A schedule starts it only from the default branch.** | nightly sweep, fuzz campaign, long fuzz |
| candidate | noun | One test that no mutation of one sweep makes fail. | survivor, weak case |
| clean environment | noun | One module cache and one build tree that hold no source of this repository. `features/16-pre-release-validation.md` states the cases that build it. | fresh environment, sandbox |
| fetch | verb | To download the corpus from the reference at the pinned commit. | pull, sync, grab |
| close | verb | To change the code so that a finding or a deviation no longer exists. | fix, resolve, address |
| guard | noun | One test that fails when a fact a document states and a fact the repository holds disagree. | check, assertion, sentinel |
| autostash | noun | The stash entry that git creates for the `--autostash` option of `git merge`, `git rebase` or `git pull`. git records it in `MERGE_AUTOSTASH` or in `autostash`, and it writes `refs/stash` when the entry cannot re-apply. | auto stash, temporary stash |
| conform | verb | To produce the same output as the FoxIO reference. | comply, match spec |
| emit | verb | To return a fingerprint from a fingerprinter. | output, produce, yield |
| decline | verb | To choose not to reproduce a FoxIO value or not to build a capability, and to record the choice in the register. A reader that returns no value and no error for an input of the other role is a separate meaning, and `docs/specs/features/05-conformance-gaps.md` FR-gaps-27 carries that sense. That meaning writes no register entry, and `DecryptQUICInitialCrypto` of `internal/parser/quic.go` is the one function that holds it. | reject, skip, ignore |
| batch | noun | One set of issues that the project manager builds together and merges as one. It never names a JA4SSH window, and row `window` above declines the word for that meaning. | group, wave, set |
| member | noun | One issue of a batch, and the pull request that builds it. | task, item, sub-issue |
| cross-member review | noun | One read-only review of the merged result of a whole batch, which looks for a defect that no member reaches alone. `.claude/rules/cross-member-review.md` states the return contract. That file states one rule for the short word: `review` inside it always names a cross-member review, and never an audit. | batch review, integration review, merge review |
| mailbox | noun | The destination that one session addresses a cross-session agent at. A spawn response that names a mailbox reached the cross-session path. | inbox, message queue, channel |
| idle signal | noun | The message a cross-session agent returns in place of text, to report that it holds no work. | idle notification, heartbeat, ping |
| spawn path | noun | The mechanism a spawned agent returns its result through. This project measures two: the in-process path, which delivers a result to the spawner without a request, and the cross-session path, which delivers a result to the session that addresses the agent, and which can return an idle signal and no text instead. Row `mailbox` above declines the word `channel`, and this project uses that word for no meaning. | spawn mode, return path |
| uncovered value | noun | One value that the library produces, and whose vector file publishes no key for the method. The conformance run counts it apart from a match and apart from a deviation, and it fails no gate. The register may hold a decline for one. | unmatched value, unreached value, skipped value |
| one-shot function | noun | The exported function that computes one fingerprint from one packet and holds no connection state. `ComputeJA4T` is one. A multi-packet method reaches no one-shot function. | convenience function, helper, single-packet API, stateless function |
| multi-packet method | noun | One method that reads more than one packet of a connection and holds connection state between them. JA4L, JA4LS and JA4SSH are the three, and each one reaches no one-shot function. A method that reassembles one message from more than one packet is not one. The word `multi-packet` also describes such a message, and that meaning names no method. | stateful method, multi-packet state, connection-state method |
| stable target | noun | The name that an internal citation points at, which survives an edit above it. A section heading, a rule number, a requirement number and an identifier are the four. A line number is not one. `.claude/rules/ste.md` `## How a citation names its target` states the rule, and an evidence citation keeps `file:line` instead. | anchor, stable anchor, durable reference |
| pipelined pair | noun | One byte range that holds a complete HTTP request and then a second request. The maintainer ruled on 2026-08-14 that the library produces one JA4H value for one, and `ja4h_pipelined_request_test.go` holds that ruling. Two requests in two byte ranges are no pipelined pair, and each one reaches a value. | pipelined request, request pair, double request |

## Goals

1. Every method produces the fingerprint that the FoxIO reference produces, for every
   capture in the corpus, except where the register records a decline.
2. This project and the port produce the same fingerprint for every capture in the
   corpus, and expose the same interface where FoxIO specifies none.
3. Eleven methods are implemented. JA4LS is the eleventh.
4. The concurrency contract is documented on every exported type, and a race test proves
   it.
5. The license files state the FoxIO terms for the FoxIO-licensed methods.
6. CI gates conformance, race detection, fuzz testing, vulnerability scanning, lint and
   test coverage on every pull request.
7. The documentation site publishes to GitHub Pages on a push to the live branch.
8. The library releases as `v1.0.0` with a frozen exported API, and the release carries
   binaries for five platforms.
9. Every ruling carries a register entry or a test, and every reading cites its source.

## Non-goals

**The rule for this section is capability.** A capability that Go can achieve is in
scope. Only a capability that Go cannot achieve, or that FoxIO defines too little for any
implementation to achieve, stays out.

- **JA4TScan is out of scope, because FoxIO publishes nothing to implement.** A read of
  `FoxIO-LLC/ja4` on 2026-08-11 reports that `technical_details/` holds material for ten
  of the methods FoxIO names, and holds no file for JA4TScan. FoxIO ships no Python, no
  Rust, no Zeek and no Wireshark implementation of it. The name appears in `License FAQ.md:5`, in
  `LICENSE:3` and in the FoxIO `README.md:293`, which describes it as an Active TCP
  Fingerprint Scanner. **A goal of one-to-one with FoxIO cannot be met for a method that
  FoxIO does not define**, so no amount of Go capability closes this. The ruling is
  reversible, and it reverses when FoxIO publishes a format. The port declined the same
  method for a different reason, and `Parity with ja4plus` records the difference.
- **JA4Scan carries no ruling.** `License FAQ.md:5` names `JA4Scan`, `LICENSE:3` spells
  it `JA4SScan`, and the FoxIO `README.md:293` names neither spelling. FoxIO publishes no
  format and no implementation. **This project decided nothing about it, so this section
  states no reason.** A stated reason would assert a ruling that no round holds.
- **JA4E carries no ruling, for the same cause.** `LICENSE:3` is the one FoxIO record
  that names it.
- The project does not run the port. No test in this repository builds, imports or
  executes Python. `Parity with ja4plus` rule 3 states why.
- The project does not change the port. A parity gap found here produces an issue in
  `Crank-Git/ja4plus`, and no code in this repository.
- The project does not seek a commercial license from FoxIO as part of this work.
- Wire-speed performance is out of scope. The project measures throughput and records it,
  and sets no target for `v1.0.0`.

### What round 3 moved out of this section

Round 2 held three non-goals that round 3 removes, because Go can achieve each one.

| Round 2 non-goal | Why it is now in scope | Feature set |
|---|---|---|
| "The project does not add an eleventh method. JA4E, JA4LS, JA4SScan and JA4TScan stay out of scope." | JA4LS is a defined FoxIO method with published reference values, and the port implements it. The other three stay out, and the bullets above hold the reason. | `features/12-ja4ls.md` |
| "The project does not add live network capture." | `gopacket/pcapgo` provides a pure-Go capture handle on Linux, and the `libpcap` build tag reaches macOS. | `features/13-live-capture.md` |
| "The project does not become a network monitor." | The same reason. `GetShardKey` and `CleanupConnection` now serve a monitor this project ships, as well as one the caller writes. | `features/13-live-capture.md` |

## Users & personas

| Persona | Who they are | What they need | What they may do |
|---|---|---|---|
| Library author | A Go developer who embeds `ja4plus-go` in a network monitor. | A stable API, a documented concurrency contract, and fingerprints that match every other JA4+ tool. | Import the module and call the exported API. |
| Analyst | A network analyst who runs `ja4plus` against a capture. | Correct output, and a fingerprint that they can search for in other tools. | Run the command-line program. |
| Monitor operator | An operator who runs `ja4plus watch` against a live interface. | A process that reads an interface, reports its drop count, and never exhausts memory. | Open a capture handle. This needs elevated privileges on the host. |
| Maintainer | The person that row `maintainer` of `## Terms` names. | Evidence that a change broke no conformance and no parity. | Merge, tag and release. |

## Feature map

| Feature set | Spec file | Epic | Mockups |
|---|---|---|---|
| Foundation | `features/00-foundation.md` | Epic 0: Foundation | — |
| License compliance | `features/01-licensing.md` | Epic 1: License compliance | — |
| Correctness audit | `features/02-correctness-audit.md` | Epic 2: Correctness audit | — |
| Concurrency contract | `features/03-concurrency.md` | Epic 3: Concurrency contract | — |
| Conformance harness | `features/04-conformance-harness.md` | Epic 4: Conformance harness | `mockups/01-conformance-report.html` |
| Conformance gap closure | `features/05-conformance-gaps.md` | Epic 5: Conformance gap closure | — |
| Fuzz testing | `features/06-fuzz-testing.md` | Epic 6: Fuzz testing | — |
| Supply chain and CI gates | `features/07-supply-chain.md` | Epic 7: Supply chain and CI gates | — |
| FoxIO reference material | `features/11-foxio-reference.md` | Epic 11: FoxIO reference material | — |
| Parity with the port | `features/08-python-parity.md` | Epic 8: Parity with the port | — |
| JA4LS | `features/12-ja4ls.md` | Epic 12: JA4LS | — |
| Database lookup | `features/09-database-lookup.md` | Epic 9: Database lookup | — |
| Live capture | `features/13-live-capture.md` | Epic 13: Live capture | `mockups/03-watch-output.html` |
| Documentation site | `features/14-documentation.md` | Epic 14: Documentation site | `mockups/04-docs-site.html` |
| Mutation sweep | `features/15-mutation-sweep.md` | Epic 15: Mutation sweep | — |
| Pre-release validation | `features/16-pre-release-validation.md` | Epic 16: Pre-release validation | — |
| API freeze and release | `features/10-release.md` | Epic 10: API freeze and release | `mockups/02-cli-output.html` |

## Architecture & stack

### Components

| Component | Path | Responsibility |
|---|---|---|
| Public library | `*.go` at the repository root, package `ja4plus` | One fingerprinter per method, plus `Processor` and the local database lookup. It imports no HTTP client. |
| Remote lookup | `ja4db/`, package `ja4db` | The request to `ja4db.com` for the record of one fingerprint. It is the one package of the library that reaches the network. |
| Parser | `internal/parser/` | Protocol decoding for TLS, QUIC, HTTP, SSH, TCP streams, X.509 and GREASE. |
| Database cache | `internal/dbcache/` | The validation of a downloaded database, the 16 MB bound and the atomic cache write. |
| Capture backend | `internal/capture/` | New. Opens a capture handle on one interface. Holds the pure-Go backend and the libpcap backend. |
| Command-line program | `cmd/ja4plus/` | Reads a capture or an interface, and prints fingerprints. |
| Embedded database | `data/ja4plus-mapping.csv` | The FoxIO fingerprint-to-application mapping. |
| Corpus | `testdata/foxio/` | The fetched FoxIO captures and vectors. Not tracked in git. |
| Register | `testdata/deviations.json` | Tracked. One entry per accepted difference from a FoxIO value. |
| Reference material | `docs/specs/foxio/` | Tracked. The transcriptions and the readings that every ruling cites. |
| Documentation site | `docs/`, `mkdocs.yml` | The pages GitHub Pages serves. |

### Data flow

1. The caller reads a packet with `gopacket`, or the monitor reads one from a capture
   handle.
2. The caller passes the packet to `Processor.ProcessPacket`.
3. The processor calls every fingerprinter in turn.
4. Each fingerprinter decodes the packet through `internal/parser`.
5. Each fingerprinter updates its state table and returns zero or more results.
6. The processor returns the joined results and the non-fatal errors.
7. When the packet source ends, the caller calls `Processor.CloseOpenWindows`, which
   returns the results that the open windows hold.

### Key choices

| Choice | Alternative | Why this choice wins |
|---|---|---|
| `github.com/gopacket/gopacket` v1.6.1 | `google/gopacket` v1.1.19 | The original published no release since 2020-10-19. The maintainer decided the move on 2026-08-13, and #438 carried it. No fingerprint value moved. `docs/audit/dependency-decision.md` records the decision. |
| Pure-Go capture reading through `pcapgo` | `libpcap` through cgo for every build | Commit `e32e49e` removed the cgo dependency. The default build cross-compiles to five platforms with no C toolchain. |
| A `libpcap` build tag for the macOS monitor | No macOS monitor, or cgo in every build | `pcapgo/capture.go` carries the build constraint `linux,go1.9`, so the pure-Go handle reaches Linux alone. The tag keeps cgo off the default build and off every released binary. `features/13-live-capture.md` states the containment. |
| Go 1.24 as the module floor | Go 1.22, Go 1.23 | Go 1.22 has left upstream security support. `go.mod` still declares `go 1.22`, so Epic 0 moves it. |
| Fetch the corpus at a pinned commit | Commit the corpus into the repository | The corpus is FoxIO-licensed material. A fetch avoids redistribution, and a pin keeps the result reproducible. |
| A separate `SyncProcessor` wrapper | Lock every fingerprinter | The lock-free core keeps the per-packet path fast for the shard-per-goroutine design that `GetShardKey` implies. |
| MkDocs with the Material theme | Hugo, a pure-Go generator | The port publishes the same site, and a reader moves between the two. The prose pages port across with no reformatting. |
| A hand-written API reference that links to `pkg.go.dev` | A generator such as `gomarkdoc` | Go already publishes a canonical API surface at `pkg.go.dev`. A second generated copy drifts, and a drift check costs more than the pages are worth. |
| GoReleaser for the release | The hand-rolled build matrix in `release.yml` | One configuration file replaces the inline shell, and it produces the checksums, the software bill of materials and the release notes. |

Verified against <https://pkg.go.dev/github.com/google/gopacket/pcapgo> (v1.1.19),
<https://www.mkdocs.org/user-guide/configuration/> (MkDocs 1.6),
<https://squidfunk.github.io/mkdocs-material/> (Material 9.7.7) and
<https://goreleaser.com/customization/> (GoReleaser v2), retrieved 2026-08-11.

### Deploy target

The library has no runtime deployment. It produces four release artifacts.

| Artifact | Where it goes | Who publishes it |
|---|---|---|
| The Go module | `proxy.golang.org` | The `v1.0.0` tag. |
| Five platform binaries and their checksums | The GitHub release | GoReleaser, from the tag. |
| The package documentation | `pkg.go.dev` | The module proxy. |
| The documentation site | `https://crank-git.github.io/ja4plus-go/` | `.github/workflows/docs.yml`, on a push to `master`. |

## Parity with ja4plus

The port is the Python implementation at `Crank-Git/ja4plus`. It is at version `v1.1.0`.
The two implementations must not drift apart, because a user who runs both must get one
answer.

**The port's own spec holds the register that governs this section.** Its `Parity with
ja4plus-go` section carries about thirty rows, and about twenty of them end with a
sentence of the form "The port must X". In the port's vocabulary "the port" names this
repository. **Those sentences are the work list of Epic 8.**

Three rules govern parity. **These are the port's rules, adopted here without a change,
so that one rule set governs both repositories.**

1. **FoxIO decides behavior.** Where FoxIO specifies the output, the vectors decide.
   This rule outranks the port. Where this project disagrees with a vector, this project
   is wrong.
2. **The port decides interface where this project shipped nothing.** Where FoxIO
   specifies nothing, and where this project has no exported name for the behavior, this
   project adopts the port's choice rather than inventing a second one. **Where this
   project shipped a name first, rule 2 runs the other way and the port adopted it.** The
   register records which side each row followed.
3. **The gate is the shared vector set.** Both repositories read the same FoxIO vectors,
   pinned to the same upstream commit. **No test in this repository builds, runs or
   imports the port.** A cross-language test rig couples two repositories that move at
   different speeds, and it fails for reasons that have nothing to do with the change
   under test.

Rule 3 reverses a decision of round 2. `features/08-python-parity.md` held FR-parity-8
through FR-parity-14, which specified a test that runs the Python library over the corpus
and compares the two outputs as strings. Round 3 deletes those seven requirements.

### The register

Each row states what the port ruled, what this project does today, and what this project
must change. The **Ruling** column names the port issue that holds the ruling and the
measurement. **A bare number in that column therefore names an issue of the port.** A cell
that names an issue of this repository says so. `.claude/rules/rulings.md` holds the
convention for the rest of the tree. Every ruling is reversible, and a reversal happens in
both repositories or in neither.

Rows are grouped by method. `features/08-python-parity.md` numbers each row as a
requirement and holds the acceptance criteria.

#### JA4 and JA4S

| Item | This project today | What must change | Rule | Ruling |
|---|---|---|---|---|
| ALPN value for a first byte outside the printable ASCII range `0x20-0x7E` | **Writes `99`.** #50 built it, and the maintainer ruled the condition on 2026-08-12. | Done. **The condition read `not alphanumeric` until that ruling.** Both FoxIO implementations pass a printable byte through, so `\x20\x61` reads ` a` in each one. The register row and FR-parity-8 adopted the range, and the code did not move. `Crank-Git/ja4plus#601` closes the same wording on the port side. | 1 | #127, #141 |
| ALPN value for a last byte outside the printable ASCII range `0x20-0x7E` | **Writes `99`.** #50 built it. | Done. **This row read `a byte outside 0x20-0x7E in a position other than the first` until the ruling of 2026-08-12.** The last byte is the one position other than the first that the field reads, and the old title also covered a middle byte. `\x30\xab\xcd\x31` reads `01` in both FoxIO implementations and in this library, so a middle byte writes no character. This is a reference split. FoxIO Python writes `U+FFFD`, which is a character no packet byte holds. FoxIO Rust writes the `tshark` escape text. Neither reads the packet. | 1 | #141, #162, #522 |
| ALPN value for a first ALPN value of one byte | **Repeats the byte and writes `hh`.** #50 measured it. | Done. FoxIO Python writes `h`, which cannot fill a two-character field. FoxIO Rust writes `h0`. No vector separates them. **This row keeps the alphanumeric test**, because the two implementations dispute every one-byte value. A one-byte value outside the alphanumeric ranges writes `99`. | 1 | #141, #162, #522 |
| A structurally valid ClientHello whose body describes no real client | Produces a fingerprint. The library holds no plausibility guard. | No change. **Hold the current behavior and add a test that a guard would fail.** The port measured 5000 random bodies: all 5000 produced a well-formed fingerprint, and a guard on the two separating conditions would move no vector and would still admit about one fabricated value in twenty-five hundred. | 1 | #338, #343 |

#### JA4L and JA4LS

| Item | This project today | What must change | Rule | Ruling |
|---|---|---|---|---|
| JA4LS | **Implemented.** `processTCP` and `processUDP` of `ja4l.go` each emit the server value, and `emitResult` of the same file sets `Type: "ja4l"` for every result. **The row read `Not implemented` and cited `ja4l.go:198` until 2026-08-13, and both statements were wrong.** **The row cited `ja4l.go:183` and `ja4l.go:381` until 2026-08-13, and #447 then moved the second write to `ja4l.go:413`.** A cell that names a function survives the next edit, and a cell that names a line does not. #440 holds that convention. | Done. **The type stays `ja4l`, and the `JA4L-S=` label names the method.** The maintainer ruled on 2026-08-13 that JA4LS reaches no type of its own, because one connection has one owner. `ja4plus/processor.py:106` at `v1.1.0` registers `("ja4l", JA4LFingerprinter)`, so the port writes one type for both methods. | 1 | `features/12-ja4ls.md`, and this repository's #60 |
| The client value on the return path | Not measured. | Report one client value for one connection. The port's return path repeats the value and its stored list holds one, and its issue #156 measured every candidate end-of-connection point. | 1 | #156 |
| The part count | Not measured. | Write two timing parts on a TCP connection. This is a reference split: the image and two implementations write three parts, and two implementations write two. All 114 JA4L values of the port's vector set hold two. | 1 | #225 |
| The protocol marker on a QUIC connection | Not measured. | Write `quic` as the third part of both values on a QUIC connection. **Follow the Wireshark spelling.** The Zeek package appends `q`, and the port's rule declines the Zeek spelling. Do not write the `tcp` literal that the dissector writes on an HTTP connection. | 1 | #225 |
| The server value on a retransmitted SYN-ACK | Not measured. | Emit one server value for one connection. A retransmitted SYN-ACK finds the measurement point set, and it gives no value. | 1 | #272 |
| A reference file that holds no JA4L key | **Records 8 value declines.** #361 built the comparison that reaches them, and the maintainer ruled the category on 2026-08-13. **#361 wrote 28 entries, and #447 then removed 20 of them**, because the library no longer produces the value each one records. `CHANGELOG.md` records that removal, and #486 counted 8 entries under ruling `#361` at the branch head. | Done. **The row read `Record five value declines` until #361, and it read `28` until #486.** The FoxIO run deleted the key, so the reference file publishes no value to compare. The harness now reports such a value as an uncovered value, which is neither a match nor a deviation. **The port writes five keys, and this repository writes 8.** A port key names a capture and a method, and a key here names a capture, a stream and a method. This row changes no fingerprint. | 1 | #200, #272 |

#### JA4SSH

| Item | This project today | What must change | Rule | Ruling |
|---|---|---|---|---|
| The window threshold | **Emits at `packetCount`, which defaults to 200.** `checkWindow` of `ja4ssh.go` reads `f.packetCount` and it holds no upper cap. | Done. **The cell read that `ja4ssh.go:176-180` caps the threshold at `min(packetCount, 10)` until 2026-08-13, and the tree held no such cap on that date.** **The port names the cap a defect of this repository**, and its issue #28 fixed the same shape on its own side. | 1 | #28 |
| The mode field | Not measured. | Read the mode field from the window alone. | 1 | #96 |
| A window that holds no SSH packet | Not measured. | Emit nothing. | 1 | #97 |
| The window that a connection holds open at the end of a capture | Not measured. No equivalent method exists. | Emit it. Add `CloseOpenWindows`, on every fingerprinter and on `Processor`. **The name follows the port, under rule 2.** The FoxIO references split two against two, and the maintainer followed the Rust reference and the Zeek package. Two FoxIO reference values verify the result. | 1, 2 | #105, #199, #214 |

#### JA4T and JA4TS

| Item | This project today | What must change | Rule | Ruling |
|---|---|---|---|---|
| The empty option list, the absent maximum segment size and the zero window scale | `ja4t.go:68`, `ja4t.go:73` and `ja4t.go:87` write `%d` with no padding and no sentinel. | Write the two-digit form. An empty option list writes `00`, part c writes two digits, and a window scale of zero writes `00`. The Wireshark dissector and the Zeek package both write it, and the deleted `technical_details/JA4T.md` corroborates it in prose. | 1 | #215 |
| Part e of JA4TS | `ja4ts.go` writes it. **Closed by #56.** | Write part e. A connection the server answered twice or more carries the delay of each SYN-ACK after the first, in whole seconds. A connection the server answered once omits part e. | 1 | #226 |
| The JA4TS value that a RST produces | `ja4ts.go` writes it. **Closed by #56.** | Append `-R` and the delay of the RST to part e, on a connection that already holds a delay. Read part a through part d from the first SYN-ACK. **Test the RST bit rather than the whole flag byte**, so a RST that also carries ACK reaches the rule. A RST on a connection with no delay produces no value, and a client RST produces no value. | 1 | #246 |

#### JA4H, JA4X, JA4D and JA4D6

| Item | This project today | What must change | Rule | Ruling |
|---|---|---|---|---|
| The JA4H emission frame | **Produces the value at the packet that completes the request.** #462 built it, and the maintainer ruled the frame on 2026-08-13. `HTTPMessageIsComplete` of `internal/parser/http.go` holds the value until the payload after the header block reaches the byte count that `Content-Length` names. **A request whose body never completes reaches no value**, and `ja4h_body_gate_test.go` records that half of the ruling because no vector separates the two answers. | **The port must move the emission frame.** The port produces the value at the packet that ends the header block: `ja4plus/fingerprinters/ja4h.py:149-166` at `v1.1.0` reads `header_block_end(stream_data)` and emits at once. **So a user who runs both implementations reads the JA4H value on two different frames of the same capture.** The value agrees and the frame differs. `Crank-Git/ja4plus#607` carries the port half, and it is open. **This row moved a fingerprint on this side.** #462 closed 96 per-packet deviations into 48 comparisons, of which 16 are matches and 32 are accepted deviations under ruling #285. | 1 | This repository's #455, `Crank-Git/ja4plus#607` |
| JA4H part b of a request that carries no header | **Hashes the empty header list, and writes `e3b0c44298fc`.** #527 built it, and the maintainer ruled the reference split on 2026-08-14. R18 of `docs/specs/foxio/JA4H.md` is a rank 1 image rule, it states `Truncated SHA256 hash of Headers, in the order they appear`, and it names no sentinel. R27 confines the zero sentinel to part c and to part d. R28 records the ruling, and `TestJA4HPartBHashesAnEmptyHeaderList` holds the value. | **The port must stop writing the sentinel.** `ja4plus/fingerprinters/ja4h.py:480` at `v1.1.0` writes `"000000000000"` when the header string is empty. **So a user who runs both implementations reads two JA4H values for a request that carries no header.** `Crank-Git/ja4plus#612` carries the port half, and it is open. **This row moved a fingerprint on this side.** 1 comparison of the corpus closed: `gre-erspan-vxlan.pcap/4/JA4H.1` now matches `ge10nn000000_e3b0c44298fc_000000000000_000000000000`. The corpus holds 215 JA4H values, and 1 of them reads a header count of `00`. **The two raw forms of that frame became reachable, and #527 wrote a register entry for each one under ruling #285.** | 1 | This repository's #527, `Crank-Git/ja4plus#612` |
| An empty JA4X object identifier list | **Hashes the empty list, and writes `e3b0c44298fc`.** #582 built it, and the maintainer ruled the reference split on 2026-08-14 as part of #527. R12 of `docs/specs/foxio/JA4X.md` transcribes the split: `rust/ja4x/src/lib.rs:171` and `wireshark/source/packet-ja4.c:590` write the zero sentinel `000000000000`, and `python/ja4x.py:87` hashes the empty string. The deciding rule is R18 of `docs/specs/foxio/JA4H.md`, which states a hash of the list and names no sentinel. **The ruling reaches part a, part b and part c together**, because one rule governs all three. `computeJA4XWithRaw` in `ja4x.go` calls `parser.TruncatedHashNoSentinel` at each of the three sites, and `ja4x_empty_oid_list_test.go` builds the separating certificate for each part. | **The port must stop writing the sentinel.** `ja4plus/fingerprinters/ja4x.py:70-78` at `v1.1.0` writes `"000000000000"` in each of the three parts, and `:71` states `# Use '000000000000' sentinel for empty values (consistent with other fingerprinters)`. **So a user who runs both implementations reads two JA4X values for a certificate with an empty list.** `Crank-Git/ja4plus#619` carries the port half, and it is open. **This row moved no fingerprint of the corpus.** The corpus holds 120 JA4X values across both vector sets, and none of them holds `000000000000` in any part. The eight conformance figures are equal before and after: 38 captures, 1754 matches, 267 deviations the register does not hold, 585 accepted deviations, 175 unaccepted uncovered values, 32 accepted uncovered values, 0 stale entries and 0 orphan entries. **The register key count is 617 before and after**, because no vector separates the two answers and the ruling therefore carries a test rather than a register entry. | 1 | This repository's #582, `Crank-Git/ja4plus#619` |
| A JA4H request path that holds a space | **Reads the path.** #527 built it. The path group of `requestLineRe` in `internal/parser/http.go` read non-space characters, so frame 4 of `gre-erspan-vxlan.pcap`, which holds `GET /Hello Arkime HTTP/1.0`, reached no value. **The expression reads the first line and no other line.** Each separator reads a space or a horizontal tab, and the path group reads no carriage return and no line feed. | **The port carries the same gap.** `ja4plus/utils/http_utils.py` at `v1.1.0` holds `REQUEST_LINE_PATTERN` with the same non-space path group. `Crank-Git/ja4plus#612` states both rules of #527. **This row moved no fingerprint on its own.** Rule 5a and the part b rule above close one comparison together, and neither one closes it alone. | 1 | This repository's #527, `Crank-Git/ja4plus#612` |
| The JA4H method code | **Reads the first two characters of any method token.** #57 measured it and built it. `ja4h.go:201-205` already lower-cased the token, and `internal/parser/http.go:22` named nine methods, so a `PROPFIND` request and a `MKCOL` request reached no JA4H value at all. | Done. #57 widened the request-line pattern to the method token character set that RFC 9110 section 5.6.2 names, and `IsHTTPRequest` keeps the nine-prefix fast path before it matches a whole request line. `ja4h_method_code_test.go` holds the reading. | 1 | #219 |
| JA4X on a stream that a proxy tunnel carries | **Reads the record layer without regard to the tunnel protocol.** #57 measured it: no port test gates the read, and the SOCKS4 tunnel of `socks4-https.pcap` produces three JA4X values on port 9901. **Two defects held those values back, and neither one names a tunnel.** The reader concatenated the segments in arrival order, and it read the first handshake message of a TLS record alone. | Done for the behavior. #57 gave JA4X the `parser.TCPStreamReassembler` that `ja4h.go:18` already uses, and it walks every handshake message of a record. **The measurement repaired 30 comparisons across `browsers-x509.pcapng`, `latest.pcapng`, `ssh2.pcapng` and `tls-handshake.pcapng`**, each of which held a reference value the library produced nothing for. **The three SOCKS4 value declines are closed, and this repository's #375 wrote them.** No FoxIO reference publishes a JA4X key for that capture, so the run reports each value as an uncovered value. #361 built the machinery that makes the entry reachable. **The three values reach twelve register entries**, because the run compares `JA4X` and `JA4X_r` in the per-stream set and in the per-packet set. | 1 | #138 |
| Subfield 2 of JA4D when a message repeats option 57 | **Wrote the last Maximum DHCP Message Size.** #57 measured it: `ja4d.go:117` overwrote the value on each occurrence. | Done. #57 writes the first one, in four digits. The dissector appends each occurrence to one buffer, which gives a part a of fifteen characters where the image gives eleven. No vector repeats option 57, so `ja4d_readings_test.go` holds the separating packet and no fingerprint moved. | 1 | #231 |
| JA4D on a BOOTP message that carries no option 53 | **Emits no value.** #57 measured it at `ja4d.go:131-133`. | Done. No change to the code. Two FoxIO references against one, and the Zeek author recorded their own doubt in a comment. `ja4d_readings_test.go` holds the separating packet. | 1 | #231 |
| Subfield 1 of JA4D6 on a relay message | **Writes the outer DHCPv6 message type alone.** #57 measured it, and `ProcessPacket` of `ja4d6.go` reads `dhcp.MsgType`. **The cell cited `ja4d6.go:138-141` until 2026-08-14, and #370 moved that read.** **The cell also stated that a one-level relay and a two-level relay give one value, and #370 falsified that sentence.** `TestJA4D6ReadsTheInnerMessageOfARelayMessage` reads `rlayf0014nn_9-1-6_23-24` for the first and `rlayf0014nn_9-9-1-6_23-24` for the second. | Done for subfield 1. **The cell read `Done. No change to the code.` until 2026-08-14, and #370 changed `ja4d6.go` on that day.** The dissector writes both the outer and the inner type, which gives sixteen characters. `ja4d6_relay_message_test.go` holds the separating packets, and it builds the two-level case. **Part b, part c and subfield 2 of part a now read every option of the inner message, at a nesting depth bounded at 32.** #370 built that rule, and the port ships it at `ja4plus/fingerprinters/ja4d6.py:161-165` at tag `v1.1.0`. **The four subfield options also read the `IA_NA`, `IA_TA`, `IA_PD`, IA Address and IA Prefix containers**, which is wider than the title of #370. `TestJA4D6ReadsASubfieldOptionOfANestedContainer` holds it. **No fingerprint value of the corpus moved**, because no FoxIO vector carries a relay message. | 1 | #271 |

#### Interface

| Item | This project today | What must change | Rule | Ruling |
|---|---|---|---|---|
| Result type | `FingerprintResult` struct with 9 fields. | No change. **The port adopted this field set**, including `Timestamp`, under rule 2. | 2 | — |
| Results per packet | A slice of results. | No change. The port adopted it. | 2 | — |
| Parse failures | Returned as a second value. | No change. The port adopted it. | 2 | — |
| Shard key format | `tcp:ip:port->ip:port`. | No change. Already at parity. Keep a test that proves it. | 2 | — |
| Mapping file refresh | `db update` and `db info`. | No change. Already at parity. Keep a test that proves it. | 2 | — |
| Remote lookup | One opt-in covers the local lookup and the remote call. | **Separate the local lookup from the remote lookup**, so the remote call needs its own opt-in. The port fixed the same shape in its Epic 7. `features/09-database-lookup.md` holds the work. | 2 | — |
| `CloseOpenWindows` | No equivalent method. | Add it. The port shipped `close_open_windows` first, so rule 2 names the port's word and this project writes the Go form of it. | 2 | #214 |
| `CloseConnectionWindow` | `JA4SSHFingerprinter`, `Processor` and `SyncProcessor` each export it. `ConnectionWindowCloser` declares it. | No change here. **This project named the behavior first**, so rule 2 runs the other way and the port adopts the name. The port exports no method that emits the window of one connection, and round 27 of the `## Changelog` below records that reading. This row moves no fingerprint. | 2 | This repository's #216, `Crank-Git/ja4plus#598` |
| The key-log interface | `types.go` exports `ErrNoSecret`, `KeyLog`, `ParseKeyLog`, `ReadKeyLogFromCapture`, `KeyLog.Secret`, `KeyLog.ClientRandoms`, `KeyLog.Len` and `DecryptQUICPacket`. | No change here. **This project shipped the interface first**, so rule 2 runs the other way and the port adopts these eight names. The port holds no key-log interface: a code search of `Crank-Git/ja4plus` for `keylog` returns no file. This row moves no fingerprint. | 2 | `Crank-Git/ja4plus#593` |

#### License and coverage

| Item | This project today | What must change | Rule | Ruling |
|---|---|---|---|---|
| The methods the FoxIO License 1.1 covers | The README claims BSD 3-Clause for the whole library, and links to a `LICENSE` file that does not exist. | Name the methods this project implements under the license, state that FoxIO's list is wider, and cite the pinned commit. **Assert no equality with FoxIO's list.** Three FoxIO records at the pinned commit name three different sets: `License FAQ.md:5` names twelve, the FoxIO `README.md:293` names nine, and `LICENSE:3` names thirteen and spells the scanner `JA4SScan`. | — | #388, #466 |
| JA4TScan | Not implemented, and round 2 declined it as an eleventh method. | Restate the decline. **The reason changes.** The port declined it as a capability boundary, because it sends crafted packets. This project declines it because FoxIO publishes no format and no implementation. The two repositories therefore hold one outcome and two readings, and this row records that. | — | #197 |

Verified against <https://github.com/Crank-Git/ja4plus> (`docs/specs/spec.md`, retrieved
2026-08-11, default branch `dev` at `v1.1.0`).

## Data model

The library holds no database. It holds in-memory state per connection, one embedded
lookup table, and one tracked register.

### `FingerprintResult`

| Field | Type | Meaning | Lifecycle |
|---|---|---|---|
| `Fingerprint` | `string` | The method output for the packet. | Created per packet. |
| `Raw` | `string` | The unhashed form, such as `JA4_r`. | Created per packet. |
| `RawOriginalOrder` | `string` | The wire-order unhashed form, `JA4_ro`. | Created per packet. Empty for methods that define no such form. |
| `Type` | `string` | The method name in lower case, such as `ja4h`. **`JA4LFingerprinter` writes `ja4l` for both of its methods**, and the `JA4L-C=` and `JA4L-S=` labels of the value tell them apart. The maintainer ruled that on 2026-08-13, in issue #60. | Created per packet. |
| `SrcIP`, `DstIP` | `string` | The packet addresses. | Created per packet. |
| `SrcPort`, `DstPort` | `uint16` | The packet ports. | Created per packet. |
| `Timestamp` | `time.Time` | The packet capture time. | Created per packet. |

### Fingerprinter state

| Fingerprinter | State it holds | Keyed by | Cleared by |
|---|---|---|---|
| `JA4Fingerprinter` | `quicFragments`, `dcidToTuple`, `results` | QUIC connection identifier | `Reset`, `CleanupConnection` |
| `JA4SFingerprinter` | `quicDCIDs`, `results` | QUIC connection identifier | `Reset`, `CleanupConnection` |
| `JA4HFingerprinter` | `reassembler`, `ranges`, `results` | Five-tuple. **`ranges` records the sequence range of each stream the fingerprinter already read, so a repeated TCP segment produces no second value. #446 built it, under an entry bound and an age bound.** | `Reset`, `CleanupConnection` |
| `JA4TFingerprinter` | `results` | — | `Reset` |
| `JA4TSFingerprinter` | `connections`, `results` | Five-tuple. **Built by #56. Part e and the RST value need the first SYN-ACK of the connection.** | `Reset`, `CleanupConnection` |
| `JA4LFingerprinter` | `connections`, `results` | Five-tuple | `Reset`, `CleanupConnection` |
| `JA4XFingerprinter` | `reassembler`, `processedCerts`, `certsByStream`, `streamBytes`, `lastCleanup` | Five-tuple, certificate hash | `Reset`, `CleanupConnection`, an interval sweep |
| `JA4SSHFingerprinter` | `connections`, `packetCount`, `arrivals`, `order`, `packets`, `handshakes`, `handshakeOrder`, `results` | Five-tuple, and an endpoint pair that encodes no direction. **#239 bounded `connections` with `order`, and #413 added `handshakes` and `handshakeOrder` for step 2 of the client direction. Each of the two tables holds an entry bound and an age bound.** | `Reset`, `CleanupConnection`, `CloseOpenWindows`, `CloseConnectionWindow` |
| `JA4DFingerprinter` | `results` | — | `Reset` |
| `JA4D6Fingerprinter` | `results` | — | `Reset` |

**This table went stale for the second consecutive batch, and the shape is the same both
times.** Batch #342 found it naming a field that a member had deleted, and batch #421 found
it naming none of the state that #239, #413 and #446 added. **A member that adds a field to a
fingerprinter must edit this table in the same change.** No test reads this table against the
package. **The `results` field of every row is separately false**, and #326 owns
that repair. This round leaves it alone.

Every fingerprinter appends to `results` and never truncates it. `CleanupConnection` does
not touch `results`. `features/02-correctness-audit.md` treats this as a suspected defect,
because `CleanupConnection` exists to stop state growth in a long-running process. **The
monitor makes that defect reachable**, so `features/13-live-capture.md` depends on the
audit closing it.

### The register

`testdata/deviations.json` holds one entry for each accepted difference from a FoxIO
value. The conformance suite reads it, and a comparison that the register names is
expected to differ. **An entry that no longer differs fails the suite**, so a closed
deviation cannot sit in the file unnoticed.

| Field | Type | Constraint |
|---|---|---|
| `key` | string | The capture, the stream and the method. The middle part holds the stream number in the per-stream set, the endpoint key where no vector entry names the connection, and the frame number in the per-packet set. The register holds each key once. |
| `capability` | boolean | `true` for a capability decline, `false` for a value decline. |
| `ours` | string | The value this project produces. |
| `theirs` | string | The value the FoxIO reference publishes. |
| `ruling` | string | The port issue or the local issue that holds the ruling. |
| `reason` | string | One sentence. |

**One key names one comparison.** The suite reads one register for both vector sets, and a
stream number reads exactly like a frame number. A key that named a comparison in each set
would therefore accept a difference that no ruling covers. FR-reference-30 fails the suite
for such a key, and `testdata/README.md` states both meanings of the middle part.

### Lookup database

`data/ja4plus-mapping.csv` maps a fingerprint to an application name, a type and a note.
The library embeds the file with `go:embed` and publishes one immutable table through
`atomic.Pointer[lookupTable]`. **#74 replaced `sync.Once` with that pointer on
2026-08-14**, so a process that updates the database reads the new table at its next
lookup. `internal/dbcache` validates a downloaded database and writes the cache file
atomically, and `github.com/Crank-Git/ja4plus-go/ja4db` holds the remote lookup.
`features/09-database-lookup.md` covers the reload path and the remote fallback.

## Cross-cutting concerns

### Concurrency

The core fingerprinters are not safe for use from more than one goroutine. The contract
after this work is explicit: one `Processor` serves one goroutine, and the caller routes
packets to processors with `GetShardKey`. `SyncProcessor` wraps a `Processor` and serves
callers who share one instance. **The monitor owns one `Processor` on one goroutine**, so
it needs no lock. `features/03-concurrency.md` holds the detail.

### Error handling

A fingerprinter returns a non-fatal error rather than a panic when a packet does not
decode. `Processor.ProcessPacket` collects the errors and returns them alongside the
results. A malformed packet never terminates the caller.

### Input validation

Every packet is untrusted input. Every parser bounds-checks every length field it reads
before it slices. The fuzz targets in `features/06-fuzz-testing.md` are the enforcement.

### Logging

The library writes nothing to standard output and nothing to standard error. The
command-line program owns all output. **The statistics line is command-line output**, and
`features/13-live-capture.md` places it in `cmd/ja4plus`.

### Rulings and evidence

- **A ruling carries a register entry or a test.** A ruling that neither records is a
  ruling a later reader cannot find.
- **A reading cites its source with a file and a line.** `docs/specs/foxio/` holds the
  transcriptions that a reading cites, and `features/11-foxio-reference.md` builds them.
- **Text copied from FoxIO material is verbatim.** `.claude/rules/ste.md` bars a
  rewording of it, because a reworded quotation is no longer evidence.

### Performance targets

- `Processor.ProcessPacket` allocates no more per packet after this work than before it.
- `SyncProcessor` adds one mutex acquisition per packet over `Processor`.
- The library holds no unbounded state for a connection that `CleanupConnection` removed.
- The project records the packet throughput of one named capture on one named host, and
  sets no target.

### Security posture

- The library performs no network input and no network output, except the opt-in database
  lookup that `features/09-database-lookup.md` covers.
- **The monitor is the one component that opens an interface.** It reads packets and
  sends none. `features/13-live-capture.md` states that boundary.
- The library reads no key material outside a capture file. **It reads a pcapng
  Decryption Secrets Block, because the block is part of the capture the operator already
  holds.** `features/05-conformance-gaps.md` holds that work.
- `govulncheck` gates every pull request.

### Accessibility and internationalization

The command-line program and the monitor write plain text. The documentation site inherits
the Material theme's contrast and keyboard behavior. No further work is in scope.

## Environments & config

The library reads no environment variable. The command-line program, the monitor and the
test suite read the following.

| Name | Read by | What it does | Default |
|---|---|---|---|
| `JA4PLUS_DB_CACHE` | `lookup.go` | The path of the downloaded database cache file. | An operating-system cache directory. |
| `JA4PLUS_DB_LOOKUP` | `cmd/ja4plus` | When set to `1`, the program allows the remote lookup. The `--lookup-remote` flag does the same. **The name follows the port, under rule 2.** | Unset. |
| `JA4PLUS_FOXIO_DIR` | The conformance suite | The directory that holds the fetched corpus. | `testdata/foxio` |
| `JA4PLUS_FOXIO_REF` | The fetch script | The FoxIO commit to fetch. | The pin in `testdata/foxio.pin` |
| `GITHUB_TOKEN` | The workflows | Provided by GitHub Actions. The documentation workflow publishes to GitHub Pages with it. | Provided. |

The project holds no secret. The release workflow and the documentation workflow use the
GitHub-provided token only.

Seed data is the corpus. `make corpus` fetches it. `features/04-conformance-harness.md`
defines the script.

## Testing strategy

| Layer | What it covers | Command |
|---|---|---|
| Unit | One function or one fingerprinter, with a hand-built packet. | `go test ./...` |
| Race | The concurrency contract, under the race detector. | `go test -race ./...` |
| Conformance | Every method against every FoxIO vector, and the register. | `go test -tags conformance ./...` |
| Ruling | Every ruling that no vector separates, against a constructed packet. | `go test -run TestRuling ./...` |
| Fuzz | Every parser entry point against generated input. | `go test -fuzz=Fuzz -fuzztime=60s ./internal/parser` |
| Benchmark | The per-packet path, for allocation and time. | `go test -bench=. -benchmem ./...` |
| Mutation | The test cases that no mutation makes fail. | `make mutate` |
| Documentation | Every internal link of the site, and every code sample. | `mkdocs build --strict` |
| Pre-release | The released binary and `go install` from a clean environment. | `make prerelease` |

A change is done when six things are true.

1. `go build ./...` succeeds on the module floor.
2. `go test -race ./...` passes.
3. `make lint` reports nothing. Run the linter through `make lint`, and never as a bare
   `golangci-lint run`. A bare run reads the linter cache of the whole user account, and
   #257 records the false failure that cache produces.
4. The conformance suite reports no new deviation, and the register holds no closed entry.
5. Coverage does not fall below the recorded floor.
6. `mkdocs build --strict` succeeds when the change touches a page.

## Epics

Epics 0 through 10 keep their round 2 numbers, so an existing issue keeps its epic. Round
3 adds Epics 11 through 16. **Epic 11 runs before Epic 8**, because Epic 8 cites the
material that Epic 11 writes.

### Epic 0: Foundation

**Goal.** Make the repository able to run every gate that the later epics add.

**Covers.** `features/00-foundation.md`

**Depends on.** Nothing.

**Exit criteria.** A fresh clone runs `make test`, `make lint`, `make bench`,
`make corpus` and `make conformance`. The `dev` branch exists. `.golangci.yml` pins the
linter set. **`go.mod` declares Go 1.24**, which it does not today. `CLAUDE.md` and
`.claude/` are tracked.

### Epic 1: License compliance

**Goal.** State the correct license terms for every part of the repository.

**Covers.** `features/01-licensing.md`

**Depends on.** Nothing. This epic runs first among the content epics, because the release
cannot ship without it.

**Exit criteria.** `LICENSE` and `NOTICE` exist. The README, the package documentation and
`data/ja4plus-mapping.csv` carry the FoxIO attribution. No document claims that the
FoxIO-licensed methods are BSD 3-Clause. **The README names the methods it implements
under the license and asserts no equality with FoxIO's list.**

### Epic 2: Correctness audit

**Goal.** Find and close every logic defect in the library, the parser and the
command-line program.

**Covers.** `features/02-correctness-audit.md`

**Depends on.** Epic 0.

**Exit criteria.** Every file has an audit record. Every confirmed finding is closed and
has a regression test. The findings report is committed at `docs/audit/findings.md`.

### Epic 3: Concurrency contract

**Goal.** Make the concurrency behavior explicit, correct and tested.

**Covers.** `features/03-concurrency.md`

**Depends on.** Epic 0. Runs alongside Epic 2.

**Exit criteria.** Every exported type documents its contract. `SyncProcessor` exists. A
race test drives `SyncProcessor` from many goroutines and passes under `-race`. A second
race test proves the shard-per-goroutine pattern.

### Epic 4: Conformance harness

**Goal.** Test every method against the FoxIO reference vectors.

**Covers.** `features/04-conformance-harness.md`

**Depends on.** Epic 0.

**Exit criteria.** `make corpus` fetches the corpus at the pinned commit. The harness reads
both the per-stream vectors and the per-packet vectors. **The harness reads the register,
and a comparison the register names is expected to differ.** The harness reports one row
per capture and per method.

### Epic 5: Conformance gap closure

**Goal.** Make every vector match, or record a decline.

**Covers.** `features/05-conformance-gaps.md`

**Depends on.** Epic 4 and Epic 11. The deviation list that Epic 4 produces defines the
work, and Epic 11 supplies the material that decides each disputed value.

**Exit criteria.** The conformance suite reports no deviation that the register does not
hold. Tunnel decapsulation and pcapng decryption-secret reading are implemented, or a
named requirement records why a vector is unreachable and the maintainer accepted it.

### Epic 6: Fuzz testing

**Goal.** Prove that no packet input can panic the library.

**Covers.** `features/06-fuzz-testing.md`

**Depends on.** Epic 0.

**Exit criteria.** A fuzz target exists for every parser entry point. A seed corpus exists
for each. A short fuzz run gates every pull request. A long fuzz run runs nightly. Every
crash the fuzzer finds is closed and its input joins the seed corpus.

### Epic 7: Supply chain and CI gates

**Goal.** Make the build reproducible and the dependencies watched.

**Covers.** `features/07-supply-chain.md`

**Depends on.** Epic 0.

**Exit criteria.** `govulncheck` gates every pull request. Dependabot watches Go modules
and GitHub Actions. **Every action reference is pinned to a commit hash**, which
`release.yml` does not do today. Coverage has a floor that CI enforces. Benchmarks run in
CI and report a regression.

### Epic 11: FoxIO reference material

**Goal.** Commit the material that every ruling cites, so a reader can check a claim
without leaving the repository.

**Covers.** `features/11-foxio-reference.md`

**Depends on.** Epic 0.

**Exit criteria.** `docs/specs/foxio/` holds one transcription per FoxIO image, the text
of the deleted `technical_details/*.md` files, and the Zeek reading. Each transcription
numbers its rules. `testdata/deviations.json` exists and the conformance suite reads it.

### Epic 8: Parity with the port

**Goal.** Close every row of the register that names a change to this repository.

**Covers.** `features/08-python-parity.md`

**Depends on.** Epic 4 and Epic 11.

**Exit criteria.** Every register row that names a change is closed and carries a test.
Every row that names no change carries a test that holds the current behavior. `docs/
parity.md` records the port version it was read from. **No test in this repository runs
Python.**

### Epic 12: JA4LS

**Goal.** Implement the eleventh method.

**Covers.** `features/12-ja4ls.md`

**Depends on.** Epic 8, because the JA4L rows decide the form that JA4LS also writes.

**Exit criteria.** `JA4LFingerprinter` emits a JA4LS value under the `JA4L-S=` label, and the
result carries `Type: "ja4l"`. **The maintainer amended this criterion on 2026-08-13, in issue
#60**, because a second type would split one connection across two owners. The
conformance suite compares it against the FoxIO vectors. Every document that states a
method count states eleven methods and ten fingerprinters, and a test holds that count.

### Epic 9: Database lookup

**Goal.** Decide and implement where the remote database lookup belongs.

**Covers.** `features/09-database-lookup.md`

**Depends on.** Epic 2.

**Exit criteria.** Network input and output sit behind an explicit boundary. **The remote
lookup carries its own opt-in, separate from the local lookup.** The client sets a
timeout, verifies the server certificate, and bounds the response size. The lookup table
reloads after an update within the same process.

### Epic 13: Live capture

**Goal.** Read packets from an interface until the operator stops the process.

**Covers.** `features/13-live-capture.md`

**Depends on.** Epic 2 and Epic 3. The monitor makes an unbounded state table reachable,
so the audit closes that first.

**Exit criteria.** `ja4plus watch` reads an interface on Linux with no cgo. The `libpcap`
build tag reaches macOS. **The default build and every released binary hold no cgo.** The
monitor answers a termination signal, holds bounded memory, and writes a statistics line
that reports the drop count.

### Epic 14: Documentation site

**Goal.** Publish the documentation to GitHub Pages.

**Covers.** `features/14-documentation.md`

**Depends on.** Epic 12, because the site holds one page per method.

**Exit criteria.** `mkdocs build --strict` succeeds. The site holds one page per method,
a usage guide, an output schema, a concurrency page and an API reference. A push to
`master` publishes it. A broken internal link fails the build.

### Epic 15: Mutation sweep

**Goal.** Find the tests that no mutation makes fail.

**Covers.** `features/15-mutation-sweep.md`

**Depends on.** Epic 6.

**Exit criteria.** `make mutate` runs the sweep over a named package set and writes a
report. Every candidate is settled: the test gains an assertion, or the report records why
the mutation is equivalent. The sweep runs on a schedule and does not gate a pull request.

### Epic 16: Pre-release validation

**Goal.** Prove that the artifact a user installs works.

**Covers.** `features/16-pre-release-validation.md`

**Depends on.** Every epic that changes behavior.

**Exit criteria.** `go install` from a clean module cache produces a program that
fingerprints a capture. Each released binary runs on its platform and fingerprints a
capture. The documentation site builds from the committed pins in an empty environment.

### Epic 10: API freeze and release

**Goal.** Freeze the exported API and release `v1.0.0`.

**Covers.** `features/10-release.md`

**Depends on.** Every other epic.

**Exit criteria.** The exported API is recorded in `docs/api/v1.md`. The README, the
package documentation and the CHANGELOG describe the released behavior. **GoReleaser
builds the tag** and attaches five binaries, the checksums and the software bill of
materials. `pkg.go.dev` serves the documentation, and GitHub Pages serves the site.

## Milestones

| Milestone | Epics | What "shippable" means |
|---|---|---|
| M1: Ready to work | Epic 0, Epic 1, Epic 11 | The repository runs every gate, the license is correct, and the reference material is committed. The library is safe to distribute. |
| M2: Known-correct | Epic 2, Epic 3, Epic 4 | Every defect the audit found is closed, the concurrency contract holds, and the deviation list is known. |
| M3: Conformant | Epic 5, Epic 6, Epic 8, Epic 12 | Every FoxIO vector matches or carries a register entry, no generated input panics the library, and the two implementations agree on eleven methods. |
| M4: Hardened | Epic 7, Epic 9, Epic 15 | The supply chain is watched, the network boundary is explicit, and the sweep found no unsettled candidate. |
| M5: Complete | Epic 13, Epic 14 | The monitor reads an interface, and the documentation site publishes. |
| M6: Released | Epic 16, Epic 10 | The artifacts install and run from a clean environment, and `v1.0.0` is tagged and published. |

## Assumptions

1. The maintainer accepts a Go 1.24 module floor, and accepts that consumers on Go 1.22
   and Go 1.23 stay on `v0.3.0`.
2. FoxIO keeps publishing the corpus at `FoxIO-LLC/ja4`. A pinned commit protects the
   project from a move for the life of the pin.
3. The FoxIO Wireshark vectors are authoritative for JA4D and JA4D6, because the FoxIO
   Python vectors for `dhcpv6.pcap` are an empty list.
4. The maintainer wants `dev` as the integration branch and `master` as the live branch.
   `master` stays releasable at all times.
5. A conformance failure blocks a merge. A benchmark regression warns and does not block.
6. **The port's rulings are adopted without re-litigation.** Each one carries a
   measurement in a closed port issue. This project re-measures a ruling only when a Go
   fact contradicts it.
7. **The macOS monitor is an opt-in build, and no released binary carries cgo.** The
   maintainer accepts that a macOS user who wants the monitor builds from source with the
   `libpcap` build tag.
8. The documentation site needs Python in one CI job. The maintainer accepts that, because
   the site matches the port's site and no Go generator produces the same pages.

## Risks & open questions

### R1 — The license split is a release blocker (resolved 2026-08-06)

FoxIO License 1.1 is a non-commercial license. `LICENSE-JA4` covers JA4 alone under BSD
3-Clause. The repository README claims BSD 3-Clause for the whole library and links to a
`LICENSE` file that does not exist.

Epic 1 implements the model that the FoxIO licensing FAQ recommends: the original Go code
stays BSD 3-Clause, and a `NOTICE` file carries the FoxIO terms.

**This spec is not legal advice.**

**The maintainer resolved this risk on 2026-08-06.** The dual model is sufficient for
`v1.0.0`. Epic 1 records the decision in `docs/audit/license-decision.md`.

**Round 3 adds one requirement.** The port's issues #388 and #466 measured three FoxIO
records at the pinned commit and found three different method lists: nine names, twelve
names and thirteen names. The README therefore names the methods this project implements
and asserts no equality with FoxIO's list.

Source: <https://github.com/FoxIO-LLC/ja4/blob/main/LICENSE>,
<https://github.com/FoxIO-LLC/ja4/blob/main/LICENSE-JA4>,
<https://github.com/FoxIO-LLC/ja4/blob/main/License%20FAQ.md>, read 2026-08-06 at commit
`27f0cbf`.

### R2 — Two vectors may be unreachable (open)

`chrome-cloudflare-quic-with-secrets.pcapng` carries TLS secrets in pcapng Decryption
Secrets Blocks. `gopacket` does not read those blocks. `dtls-udp.notest.cap` carries the
`notest` marker, which means FoxIO excludes it from their own suite.

Epic 5 treats both as named requirements with their own issues. If reading a Decryption
Secrets Block proves impossible within `gopacket`, the requirement records the reason and
the maintainer accepts it before Epic 10 releases.

### R3 — Encapsulation support is unscoped until Epic 4 runs (open)

The corpus holds `gre-sample.pcap`, `gre-erspan-vxlan.pcap` and `tcpdump-geneve.pcap`.
`tshark` decapsulates all three. The library appears not to. Epic 5 is sized after Epic 4
finishes.

### R4 — The port carries the same license gap (closed, out of scope)

`Crank-Git/ja4plus` shipped the correction in its Epic 1. This project records the finding
and acts only on its own side.

### R5 — `gopacket` upstream is unmaintained (open)

`github.com/google/gopacket` has had no release since v1.1.19 on 2020-10-19. **The gap is
five years, nine months and 25 days on 2026-08-13.** The community fork is
`github.com/gopacket/gopacket`. Epic 7 held the decision, and the maintainer made it.
**This paragraph read `no release since v1.1.19 in 2022` until 2026-08-13**, and the Go
module proxy reports `{"Version":"v1.1.19","Time":"2020-10-19T16:12:32Z"}`.

**The maintainer settled this risk on 2026-08-13, and #438 carried the migration.** The
library depends on `github.com/gopacket/gopacket` v1.6.1 today, and no fingerprint value
moved. `docs/audit/dependency-decision.md` records the decision and the reversal path.
**The heading of this risk still reads `(open)`, and the `## Changelog` round owns that
status.**
`docs/audit/dependency-decision.md` holds that measurement, and #434 recorded it.

**Round 3 raises the cost of this question.** `features/13-live-capture.md` builds on
`pcapgo.NewEthernetHandle`, so a migration now moves the monitor as well as the parser. A
decision after Epic 13 is more expensive than a decision before it.

### R6 — This is a large `v1.0.0` (open)

The maintainer chose one `v1.0.0` that carries every register row, JA4LS, the monitor, the
documentation site, the mutation sweep and the pre-release validation. **Seventeen feature
sets reach one tag.**

The gain is a freeze that covers the final shape: no exported name moves in a `v1.1.0`,
because JA4LS and the monitor are already in. The cost is a long path to the first `v1`
tag, and no user gets the license correction until the whole set lands.

**One mitigation is available and this spec does not take it.** Epic 1 alone could ship as
`v0.4.0` before the rest, because the license correction is the one change that a user is
harmed by waiting for. The maintainer decides whether to take it.

### R7 — The macOS monitor introduces cgo to a no-cgo project (open)

`CLAUDE.md` states "No cgo. The build cross-compiles to five platforms." The `libpcap`
build tag contradicts that sentence for one build.

`features/13-live-capture.md` contains the contradiction three ways. The default build
selects the pure-Go backend and holds no cgo. GoReleaser builds no tagged binary with the
tag. CI builds the tagged path on macOS to prove it compiles, and gates nothing else on
it. **Epic 13 rewrites the `CLAUDE.md` sentence** so that the rule states the containment
rather than an absolute.

The residual risk is that a macOS user reads the README, builds from source without the
tag, and finds `watch` unavailable. FR-capture-24 answers that with a message that names
the tag.

### R8 — A ruling can move on the port's side after this spec is approved (open)

Every register row is reversible. The port shipped `v1.1.0` on 2026-08-11, and it may rule
again.

This spec records the port version each row was read from. **The mitigation is a check,
not a promise.** Epic 8 adds a test that reads the port's `docs/specs/spec.md` register at
a pinned commit and compares its row count against the register here. **The test reads a
committed copy under `docs/specs/foxio/port-register.md` and performs no network call**,
so it obeys rule 3. A count that moves reports that a re-read is due.

### R9 — Three questions the port never settled (question 1 and question 3 are closed)

**This heading read `Three rulings the port never made` until 2026-08-12.** Question 1 needed
a reading and not a ruling, so the heading now names three questions.

**The maintainer confirmed on 2026-08-11 that all three needed a ruling.** **Question 1 is
closed, and the maintainer closed it on 2026-08-12.** The subsection
`R9 question 1 is closed, and a FoxIO source states the answer` below records that closure and
the evidence.

**Question 3 is closed, and the maintainer ruled it on 2026-08-13.** The subsection
`R9 question 3 is closed, and the maintainer ruled the filter token` below records the ruling.
**Question 2 stays open in this table.**

**Question 2 is ruled, and this table does not yet record it.** The maintainer ruled it on
2026-08-11, round 17 of the `## Changelog` holds the ruling, and #53 is closed. **#279 owns
the repair of that row**, and #50 changed no part of it.

**FoxIO settles no open question of this table, and the port settled none.** Each one
is the maintainer's to rule on. **An engineer who reaches one of these stops and asks.** A
requirement below is not built until the ruling exists, because a guess here produces two
answers across the two repositories, which is the one outcome parity exists to prevent.

**A ruling lands in this repository and in the port together.**

| Question | What is already settled | What is not | Blocks |
|---|---|---|---|
| ~~**What does the ALPN field write when the first ALPN value is empty?**~~ **Closed on 2026-08-12. The answer is `00`.** | Every part of the question. A one-byte first value repeats the byte and writes `hh`. A first byte outside `0x20-0x7E` writes `99`. An empty first value writes `00`. | Nothing. **The FoxIO text specification states the answer, so the question needed a reading and not a ruling.** | Nothing. #50 built all three requirements. |
| **Does `CloseOpenWindows` belong on the `Fingerprinter` interface?** | The method exists, and the port's name decides the word under parity rule 2. | Where it sits. On `Fingerprinter` it matches the port and breaks every third-party implementation of an exported interface. On a second optional interface it breaks nothing and is the more idiomatic Go shape. **The freeze makes this the last chance to choose.** | FR-parity-30, FR-parity-31 |
| ~~**Does `--types ja4l` alone print the JA4LS value?**~~ **Closed on 2026-08-13. The answer is yes, and `ja4ls` is a second token.** | Every part of the question. `--types ja4l` prints the client value and the server value. `--types ja4ls` prints the server value alone. | Nothing. **The maintainer ruled the token, and the ruling moves no fingerprint value.** | Nothing. #61 built FR-ja4ls-12. |

#### R9 question 1 is closed, and a FoxIO source states the answer

**The maintainer closed question 1 on 2026-08-12.** An empty first ALPN value writes `00`,
and `internal/parser/tls.go` already wrote it. **No behavior changed.**

**The question asked for a ruling, and the answer needed a reading.** The row above stated
that no FoxIO source states a rule for the input. #50 read the FoxIO text specification and
found that one does.

**`technical_details/JA4.md` is a live FoxIO text specification, and not a deleted one.** It
holds 9153 bytes at the pinned commit, so `.claude/rules/rulings.md` rank 3 does not describe
it. `docs/specs/foxio/JA4.md` states the ranking this method uses: the image first, and this
text second. **JA4 is the one method that holds a complete text specification at the pin.**

`technical_details/JA4.md:93` covers three inputs in one sentence, and this is the verbatim
text:

> If there is no ALPN extension, no ALPN values, or the first ALPN value is empty, then we
> print "00" as the value in the fingerprint.

**So the ratified behavior rests on a stated rule.** The maintainer ratified `00` on
2026-08-11 from the two implementations alone, and the issue comment recorded that FoxIO
Python raises `IndexError` on the input. `docs/specs/foxio/JA4.md` R17 records the reading
and cites the line. **The reading is a reading, and the closure of the question is the
maintainer's.**

**The cost the 2026-08-11 ratification recorded still holds.** `00` is also the value for a
client hello that carries no ALPN extension, so a client that offers one zero-length protocol
is indistinguishable from a client that offers none. The FoxIO sentence above states the same
collapse, so FoxIO accepts it too.

**This closure opens no issue in the port.** `ja4plus/fingerprinters/ja4.py:75` returns `00`
for the input, so the two implementations already agree.

**The second question has a deadline that the other two do not.** `Fingerprinter` is an
exported interface. Adding a method to it is a breaking change that `v0.3.0` permits and
that `v1.0.0` forbids for the whole `v1` series. If the ruling arrives after the freeze,
the only remaining answer is the optional interface.

#### R9 question 3 is closed, and the maintainer ruled the filter token

**The maintainer ruled question 3 on 2026-08-13.** `--types ja4l` prints the JA4L value and
the JA4LS value. `--types ja4ls` prints the JA4LS value alone. The plan comment of #61
records the ruling.

**The ruling is a superset over the port, and it is not a divergence.** The port reaches both
values through `ja4l`, and this library reaches them the same way. The second token adds a
filter the port does not offer, and no fingerprint value moves.
`Crank-Git/ja4plus#605` proposes the same token for the port.

**The result carries the type `ja4l` for both methods, so the filter reads the label of the
fingerprint.** #60 rules that the type stays `ja4l`. `cmd/ja4plus/types.go` therefore selects
the JA4LS value on the prefix `JA4L-S=`, which is the discriminator that
`conformance_test.go:347-362` and `ja4plus/fingerprinters/ja4l.py:182` each read.

**The reversal path is #61 and this subsection.** A reversal removes the `ja4ls` token before
the `v1.0.0` freeze.

## Issue map

`/spec-to-issues` created these on 2026-08-11, from spec version 3. Every issue carries the
marker `<!-- spec:ja4plus-go feature:<id> -->`, so a later planning wave reads the id and
re-issues nothing that already exists.

**Epic 8 reached the tracker as two epics.** Its 60 requirements slice along eight seams, and
one issue-flow batch holds six sub-issues at most. Epic 8a carries the TLS and latency methods,
and Epic 8b carries the TCP, HTTP, certificate and DHCP methods. Both point at
`features/08-python-parity.md`.

| Epic | Epic issue | Sub-issues | Feature file |
|---|---|---|---|
| Epic 0: Foundation | #3 | #4, #5, #6, #7, #8 | `features/00-foundation.md` |
| Epic 1: License compliance | #9 | #10, #11, #12, #13 | `features/01-licensing.md` |
| Epic 11: FoxIO reference material | #14 | #15, #16, #17, #18, #19 | `features/11-foxio-reference.md` |
| Epic 2: Correctness audit | #20 | #21, #22, #23, #24, #25 | `features/02-correctness-audit.md` |
| Epic 3: Concurrency contract | #26 | #27, #28, #29, #30 | `features/03-concurrency.md` |
| Epic 4: Conformance harness | #31 | #32, #33, #34, #35, #36 | `features/04-conformance-harness.md` |
| Epic 5: Conformance gap closure | #37 | #38, #39, #40, #41, #42 | `features/05-conformance-gaps.md` |
| Epic 6: Fuzz testing | #43 | #44, #45, #46, #47 | `features/06-fuzz-testing.md` |
| Epic 8a: Parity — the TLS and latency methods | #48 | #49, #50, #51, #52, #53 | `features/08-python-parity.md` |
| Epic 8b: Parity — the TCP, HTTP, certificate and DHCP methods | #54 | #55, #56, #57, #58 | `features/08-python-parity.md` |
| Epic 12: JA4LS | #59 | #60, #61, #62, #63 | `features/12-ja4ls.md` |
| Epic 7: Supply chain and CI gates | #64 | #65, #66, #67, #68, #69, #70 | `features/07-supply-chain.md` |
| Epic 9: Database lookup | #71 | #72, #73, #74, #75 | `features/09-database-lookup.md` |
| Epic 13: Live capture | #76 | #77, #78, #79, #80, #81, #82 | `features/13-live-capture.md` |
| Epic 14: Documentation site | #83 | #84, #85, #86, #87, #88 | `features/14-documentation.md` |
| Epic 15: Mutation sweep | #89 | #90, #91, #92, #93 | `features/15-mutation-sweep.md` |
| Epic 16: Pre-release validation | #94 | #95, #96, #97, #98, #99 | `features/16-pre-release-validation.md` |
| Epic 10: API freeze and release | #100 | #101, #102, #103, #104, #105, #106 | `features/10-release.md` |

### Issues that wait on a ruling

**No worker may guess an answer to any question below.**

**This table is stale, and #279 owns the repair.** It stated that five issues carry
`status:needs-feedback`, and it listed #50, #53, #61, #70 and #72. #50 measured the label on
2026-08-12. **Not one of those five carries the label today**, and #53 is closed. **#50
removed its own row alone, because its question is closed.** **#61 removed its own row
alone, on 2026-08-13, for the same reason.**

**One issue carries the label today, and it is #364.** The command
`gh issue list --label status:needs-feedback --state all --limit 300` measured that on
2026-08-13. **This section named six other issues before that measurement.** #126, #129,
#289 and #300 are closed. #298 carries `status:ready`, and #346 carries `status:batched`.
**#279 reconciles the rows and the count.**

| Issue | Question | Recorded as |
|---|---|---|
| #53 | Whether `CloseOpenWindows` sits on `Fingerprinter` or on a second optional interface. **This one expires at the API freeze.** | R9 question 2 |
| #70 | Whether the project moves to the `gopacket` fork before the freeze. | R5 |
| #72 | Where the remote database lookup belongs. | FR-lookup-2 |

## Changelog

| Round | Date | What changed |
|---|---|---|
| 1 | 2026-08-06 | First draft, written from the Phase 1 interview and from the FoxIO reference at commit `27f0cbf`. |
| 2 | 2026-08-06 | The maintainer approved the spec and the scaffold. R1 resolved: the dual BSD 3-Clause and FoxIO `NOTICE` model is sufficient for `v1.0.0`. |
| 3 | 2026-08-11 | Revised after a read of the port at `v1.1.0`. Added the `Parity with ja4plus` register, which carries about twenty rows the port's own spec directs at this repository. Added Epics 11 through 16: the FoxIO reference material, JA4LS, live capture, the documentation site, the mutation sweep and the pre-release validation. Removed three non-goals, under the maintainer's rule that only a capability Go cannot achieve stays out of scope. Restated the JA4TScan decline on the ground that FoxIO publishes no format, rather than on the port's capability-boundary ground. Deleted FR-parity-8 through FR-parity-14, because the port's parity rule 3 rejects a cross-language test rig. Added R6, R7 and R8. |
| 4 | 2026-08-11 | The maintainer approved the spec and the scaffold. R6 resolved: the license correction waits for the full `v1.0.0`, and no `v0.4.0` ships first. Added R9, which holds the three rulings that neither FoxIO nor the port has made. The maintainer confirmed that all three need a ruling, and each one blocks its requirements until the ruling exists. |
| 5 | 2026-08-11 | Repaired four method counts that round 3 changed in `spec.md` and left stale in the round 2 feature files. FR-licensing-5 named nine methods under FoxIO License 1.1 and now names ten, and it states that JA4LS joins the list when Epic 12 lands. FR-release-7 and FR-release-17 named ten methods and now name eleven through ten fingerprinters. FR-foundation-20 asked for a benchmark for each of ten methods, and it now names the count the library implements today and points at Epic 12 for the eleventh. Added FR-licensing-5a, which states that `NOTICE` asserts no equality with FoxIO's own method list. Found while `/spec-to-issues` read the feature files. |
| 6 | 2026-08-11 | Epic 0 shipped. #32, which `features/04-conformance-harness.md` holds, moved into the Epic 0 batch: #6 adds a `corpus` target, and the target needs the fetch script that #32 writes. Without the move the criterion "make corpus fetches the corpus" could not pass inside Epic 0. The criterion "make conformance without a corpus prints a message that names make corpus" stays with #33, because `features/00-foundation.md:174` gives Epic 0 only the target that calls the harness. The `features/00-foundation.md` status moves to `built`. |
| 7 | 2026-08-11 | Epic 1 shipped. `LICENSE`, `NOTICE`, `data/README.md`, `docs/audit/license-decision.md` and `doc.go` land, and the README no longer states that the library is BSD 3-Clause without the FoxIO qualification. The maintainer named `Crank-Git` as the copyright holder and 2026 as the year. FR-licensing-14 landed as `data/README.md`, because `lookup.go` reads the first CSV row as the column header and sets no `Reader.Comment`, so a header comment would break every lookup. #114 joined the batch: `.claude/rules/rulings.md` named an absent `.claude/rules/conformance.md`, and it now points at `.claude/rules/parity.md`. `NOTICE` names nine methods, and Epic 12 adds JA4LS. |
| 8 | 2026-08-11 | FR-reference-25 and FR-reference-26 move from #19 to #33. Both name the conformance suite, and this repository holds no conformance suite today. A read of every Go file reaches `foundation_test.go` alone, which asserts the `make conformance` recipe and not a suite. #33 of Epic 4 builds the suite, so the two requirements land there. Acceptance criterion 6 of `features/11-foxio-reference.md` moves with them. #19 keeps FR-reference-17 through FR-reference-24, FR-reference-27 and FR-reference-28. `testdata/deviations.json` lands as an empty array, because no conformance run has measured a deviation. |
| 9 | 2026-08-11 | The maintainer ruled that the CI conformance job of #36 reports and does not fail on a deviation. FR-conformance-34 and FR-conformance-35 change meaning for that slice: the job runs the suite and publishes the counts. Epic 5 makes the job fail on a deviation. The reason is the measurement of #33, which found 302 matches and 1858 deviations over 38 captures against an empty register. A job that fails on a deviation stays red on every pull request until Epic 5 closes the list. FR-conformance-36 holds without a change, because a skipped suite that reports success hides every deviation. |
| 10 | 2026-08-11 | The maintainer ruled on #38 that the register and the exclusions page hold two distinct classes of fact. `testdata/deviations.json` holds a decline, where this project compared its output against a FoxIO value and chose to differ. `docs/audit/conformance-exclusions.md` holds an exclusion, where the suite makes no comparison at all. One question settles which record a fact reaches: does the guard "an entry whose comparison now matches fails the suite" make sense for this fact? `dtls-udp.notest.cap` decides the boundary, because FoxIO marks it `notest` and it reaches no `<capture>/<stream>/<method>` register key. The maintainer also renamed `docs/audit/conformance-exceptions.md` to `docs/audit/conformance-exclusions.md`, because this table bars `exception` as a synonym for `deviation` and bars `exception file` as a synonym for `register`. `## Terms` gains the word `exclusion`. FR-gaps-5, FR-gaps-20 and FR-release-44 carry the new name and require nothing new. The ruling opens no issue in the port, because it moves no fingerprint value and no exported name. |
| 11 | 2026-08-11 | The maintainer accepted eight exported names before the Epic 10 freeze: `ErrNoSecret`, `KeyLog`, `ParseKeyLog`, `ReadKeyLogFromCapture`, `KeyLog.Secret`, `KeyLog.ClientRandoms`, `KeyLog.Len` and `DecryptQUICPacket`. #40 of Epic 5 builds them for FR-gaps-15 through FR-gaps-18, and this is the first slice of the session to widen the exported surface. The `Interface` row of the register records the parity direction: this project shipped the key-log interface first, the port holds no such interface, and the port adopts these names under rule 2. The `Terms` table gains `key log`, `secret` and `decryption secrets block`. |
| 12 | 2026-08-11 | The maintainer ruled that a tunneled connection carries two keys, and not one. The reported key holds the outer address pair with the inner port pair, and the time-to-live reads the outer address layer. The grouping key holds the inner address pair with the inner port pair. The reason is that a mirror sends both directions of one session from one outer address pair, so the outer pair separates no direction and one connection then holds two measurement points that belong to two endpoints. `gre-erspan-vxlan.pcap` is that capture. The ruling adopts `Crank-Git/ja4plus` issue #242 under `.claude/rules/parity.md` rule 2, because this project shipped no tunnel decapsulation and the port ruled first. **The ruling opens no issue in the port, because the decision already lives there.** FR-gaps-13 and FR-gaps-14 of `features/05-conformance-gaps.md` now state the two-key rule, and FR-gaps-14a and FR-gaps-14b joined them. Recorded by #39. |
| 13 | 2026-08-11 | The infrastructure batch shipped, and **it moves no fingerprint value**. #162 states how `docs/audit/findings.md` classifies a `.go` file that a later issue adds to an audited directory, and the `Terms` table gains `added file`. #148 derives every fingerprinter list from `Processor.fingerprinters`, and it requires a `SyncProcessor` wrapper for each exported method of `Processor`. #165 adds `testdata/foxio/reference/` to the corpus, so a reading cites a FoxIO file and a line at the pinned commit. #142 makes the CI job read a marker line that only the conformance suite writes, so an untagged skip no longer fails the job with the wrong reason. The review of the batch then found that the corpus cache key named the pinned commit alone. Every run after #165 restored a corpus without the reference tree, and it downloaded the archive again. The key now reads the hash of `scripts/fetch-corpus.sh` too, so a layout change moves the key. |
| 14 | 2026-08-11 | The maintainer ruled that this repository adopts the value decline the port made under its issue 138. The QUIC CRYPTO stream reassembly of #42 makes the library produce a JA4 value and a JA4S value on every QUIC stream of the corpus that carries a handshake. The FoxIO Python implementation reads no QUIC handshake, so its expected-output file omits the stream and the suite reports the deviation kind `the library produces a value the vector does not hold`. The FoxIO Rust implementation produces the value the library produces, so the library is right and the reference published nothing to compare. Each deviation reaches `testdata/deviations.json` with `"capability": false`, and the key takes the shape `<capture>/<stream>/<method>` that `testdata/README.md` states. The port's own key shape is `<capture>/<method>`, and this repository declines it, because this suite compares per stream and a capture that holds two streams would collapse to one key. **The ruling opens no issue in the port, because the decision already lives there.** These are the first entries the register has ever held. Recorded by #42. |
| 15 | 2026-08-12 | The maintainer ruled that the library fills the TCP client measurement point from the packet `python/ja4.py:570` names, and that point `C` moves. The rule reads every packet that carries `ACK` and no `SYN`, with the relative sequence number 1 and the relative acknowledgment number 1. A payload does not bar the packet, and `python/common.py:101` omits `C` from the fields it declines to update, so a later packet replaces the point. A packet that carries a whole HTTP request moves no point, because `python/common.py:77-83` gives it a separate cache. **The four FoxIO implementations state three different rules, so this is a ruling and not a reading.** `docs/specs/foxio/JA4L.md` R33 records the four rules, and R34 records that the per-stream vector holds `2181_64` on stream 0 of `badcurveball.pcap` while the per-packet vector holds `2177_64_114797` on frame 9. Wireshark bars a payload-bearing packet at `wireshark/source/packet-ja4.c:1302`, Rust reaches a terminal state at `rust/ja4/src/time/tcp.rs:145`, and Zeek reads the second packet of the originator at `zeek/ja4l/main.zeek:103`. The ruling follows Python, because the per-stream vector is the set this suite compares per stream, and the Python rule reaches it on every comparison the set holds. **The ruling knowingly gives up the per-packet vector.** Thirty-five entries reach `testdata/deviations.json` with `"capability": false` and the ruling `#196`, and each reason states the part a divergence alone, because #197 owns the third part. **The conformance harness now compares the last emission** for a per-stream method that the vector holds once, at `conformance_adapters_test.go:435`. The library keeps its per-packet streaming contract, it suppresses no intermediate value, and it gains no flush. The same slice repaired two defects that the harness change exposed, and the reference is unanimous on both, so neither is a ruling: point `A` and point `B` no longer move, and the two endpoint names of the relative number read the grouping address pair. Measured against `epic/48-parity-tls-latency` at `3e7a47a` with the corpus present: 43 `JA4L-C` comparisons moved to match on 22 captures, and 35 per-packet `JA4L` comparisons gained a registered divergence. The per-stream set reports 703 matches and 514 deviations before, and 744 matches and 457 deviations after. The per-packet set reports 332 matches and 834 deviations before, and 332 matches and 833 deviations after. The register holds 150 keys before and 185 after. #205 and #206 hold the two comparisons the harness change leaves worse, both on `chrome-cloudflare-quic-with-secrets.pcapng` stream 0. **The ruling opens no issue in the port, because the port is the Python reading and the decision already lives there.** Recorded by #196. |
| 16 | 2026-08-12 | An engineer recorded the reading of the JA4L third part, and the question reaches the maintainer. **The reading is that the third part holds two different things, and that one rule for both produces a new wrong value.** On a TCP connection the third number is the Wireshark part c, which `docs/specs/foxio/JA4L.md` R24 names as the numerator of `ja4.ja4l_delta`. The `tcpdump-geneve.pcap` frame 13 vector holds `ja4.ja4l` as `93_64_124` and `ja4.ja4l_delta` as `1.3`, and `124 / 93` reads `1.3`. The `badcurveball.pcap` frame 9 vector holds `ja4.ja4l` as `2177_64_114797` and `ja4.ja4l_delta` as `52.7`, and `114797 / 2177` reads `52.7`. Issue #127 declines that part c, so the TCP part count is settled and this slice adds one test that holds it. **On a QUIC connection the third part is the marker `quic`, and the question is open.** `docs/specs/foxio/JA4L.md` R35 records the new fact. The two FoxIO vector sets state two different part counts for one QUIC connection, and two pairs separate the part count alone. On stream 36 of `ssh2.pcapng` the per-stream vector holds `JA4L-C` as `169_128` while the per-packet vector holds `ja4.ja4l` as `169_128_quic` on frame 1147. On stream 22 of `tls3.pcapng` the per-stream vector holds `JA4L-C` as `336_128` while the per-packet vector holds `ja4.ja4l` as `336_128_quic` on frame 162. **Two rulings point opposite ways.** Issue #127 writes the marker on a QUIC connection, and round 15 above follows the per-stream set where the two sets disagree. The measurement reads `epic/48-parity-tls-latency` at `887ab53` with the corpus present. The marker moves the library value on 16 per-packet comparisons and on 16 per-stream comparisons, across 3 captures. It closes 2 per-packet comparisons. It opens 13 per-stream comparisons that match exactly today. The per-stream set reports 744 matches and 457 deviations without the marker, and 731 matches and 470 deviations with it. The per-packet set reports 332 matches and 833 deviations without it, and 334 matches and 831 deviations with it. The register holds 185 keys before and 185 after, and no entry reads as closed. **This slice moves no fingerprint, and it writes no register entry.** The reason is that `.claude/rules/rulings.md` names a disagreement between the FoxIO implementations as a stop condition, and `.claude/rules/parity.md` names the vector set as the gate. Recorded by #197. |
| 17 | 2026-08-11 | The maintainer ruled on R9 question 2, and the question closes. `CloseOpenWindows` sits on a second optional interface, which this repository names `WindowCloser`, and the exported `Fingerprinter` interface does not change. The reason is that a new method on an exported interface breaks every third-party implementation, and `v1.0.0` forbids that break for the whole `v1` series. A caller discovers the interface with a type assertion, as a caller of `io.WriterTo` does. **FR-parity-30 changes meaning.** It read that every fingerprinter exports `CloseOpenWindows`, and that a stateless fingerprinter returns an empty slice. Under the ruling a stateless fingerprinter implements nothing, and `Processor` skips it. FR-parity-31 and FR-parity-32 hold without a change. A `type:spec-update` issue proposes the new wording of FR-parity-30, of R9 question 2 and of the four JA4SSH register rows, because a spec edit is its own issue. The same slice repaired the JA4SSH window: the threshold reads the packet count the caller names, it holds no upper cap, it counts the SSH packets of the two directions alone, and it declines a window that holds no SSH packet. The HASSH trigger goes, because the port holds no such rule. Measured against `epic/48-parity-tls-latency` at `ec0f63e` with the corpus present: 1807 JA4SSH comparisons moved on seven captures, the run reports 1035 matches before and after, 3155 deviations before and 1348 after, and 150 register keys before and after. **The ruling opens no issue in the port, because the port already carries `close_open_windows` and Python states no interface.** Recorded by #53. |
| 18 | 2026-08-12 | The maintainer ruled that the library writes the marker `quic` as the third part of a JA4L value on a QUIC connection. Two parts stay on a TCP connection. **Issue #127 holds the original ruling, and this slice reverses nothing.** The QUIC half of #127 was ruled in session 3, and the code never carried it. #197 found the gap. The deciding rule is that the library matches the port one to one, and that it follows the FoxIO material where that leaves a choice. **Round 16 above cited the wrong Python.** `python/ja4.py` is FoxIO's reference Python inside the corpus, and it is not the port. **The port writes the marker.** `ja4plus/fingerprinters/ja4l.py:62` defines `QUIC_MARKER = "quic"`, `:549` and `:602` write three parts with it, and `:446`, `:466` and `:482` write two parts on a TCP connection. The FoxIO material reaches the same answer, because `.claude/rules/rulings.md` ranks an image first and `docs/specs/foxio/JA4L.md` R3 states that a value holds three parts. The literal `quic` follows `wireshark/source/packet-ja4.c:1441` and `:1447`. **The two rulings do not collide.** #127 answers a part count, which an image decides, and round 15 above answers a value, which the per-stream set decides. **The match count falls, and the ruling accepts that.** A fingerprint exists for a comparison with another implementation. A rule that raised the match count against one vector set, and diverged from the port, would defeat the register. Measured on `batch/210-session5-followups` at `0751acc` with the corpus present: the marker moves the library value on 32 comparisons, across 3 captures. It closes 2 per-packet comparisons, and it opens 13 per-stream comparisons that match without it. The per-stream set reports 744 matches before and 731 after. The per-packet set reports 332 matches before and 334 after. The run reports 1076 matches before and 1065 after. Thirteen entries reach `testdata/deviations.json` with `"capability": false` and the ruling `#197`, and each reason states the per-stream divergence alone. The register holds 185 keys before and 198 after, and no entry reads as closed. **The ruling opens no issue in the port, because the port already writes the marker.** Recorded by #197. |
| 19 | 2026-08-12 | **The spec gains two requirements and one term, and it reverses nothing.** FR-gaps-14d states that `JA4Fingerprinter` and `JA4SFingerprinter` each hold an index that pairs the reported key with the grouping key, so `CleanupConnection` removes a tunneled connection. FR-gaps-14e states that `CleanupConnection` falls back to the key the caller gave, so a caller that names the grouping key removes the connection. The `## Terms` table gains `index`. **The two requirements record behavior that #193 landed, and they complete FR-gaps-14c.** #169 added FR-gaps-14c for JA4L. The survey that #169 required found the same mismatch on JA4 and on JA4S, and #193 records that finding. **This is a reading and not a ruling.** No FoxIO source addresses a state table, because a state table reaches no fingerprint value. **The project manager accepted the two requirements in this round, and it opened no separate issue for them.** The ground is that they complete the pattern of FR-gaps-14c, and that they describe behavior that lands in the same commit. **The change moves no fingerprint value.** Measured on `batch/210-session5-followups` at `c4978ab` with the corpus present: the run reports 1065 matches, 1288 deviations and 198 accepted deviations before and after. The register holds 198 keys before and after, and no entry reads as closed. That count holds the 13 entries of #197 and the 35 per-packet entries of #196. Coverage reads 71.4%. **The change opens no issue in the port, and the port needs none.** `ja4plus/fingerprinters/ja4.py` and `ja4plus/fingerprinters/ja4s.py` name `grouping`, `innermost` and `inner_` on no line, so the port holds no grouping key on those two methods and no two-key mismatch. `ja4s.py:143` writes `self._quic_dcids[f"{src_ip}:{src_port}-{dst_ip}:{dst_port}"] = dcid`, and `ja4s.py:196-206` removes the same form. `ja4.py:449` writes `self._quic_dcid_to_tuple[dcid_key] = tuple_key`, and `ja4.py:473-481` matches the same shape. **The port reads the outer address layer on each QUIC branch.** `ja4.py:422` and `ja4s.py:83` each read `udp = packet.getlayer(UDP)`, so the port decapsulates neither method. Port issue #594 records that. **This repository holds the leak because it decapsulates and the port does not.** The port reaches FR-gaps-14d and FR-gaps-14e when it closes `Crank-Git/ja4plus#594`. Recorded by #193. |
| 20 | 2026-08-12 | **The last-emission comparison of round 15 above narrows to the two JA4L methods, and this round reverses no part of round 15.** Round 15 records that the harness compares the last emission for a per-stream method that the vector holds once. That rule reached every per-stream method the vector writes with a bare key, and issue #209 proves two consequences of the width. **A method emits twice on one stream for two different reasons, and one rule cannot serve both.** A measurement point that moves reports one connection twice, and the last report holds the value the reference publishes. A method that emits once per protocol, or once per request, reports two different values, and the second value is a surplus value. **The maintainer ruled the first case on 2026-08-12 in issue #196, and the ruling names a moving measurement point.** It names no method that emits once per protocol. `conformance_adapters_test.go` now holds `conformanceLastEmissionMethods`, which names `JA4L-C` and `JA4L-S` alone. Every other per-stream method gains an occurrence number for the second value and for each value after it. That is the key form the harness wrote before #196. **The `## Terms` table gains `emission`, `bare key`, `occurrence number` and `surplus value`.** **The first consequence is a lost match.** `chrome-cloudflare-quic-with-secrets.pcapng/0/JA4S` reported the deviation `the two values differ`, with the vector value `t130200_1301_234ea6891581` and the library value `q130200_1301_234ea6891581`. The library produces both values on that stream, the vector holds the TLS value, and the bare key kept the QUIC value. The match returns, and the QUIC value reports as the surplus `chrome-cloudflare-quic-with-secrets.pcapng/0/JA4S.2`. **The second consequence is a hidden value.** `CVE-2018-6794.pcap/0/JA4H` reported 2 deviations, and the second, the third and the fourth request values reached no comparison. The three return as `JA4H.2`, `JA4H.3` and `JA4H.4`. **This slice moves no fingerprint value, and it writes no register entry.** Measured on `batch/230-comparison-and-records` at `7ac5855` with the corpus present: the run reports 1077 matches and 1278 deviations before, and 1078 matches and 1293 deviations after. The per-stream set reports 736 matches and 454 deviations before, and 737 matches and 469 deviations after. The per-packet set reports 341 matches and 824 deviations before and after, so no per-packet comparison moves. The register holds 198 keys before and after, and no entry reads as closed. Coverage reads 72.3% before and after. Every test this slice adds carries the `conformance` build tag, and `make cover` runs no tagged file, so the coverage cannot move. **Fifteen comparisons return, and they reach two groups.** Ten reach `CVE-2018-6794.pcap` per-stream JA4H, and five reach `tls-handshake.pcapng` per-stream JA4S. **The defect hid 15 of the 1877 deviations that this session closed, measured at this commit.** #53 closed 1807 of them, so the fall from 3155 to 1278 records real repair and not a narrowed comparison. **A test derives the set that the last-emission rule reaches**, so a second method that joins the set fails the suite. #148 requires the derived form. **The change opens no issue in the port, because it moves no fingerprint value and no exported name.** Recorded by #209. |
| 21 | 2026-08-12 | **The spec gains four requirements and five terms, and it reverses nothing.** FR-reference-29 states the three things the middle part of a register key holds: the stream number in the per-stream set, the endpoint key where no vector entry names the connection, and the frame number in the per-packet set. `conformance_test.go:416-418` holds the reading of the endpoint key form, which 126 entries of #42 carry. FR-reference-29a states that `testdata/README.md` holds the three meanings. FR-reference-30 states that one key names one comparison, and a test holds it. FR-reference-31 states that the register holds each key once, and the reader declines a second entry for one key. The `## Terms` table gains `part a`, `stream number`, `frame number`, `endpoint key` and `middle part`. **This is a reading and not a ruling.** No FoxIO source addresses a register key, because the register is this project's own instrument. **The collision is unreachable today, and the guard still lands.** The measurement reads `batch/230-comparison-and-records` at `7ac5855` with the corpus present: the per-stream set names 1353 keys, the per-packet set names 1200 keys, no key belongs to both sets, and 679 pairs hold one capture and one middle part and differ in the method name alone. A per-packet method name always carries an occurrence number, and a per-stream method name carries one only where a stream holds more than one value, so the method name alone separates those 679 pairs. **The register grows on every session, and a coincidence of one method name would reach the collision**, so `conformance_key_kind_test.go` fails the suite for a key that both sets name. **The change moves no fingerprint value, and it re-keys no entry.** The run reports 1077 matches, 1278 deviations and 198 accepted deviations before and after. The register holds 198 keys before and after, and no entry reads as closed. Coverage reads 72.3%. **The 35 identical reasons of #196 stay as they are.** One ruling produced them, `.claude/rules/rulings.md` asks each entry for one sentence of reason, and round 15 above already holds the measurement of that ruling. A separate sentence for each of the 35 would add no fact, and rewording an entry without a new fact changes the maintainer's instrument for nothing. **The term `part a` closes the second note of #217.** All 35 reasons use the phrase, `CLAUDE.md` requires the `## Terms` table to hold a domain word, and `docs/specs/foxio/JA4L.md` R5 and R6 hold the reading that the term states. **The change opens no issue in the port.** Round 14 above records that the port keys its own register `<capture>/<method>`, so the port holds no middle part and reaches no collision. Recorded by #217. |
| 22 | 2026-08-12 | **`ProcessPacket` now emits the open JA4SSH window on a FIN+ACK packet, and this round reverses nothing.** Before the change `CloseOpenWindows` was the one emission path, so a value that the reference anchors to a FIN+ACK packet reached no per-packet comparison. **Three sources state the emission.** `wireshark/source/packet-ja4.c:1400` tests the flags and `wireshark/source/packet-ja4.c:1402` writes the value. `python/ja4.py:555` tests the two flags, `python/ja4.py:556` calls `finalize_ja4ssh`, and `python/ja4.py:370` defines it. The port emits the window at `ja4plus/fingerprinters/ja4ssh.py:268`. **The emission clears the four counters of the window, and the sources split on that.** `python/ja4.py:377` deletes the stream from the cache, so no later packet of the stream reads a counter of the emitted window. The port clears the counters at `ja4plus/fingerprinters/ja4ssh.py:439`. `wireshark/source/packet-ja4.c:1485` clears the counters of a filled window alone, so Wireshark writes one value twice, at `ssh-r.pcap` frame 1850 and at frame 1851. **This is a reading and not a ruling, because the port answers the split.** The maintainer's rule of 2026-08-12 states that the library matches the port one to one. The same rule states that the library follows the FoxIO material where that leaves a choice. FoxIO's own Python reaches the same answer. **Name which Python a citation reads.** `python/ja4.py` is FoxIO's reference Python inside the corpus, and the port is `Crank-Git/ja4plus`. **The flag test reads the two flags alone.** `python/ja4.py:555` and `ja4plus/fingerprinters/ja4ssh.py:268` each test the FIN bit and the ACK bit, so a FIN+PSH+ACK packet reaches the emission. `wireshark/source/packet-ja4.c:1400` tests `tcp_flags == 0x011` instead. The two tests move nothing on this corpus, because `ssh-r.pcap` frame 335 and frame 339 each hold the flags `0x0011`. **FR-parity-28 holds without a change**, so a window that holds no SSH packet emits nothing and the second FIN+ACK packet of a close emits nothing. **FR-parity-29 holds without a change**, and `CloseOpenWindows` still emits the window of a connection that sends no FIN+ACK packet. **The slice writes no register entry, and #223 owns the four JA4SSH entries.** Measured on `batch/236-ja4ssh-remainder` at `53e8678` with the corpus present: the run reports 1086 matches and 1284 deviations before, and 1091 matches and 1279 deviations after. The per-stream set reports 741 matches and 464 deviations before, and 742 matches and 463 deviations after. The per-packet set reports 345 matches and 820 deviations before, and 349 matches and 816 deviations after. The register holds 198 keys before and after, 198 accepted deviations before and after, and no entry reads as closed. Coverage reads 72.3% before and after, and the suite holds 808 tests before and 815 after. **Five comparisons reach a match**: `gre-sample.pcap` frame 30, `ssh-r.pcap` frames 335, 1826 and 1850, and `ssh-r.pcap` stream 2 `JA4SSH.5`. **Four per-packet comparisons stay open, and the clear causes each one.** Wireshark writes one value twice, and the library writes it once. `gre-sample.pcap` frame 31 and `ssh-r.pcap` frames 339, 1831 and 1851 therefore hold a value the library does not produce. **Part c of `ssh-r.pcap` stream 1 falls from `c5s5` to `c4s5` and reaches the FoxIO value**, which completes the reading that #221 recorded. `sshv1.pcap` frame 72 and `v6.pcap` frame 72 now hold a value that differs, and the SSH version 1 packet boundary causes that difference. **The four keys of #223 each still differ in part a alone.** The `## Terms` table gains `FIN+ACK packet`. **The change opens no issue in the port, because the port already emits the window on a FIN+ACK packet.** Recorded by #222. |
| 23 | 2026-08-12 | **The project manager declined four FoxIO JA4SSH values, and the register records each one.** The library keeps the value it produces, and it changes on no line. **The ruling is provisional, and it is the one provisional ruling of the register.** `.claude/rules/rulings.md` reserves a ruling to the maintainer. The maintainer delegated the session and slept, and the project manager ruled under that delegation. **A later reader confirms this ruling, and never assumes it.** **The four vectors contradict a rule that the reference implements.** `docs/specs/foxio/JA4SSH.md` R13 states that the mode is `0` when the side sent no SSH packet. Four implementations enforce R13: `zeek/ja4ssh/main.zeek:63`, `wireshark/source/packet-ja4.c:400`, `rust/ja4/src/ssh.rs:284` and `python/ja4ssh.py:51`. `testdata/foxio/python/ssh-scp-1050.pcap.json` holds `c112s1460_c0s200_c36s0` and `c112s1460_c0s200_c23s0`. Each of those two values pairs a client mode of `112` with a client packet count of `0`. **A shallow copy causes the defect.** `python/ja4ssh.py:8` opens the module-level template, and `python/ja4ssh.py:9` and `python/ja4ssh.py:10` hold two mutable lists in it. `python/ja4ssh.py:88` and `python/ja4ssh.py:128` each open a window with `entry['stats'].append(dict(ja4sh_stats))`, and `dict()` copies one level. Every window therefore reads one shared payload list at `python/ja4ssh.py:146`. R8 states that the counters reset after each window, and the two payload lists do not reset. **The per-stream vector of `ssh-r.pcap` shows that signature.** It writes `c64s64` for the first window of each of the three streams, and `c76s76` for windows 2 through 5 of stream 2. **The per-packet vector agrees with this library.** `testdata/foxio/wireshark/ssh-r.pcap.json` holds `c48s21_c6s5_c4s5` and `c76s76_c104s96_c19s82`, which are the two library values. **The library needs no change, because FR-parity-27 already holds the rule.** The mode reads the packet lengths of its own window alone, and `TestJA4SSHReadsTheModeOfTheWindowAlone` holds that rule. Port issue #96 ruled it. **Four entries reach `testdata/deviations.json` with `"capability": false` and the ruling `#223`.** They are `ssh-r.pcap/1/JA4SSH.1`, `ssh-r.pcap/2/JA4SSH.1`, `ssh-scp-1050.pcap/0/JA4SSH.3` and `ssh-scp-1050.pcap/0/JA4SSH.4`, and each one differs in part a alone. **Two reasons serve the four keys, because the two captures show the defect differently.** The two `ssh-scp-1050.pcap` keys cite R13, and the two `ssh-r.pcap` keys cite the shared payload list. **`ssh-r.pcap/2/JA4SSH.5` matches in full after #222, so the register holds no entry for it.** **This slice moves no fingerprint value.** Measured on `batch/236-ja4ssh-remainder` at `cc2c522` with the corpus present: the run reports 1091 matches before and after, and 1279 deviations before and 1275 after. The per-stream set reports 742 matches before and after, 463 deviations before and 459 after, and 163 accepted deviations before and 167 after. The per-packet set reports 349 matches, 816 deviations and 35 accepted deviations before and after. The register holds 198 keys before and 202 after, and no entry reads as closed. Coverage reads 72.4% before and after, and the suite holds 815 tests before and after. **This slice also carries seven writing repairs that a review of #222 found.** Three sentences of 26 words in `ja4ssh.go` now read as two sentences each. `CHANGELOG.md` writes `FIN+ACK packet` on the two lines that wrote `a FIN frame` and `the second FIN packet`. Round 22 above loses two sentences of 27 and 30 words, and it writes `FIN+ACK packet` for `a FIN frame`. `ja4ssh_fin_ack_test.go:231` now writes the whole term, because rule 6 of `.claude/rules/ste.md` bars one string as two parts of speech. **The change opens no issue in the port, because the port reads the mode of the window alone.** Port issue #96 holds that rule, so this ruling removes a divergence rather than creating one. **Two facts reverse this ruling.** FoxIO repairs the shallow copy and republishes the vectors, or a FoxIO source that outranks R13 states that the mode reads an earlier window. Recorded by #223. |
| 24 | 2026-08-12 | **The last-emission comparison of round 15 and round 20 above reads one connection, and this round reverses no part of either.** Round 15 records that the harness compares the last JA4L emission of one stream, and round 20 narrows the rule to `JA4L-C` and `JA4L-S`. **One vector entry names one connection, and FoxIO numbers two entries of one capture with one stream number.** `chrome-cloudflare-quic-with-secrets.pcapng` holds a TCP connection on the source port `57098` and a QUIC connection on the source port `50280`, and the per-stream vector writes the stream number `0` for both. The vector group therefore held two values, the adapter wrote an occurrence number for them, and the last-emission rule reached no value of that group. **The client measurement point of the TCP connection moves, so the surplus first value shifted every later occurrence by one.** The library reports `30_64` on frame 3 and `149_64` on frame 4, and the three occurrences held `30_64`, `149_64` and `113_64_quic` against the two values `149_64` and `113_64` that the vector holds. `conformance_test.go` now holds `conformanceMovedPoints`, which keeps the last value of each connection of one group and numbers the connections in the order in which each one first reported. A group whose vector key carries no occurrence number keeps the last value of the whole group, so the bare key compares the value it compared before. **This is a reading and not a ruling.** No FoxIO source addresses the harness, and the reading states what a vector entry names. `ja4l.go` changes on no line, and the QUIC path emits one `JA4L-C` value for this capture. **The slice moves no fingerprint value, and it writes no register entry.** Measured on `batch/236-ja4ssh-remainder` at `a3b2bf9` with the corpus present: the run reports 1078 matches and 1293 deviations before, and 1079 matches and 1291 deviations after. The per-stream set reports 737 matches and 469 deviations before, and 738 matches and 467 deviations after. The per-packet set reports 341 matches and 824 deviations before and after, so no per-packet comparison moves. The register holds 198 keys before and after, and no entry reads as closed. Coverage reads 72.3% before and after, because the new test carries the `conformance` build tag and `make cover` runs no tagged file. **One deviation of the group stays open.** `chrome-cloudflare-quic-with-secrets.pcapng/0/JA4L-C.2` reports `113_64` against `113_64_quic`, which is the QUIC marker that round 18 above holds. **The change opens no issue in the port, because it moves no fingerprint value and no exported name.** Recorded by #215. |
| 25 | 2026-08-12 | **Batch #248 closes four session-5 follow-ups, and three of the four issue premises were false.** **The batch moves one comparison and it reverses nothing.** **#206 closes as a reading, and the library keeps the QUIC `JA4S` value.** The issue read `python/common.py:101` as a rule that the reference keeps the first `JA4S` of a stream. The line exists and the inference does not follow. `python/common.py:77-83` routes a QUIC packet to `quic_cache` and a TCP packet to `conn_cache`, so the two values never reach one cache. `python/ja4.py:591` gates `to_ja4s` on `x['hl'] == 'tls'`, and `python/ja4.py:398` with `python/ja4.py:530-533` sets `x['hl'] = 'quic'`, so FoxIO's reference Python computes no `JA4S` for a QUIC packet and the `'q'` branch of `python/ja4.py:173` is unreachable. **That is a gap and not a rule.** `docs/specs/foxio/JA4S.md` R3 records the rank 1 image stating `Protocol, TCP = "t" QUIC = "q"`, and `wireshark/source/packet-ja4.c:729`, `rust/ja4/src/tls.rs:481-487` and `zeek/ja4s/main.zeek:130-131` each build the value. **#205 closes as delivered by #215, and it adds one register entry.** Round 24 above already recorded that `chrome-cloudflare-quic-with-secrets.pcapng/0/JA4L-C.2` stays open, and the entry accepts it under the ruling of round 18. **The maintainer ruled on #212, and the library changes on no line.** No FoxIO reference reports `JA4L-S` on the SYN: `wireshark/source/packet-ja4.c:1272-1275` stores `timestamp_A` alone and reports at `:1350` and `:1375`, and `python/ja4.py:563-566` stores `A` alone while `calculate_ja4_latency` is invoked at `python/ja4.py:572` and `:587`. The port reports there, at `ja4plus/fingerprinters/ja4l.py:440-449`, so rule 1 of `.claude/rules/parity.md` puts the FoxIO material above the port and the library does not follow it. **The three references split on an interval below zero.** Wireshark prints a negative number, `common.py:179-182` returns a large positive number because `timedelta.microseconds` is the sub-second field normalized into `[0, 999999]`, and `zeek/ja4l/main.zeek:151-157` returns before it assigns `ja4l_s`. `zeek/ja4l/main.zeek:7` states that JA4L cannot work when traffic is out of order. **Two tests hold the ruling, because no vector reaches it.** **The maintainer ruled on #229, and the library changes on no line.** `JA4L.png` states no rule for a reused QUIC four-tuple. Wireshark and Rust never reset, at `wireshark/source/packet-ja4.c:1412-1434` and `rust/ja4/src/stream.rs:395`, and FoxIO's Python and Zeek evict when the first connection completes, at `python/ja4.py:585-588` and `zeek/ja4l/main.zeek:198`. **The trigger in each implementation that evicts is the completion of the first connection, and never a signal of a second one**, so the three candidates the issue named reach no source. The port needs no change, because `ja4plus/fingerprinters/ja4l.py:436` is the sole call site of `_restart_connection` and it sits in the TCP branch. **The maintainer ruled on #247, and `#127` stands.** `docs/specs/foxio/JA4L.md` R3 states three parts and carries no hedge, and it is a rank 1 image rule. Wireshark and Zeek write three parts and Rust and FoxIO's Python write two, which is the split R29 records. **The corpus holds 114 `JA4L` values and every one holds two parts.** `docs/specs/foxio/port-register.md:80` quotes the image and then sets it aside, so the ruling ranked conformance evidence above a rank 1 source inside a declared split. **That ranking was implicit, and this round records it.** **#227 regenerates the report and guards it.** `.github/workflows/ci.yml` gains a step that fails when the tracked report and the run disagree, and it adds a failure while reading no exit status of the run, so an honest exit 2 still reports a deviation. **Measured on `batch/248-conformance-followups` at `1462fc3` with the corpus present: the run reports 1091 matches before and after, 1275 deviations before and 1274 after, and 202 accepted deviations before and 203 after.** The per-stream set reports 742 matches, 459 deviations and 167 accepted before, and 742 matches, 458 deviations and 168 accepted after. The per-packet set reports 349 matches, 816 deviations and 35 accepted before and after. **The register holds 202 keys before and 203 after, and no entry reads as closed.** `go test -race ./...` passes in four packages and `golangci-lint run` reports no issue. **Eight issues were filed from this batch's findings.** #249 records a `JA4L-S` latency that differs by 1705 microseconds and that no ruling explains. #250 records that the per-stream conformance key collides a TCP stream with a UDP stream of one number. #253 records that FoxIO's reference Python reads `timedelta.microseconds`, so an interval above one second is also wrong. #254 records that every citation in `docs/specs/foxio/` is missing the `reference/` prefix. #255 records that `#127` names the JA4L ruling here and the ALPN ruling in the port. #257 records that a stale `golangci-lint` cache fails a gate against a removed path. Port issues #595 and #596 carry the two cross-repository halves. Recorded by #248. |
| 26 | 2026-08-12 | **`.claude/rules/rulings.md` gains the section `What a delegated session may rule`, and it reverses nothing.** The maintainer approved this delegation on 2026-08-12. **A schema violation has one right answer, and a reference split has none**, and that sentence is the boundary the section states. **The project manager rules a schema violation under a delegation, and it rules no reference split.** **The section states that a delegated ruling is a ruling, and never a reading.** No source settles a question where a published FoxIO value contradicts a published FoxIO rule. **`## The two words` of that file gains one sentence that points at the new section**, so the table no longer reads as absolute. **Five conditions must each hold.** A published rule states the answer, and it ranks at 1 or at 2 in the source ranking. Every FoxIO implementation enforces that rule. A recorded measurement proves the violation, and each citation names a file and a line. The register entry or the test carries a provisional marker that names the issue. The entry names a reversal path. **A question that fails one condition belongs to the maintainer**, and the project manager labels the issue `status:needs-feedback`. **The maintainer confirms a delegated ruling, or reverses it**, and an unconfirmed one stays provisional. **The stop conditions of that file hold without a change.** No rank 1 source and no rank 2 source settles a question they name. **The section names three examples from session 5.** #223 is the first delegated ruling, and the maintainer confirmed it on 2026-08-12. `docs/specs/foxio/JA4SSH.md` R13 is a rank 1 rule, and four implementations enforce it. `testdata/foxio/python/ssh-scp-1050.pcap.json` holds `c112s1460_c0s200_c36s0` against that rule. #216 belongs to the maintainer, because both candidate answers change the exported surface and condition 1 fails. #247 belongs to the maintainer, because the four implementations split two against two and condition 2 fails. Round 25 above records the ruling the maintainer then made. **This slice moves no fingerprint value, it writes no register entry, and it changes no Go file.** Measured on `batch/259-freeze-and-gates` at `882e21a` with the corpus present: the run reports 1091 matches, 1274 deviations and 203 accepted deviations before and after. The per-stream set reports 742 matches, 458 deviations and 168 accepted deviations before and after. The per-packet set reports 349 matches, 816 deviations and 35 accepted deviations before and after. The register holds 203 keys before and after, and no entry reads as closed. **The change opens the port half, and `Crank-Git/ja4plus` #597 holds it.** `.claude/rules/parity.md` states that a ruling lands in both repositories or in neither. The port keeps this rule in `.claude/rules/conformance.md`, because the port holds no `rulings.md`. The port names the decider `the user`, and the port issue uses that word. **The port needs no fingerprint change**, because port issue #96 already reads the JA4SSH mode of the window alone. **The `## Terms` table holds none of `maintainer`, `project manager` and `delegated ruling`, and this round adds none of them.** #261 records that gap, because `## Terms` sits outside the scope of #246. Recorded by #246. |
| 27 | 2026-08-12 | **The library gains one exported name, and batch #259 moves no fingerprint value.** **`CloseConnectionWindow` emits the open JA4SSH window of one connection and then evicts it.** The maintainer ruled on 2026-08-12 that a second method flushes one connection, and that `CleanupConnection` keeps its signature. **Epic 10 freezes the exported surface for the whole `v1` series, so the name lands now or never**, and the maintainer confirmed the name on the same day. The method goes on the `WindowCloser` interface beside `CloseOpenWindows`, which follows the #53 ruling that `Fingerprinter` does not change. `Processor` and `SyncProcessor` each gain the wrapper, and the #148 guard reported the new exported method and was satisfied rather than bypassed. **Four names were rejected**: `CloseOpenWindow` differs from the existing name by one letter and by its behavior, `FlushConnection` rotates a second word onto a concept that `Close` already names, `CloseConnection` takes a phrase that the `## Terms` table gives to the FIN+ACK packet, and `EvictConnection` hides the emission. **The reference measures this moment per connection.** `zeek/ja4ssh/main.zeek:160` handles `connection_state_remove`, which is a per-connection instrument and not an end-of-source pass. **No conformance vector separates the two answers**, so ten tests build the separating sequence and `.claude/rules/rulings.md` records the ruling that way. **A revert experiment failed eight of the ten**, and the two that passed assert zero values and are correct to pass. **The port carries the identical gap.** `ja4plus/fingerprinters/ja4ssh.py:530` pops and returns `None`, `:408` is private, and `:382` is the only public flush, so port issue #598 proposes the same name. **The delegation boundary is now written down.** `.claude/rules/rulings.md` gains `## What a delegated session may rule`, with five conditions and three worked examples from this session. **A schema violation has one right answer, and a reference split has none**, and that sentence is the boundary. A reference split is never delegable, and a delegated ruling stays provisional until the maintainer confirms it. Port issue #597 carries the same rule, and it targets that repository's own rules file. **The four #223 entries name the maintainer, because the maintainer confirmed that ruling on 2026-08-12.** The register held the word `provisional` on four entries and it now holds it on none. **A stale linter cache produced a false failure at a gate, and the cause was not the one the issue named.** The project manager wrote that `golangci-lint` walks `.claude/worktrees/` and caches by that path. **The linter never walks a dot-directory**, which a planted file measured. **The cause is the user-global cache replaying an absolute path**, which fails the generated-file-filter processor and so disables every `//nolint` directive, and a suppressed finding then reappears against a file that no longer exists. **The proposed exclusion rule would have changed no behavior.** The `Makefile` now holds one linter cache for each checkout, `.golangci.yml` is untouched, and the finding count on tracked files reads 0 before and 0 after. `scripts/check-lint-cache.sh` holds it, its first step requires the defect to reproduce, and a mutant that shares the cache makes the guard fail. **Measured on `batch/259-freeze-and-gates` at `ef23784` with the corpus present: the run reports 1091 matches, 1274 deviations and 203 accepted deviations before and after.** The per-stream set reports 742 matches, 458 deviations and 168 accepted, and the per-packet set reports 349 matches, 816 deviations and 35 accepted, each unmoved. **The register holds 203 keys before and after, and no entry reads as closed.** The suite holds 819 tests before and 835 after. **Two issues record what this batch declined to do.** #261 records that the `## Terms` table holds none of `maintainer`, `project manager` or `delegated ruling`. #264 records that no spec file numbers the new method as a requirement, and Epic 10 reads the spec at the freeze. Recorded by #259. |
| 28 | 2026-08-12 | **The review of #267 read the members of batch #259 together, and it found six false statements.** **No member made any one of them false on its own.** Each member passed its own review, and a pair of them produced each defect. **`README.md` stated that `WindowCloser` carries one method, and the interface carries two.** The table listed `CloseOpenWindows` alone, and it now holds the row for `CloseConnectionWindow`. **The `SyncProcessor` method list of `README.md` omitted `CloseConnectionWindow`.** `sync_processor_test.go:12-21` already held the six names, so the README was the half that no test reads. **`docs/specs/features/03-concurrency.md` ended its method list with `and nothing else`, and two names were missing from it.** `CloseOpenWindows` was already absent before this batch, so batch #259 widened a defect that #53 opened. Epic 10 reads that file at the API freeze. **`CHANGELOG.md` recorded no entry for the four new exported names.** `docs/specs/features/10-release.md` FR-release-31 requires the release section to record every added exported name, and the new entry matches the shape of the `CloseOpenWindows` entry above it. **`docs/specs/spec.md` and `CLAUDE.md` held two gate definitions, and one forbade what the other prescribed.** Step 3 of `## A change is done when` read `golangci-lint run`, and `CLAUDE.md:99-103` states that a person runs the linter through `make lint`. The spec now names `make lint`. **`licenseClaimSkipDirs` of `readme_license_test.go` did not name `.golangci-cache`.** #257 put that directory inside the checkout, and `TestNoFileStatesThatTheFoxIOMethodsAreBSD3Clause` walks `.` with an extension filter that admits the empty extension and `.txt`. **After a `make lint` run, every `go test ./...` run read the whole linter cache**, which holds 1987 files and 7.8 MB in this worktree. That test reports 0.48 seconds before the repair and 0.22 seconds after, each measured with a warm build cache. **Repairs 1, 2 and 3 state what the code does, and none of them states that `WindowCloser` is the permanent home of the method.** #268 reads that question, and it holds the batch. A move of the method changes one word and one table row of `README.md`. It also changes one signature and one criterion of `docs/specs/features/03-concurrency.md`, and one clause of `CHANGELOG.md`. **This slice moves no fingerprint value, it writes no register entry, and it changes one Go file on one map.** Measured on `batch/259-freeze-and-gates` at `f26d020` with the corpus present: the run reports 1091 matches, 1274 deviations and 203 accepted deviations before and after. The per-stream set reports 742 matches, 458 deviations and 168 accepted deviations before and after. The per-packet set reports 349 matches, 816 deviations and 35 accepted deviations before and after. The register holds 203 keys before and after, and no entry reads as closed. **The `## [Unreleased]` preamble of `CHANGELOG.md` still states 1275 deviations, 202 accepted deviations and 202 register keys.** #270 records that, and this slice repairs no part of it. Recorded by #269. |
| 29 | 2026-08-12 | **The library gains one exported name, and batch #259 moves no fingerprint value.** **`WindowCloser` declares `CloseOpenWindows` alone, and `ConnectionWindowCloser` declares `CloseConnectionWindow` alone.** The maintainer ruled the split on 2026-08-12, and #268 records the ruling. **The measurement is a silent regression of a shipped capability, and not a missing new one.** A third-party type that implements `CloseOpenWindows` alone printed `compiles as Fingerprinter: true` and `fp.(WindowCloser) holds: false`, so the two-method assertion skipped it at `processor.go:188` and at `processor.go:218`. **The type lost `CloseOpenWindows`, which `v0.3.0` already delivers, and the build reported nothing.** **The per-capability assertion is part of the ruling.** Each call site asserts the interface that declares the one method it calls, as `http.Flusher` and `http.Hijacker` each carry one method. A combined assertion reintroduces the same defect. **The cost of each answer is asymmetric.** The split costs one recorded name under FR-release-1, and the two-method interface freezes an un-extendable shape for the whole `v1` series under FR-release-5. **The name `ConnectionWindowCloser` follows `WindowCloser` and the method it declares.** Three names were rejected: `WindowFlusher` rotates a second word onto a concept that `Close` already names, `ConnectionCloser` takes a phrase that the `## Terms` table gives to the connection itself, and `SingleWindowCloser` counts windows where the distinction is the connection. **No conformance vector separates the two shapes**, so five tests build the separating type and `.claude/rules/rulings.md` records the ruling that way. `Processor.CloseOpenWindows` and `Processor.CloseConnectionWindow` each call one unexported dispatch function, so a test reaches the real dispatch with a fingerprinter that `Processor` holds no field for. **A revert experiment failed three of the five.** The mutant restored the two-method `WindowCloser` and pointed both call sites at it. The two that passed are correct to pass: one asserts that each dispatch skips the other capability, which a combined assertion also satisfies, and one reads `ConnectionWindowCloser`, which the mutant kept declared so that the suite compiles. **FR-parity-30 of `docs/specs/features/08-python-parity.md` and the open question that asked where `CloseOpenWindows` sits are reconciled in the same change.** FR-parity-30 described the port's shape and contradicted the shipped #53 design, and the question stated that the freeze is the last chance to choose. **The `Open questions` list of that file drops the question and renumbers, so no ordinal names it.** **The port needs no change.** The port dispatches by inheritance, not by capability discovery: `ja4plus/fingerprinters/base.py:162` makes `close_open_windows` an inherited no-op and `ja4plus/processor.py:261-263` calls it on every fingerprinter with no capability test. Port issue #598 adds one method to one class and reaches no interface. **Measured on `batch/259-freeze-and-gates` at `f26d020` with the corpus present: the run reports 1091 matches, 1274 deviations and 203 accepted deviations before and after.** The per-stream set reports 742 matches, 458 deviations and 168 accepted, and the per-packet set reports 349 matches, 816 deviations and 35 accepted, each unmoved. **The register holds 203 keys before and after, and `testdata/deviations.json` gains no entry.** The #148 guard reported no new exported `Processor` method, and it passes after the split. **The same commit updated the two reader documents.** `README.md` names `WindowCloser` and `ConnectionWindowCloser` together, and its interface table lists one method for each of them. `CHANGELOG.md` gains a second `Added` entry, which records the `ConnectionWindowCloser` names. **That entry counted those names by a rule the sibling `WindowCloser` entry does not use**, and #278 repairs the count. Recorded by #268. |
| 30 | 2026-08-12 | **This round repairs prose alone. It changes no Go file, and it moves no fingerprint value.** **The project manager hand-edited `README.md`, `CHANGELOG.md` and this file at the gate of batch #259, and that one commit produced four defects.** #278 records the lesson: gate housekeeping that changes prose belongs to a worker with a review, exactly as feature work does. **Round 29 above described the tree that stood before its own commit.** Its closing sentences read that `README.md` named no `ConnectionWindowCloser`, that the interface table listed one method for the two interfaces, and that the `CHANGELOG.md` entry counted four exported names where the surface held six. The repairs landed in that same commit, so each sentence was false on the day it was written. The number six matched neither enumeration. This round rewrites those sentences to describe the tree that #268 produced, and it drops the number six. **`CHANGELOG.md` now counts an added exported name by one rule.** An entry counts an interface as one exported name, and it counts no second name for the method that the interface declares. The `ConnectionWindowCloser` entry read `Five exported names`, and it listed both `ConnectionWindowCloser` and `ConnectionWindowCloser.CloseConnectionWindow`. It now reads `Four exported names`, which is the count the sibling `WindowCloser` entry already gives to the identical shape. **The `### Added` heading carries the rule in two sentences**, so a later entry follows one rule, and FR-release-31 makes that section the authoritative list of added exported names. Two lines of that entry ran to 141 and to 111 characters against a modal maximum near 93, and the paragraph now wraps. **`README.md` named one interface where the #268 split delivers two.** It read that `Processor` and `SyncProcessor` each reach every fingerprinter that implements `the interface`. A reader concludes from that sentence that a fingerprinter must implement both interfaces to reach either method, and that is the misreading which produced the silent regression #268 repairs. The paragraph now names `WindowCloser` and `ConnectionWindowCloser` separately, and it states that a fingerprinter which implements one of the two alone reaches that interface's method. **Three documents stated that `CloseOpenWindows` joins the `Fingerprinter` interface, and the #53 ruling of 2026-08-11 made that statement false.** The ruling put the method on the second optional interface `WindowCloser`, so that `Fingerprinter` never changed. `docs/specs/features/08-python-parity.md:240` and `:255` now state the shipped shape, and #268 rewrote FR-parity-30 in that same file and left these two untouched. **FR-release-51 required the CHANGELOG to record the method as a breaking change to the exported `Fingerprinter` interface, and the CHANGELOG truthfully does not record that.** A numbered release requirement was therefore unsatisfiable, and Epic 10 reads that file at the freeze. FR-release-51 now requires the CHANGELOG to record the method on the optional interface `WindowCloser`, and to record that `Fingerprinter` does not change. The `WindowCloser` entry of `CHANGELOG.md` already satisfies both halves. **`.claude/skills/release/SKILL.md` no longer names the method as a breaking change, and it no longer asks the releaser where the method sits.** The maintainer settled that question on 2026-08-11 and again on 2026-08-12. **`### Issues that wait on a ruling` above still lists #53 as open, and this round repairs no part of it.** #53 is closed and round 17 above records the ruling, so that section is stale for a separate reason, and issue #279 holds the repair. **This round writes no register entry, and `testdata/deviations.json` gains no entry.** Measured on `issue/278-gate-repairs` at `d5d261a` with the corpus present: the run reports 1091 matches, 1274 deviations and 203 accepted deviations before and after. The per-stream set reports 742 matches, 458 deviations and 168 accepted deviations before and after. The per-packet set reports 349 matches, 816 deviations and 35 accepted deviations before and after. The register holds 203 keys before and after, and no entry reads as closed. **The port needs no change**, because Python states no interface and `ja4plus/processor.py:261-263` calls the method with no capability test. Recorded by #278. |
| 31 | 2026-08-12 | **The library gains one exported name, and the change moves no fingerprint value.** `FingerprintResult` gains the field `OriginalOrder`, which carries the FoxIO `JA4_o` value. `JA4_o` hashes each list of the wire-order raw form, and `RawOriginalOrder` holds the same two lists unhashed, so one function builds the two. `testdata/foxio/reference/python/ja4.py:291` states the form. **The FoxIO key suffix names each of the four value fields.** `Raw` carries `_r`, `OriginalOrder` carries `_o` and `RawOriginalOrder` carries `_ro`, so the name follows the suffix. This project rejects three other names. `FingerprintOriginalOrder` breaks the suffix rule. `HashedOriginalOrder` names a transform that no other field names. `JA4O` names one method on a struct that eleven methods share. Epic 10 freezes the exported surface for the whole `v1` series, so the name lands before that freeze. `docs/specs/features/05-conformance-gaps.md` FR-gaps-24 through FR-gaps-26 number the field. **The measurement ran on `origin/batch/281-raw-forms` at `d8ac53f` with the corpus present.** The run reports 1400 matches, 1036 deviations and 203 accepted deviations before, and 1545 matches, 940 deviations and 203 accepted deviations after. The per-stream set reports 898 matches and 346 deviations before, and 1043 matches and 250 deviations after. The per-packet set reports 502 matches and 690 deviations before and after. The register holds 203 keys before and after, and no entry reads as closed. **145 `JA4_o` comparisons close, and 49 new deviations appear.** Each new deviation is a `JA4_o` value that the vector does not hold, and the sibling `JA4` key of each one already deviates. **#287 records one reference split that the maintainer rules.** The Python reference writes the empty-hash sentinel into the wire-order extension part when the sorted list is empty. The Rust reference hashes the wire-order list. Four comparisons reach the split. Recorded by #277. |
| 32 | 2026-08-12 | **This round repairs prose alone. It changes no fingerprinter, and it moves no fingerprint value.** **An exported doc comment stated a rule that the corpus falsifies.** The `FingerprintResult` doc block of `types.go` stated that a value field stays empty `because FoxIO publishes no key of that name for every method`. The per-packet vector set holds 126 `"ja4.ja4h_r"` fields, and `conformance_test.go:66` maps that field, so the sentence was false. The true rule names the vector set. The FoxIO per-stream vector set publishes `JA4H_ro` and no `JA4H_r` value, and the FoxIO per-packet vector set publishes `ja4.ja4h_r`. `ja4h.go:73` already held the scoped rule, and #277 copied the unscoped form into the exported surface. **Epic 10 freezes the exported surface, so a false rule there outlives every other copy of it.** **`docs/specs/features/05-conformance-gaps.md` FR-gaps-26 carried the same unscoped shape, and its claim is true.** Neither vector set publishes a second `_o` key, so the requirement now names the two vector sets rather than FoxIO as a whole. **The per-packet adapter now emits the `_o` value.** `conformanceValuesOfResult` of `conformance_test.go` emitted the fingerprint, the `_r` value and the `_ro` value, and the per-stream twin `conformanceStreamValuesOfResult` emits four. The per-packet vector set names no JA4 field, and `conformanceMethodOfResultType` maps no result type to `JA4`. The new branch therefore reaches no comparison at the pinned commit. The four counts read 1545 matches, 940 deviations, 203 accepted deviations and 203 register keys before and after the branch. **The tracked conformance report was stale, and the staleness step that #227 added caught it.** No member of batch #281 regenerated `docs/audit/conformance.md`, and this round regenerates it. `CHANGELOG.md` records the measurement of the whole batch once. Issue #290 records this round. |
| 33 | 2026-08-12 | **Batch #293 repairs four fingerprint paths, and it puts 98 entries into the register.** **#55 writes the two-digit form for a zero JA4T value and a zero JA4TS value, and it closes 10 comparisons.** Ruling #125 already stated that form, and the code carried it on no line until this member. **#286 repairs three defects in the JA4H path, and it closes 5 comparisons.** The three are a split on the line feed, an answer on the first TCP segment, and a second header filter in `ja4hPartA`. **The stated cause of #286 was false, and the worker measured that before it changed one line.** The issue named an empty header value, and no defect of the three reads an empty header value. **#295 steps `ParseClientHello` over a leading non-handshake record, and it closes 40 comparisons.** **The FoxIO implementations disagree on how many client hellos one stream reports.** Rust keeps the first, Zeek computes once per connection, and FoxIO's reference Python records every one. **This member follows the vector, under rule 3 of `.claude/rules/parity.md`**, which makes the shared vector set the gate. **#287 writes the `JA4_o` zero sentinel into the wire-order part, and it closes 4 comparisons.** The maintainer ruled that split on 2026-08-12, and round 31 above records the split that #277 found. **#299 puts 98 entries into `testdata/deviations.json` under the ruling of #42, and the maintainer extended that ruling to every QUIC stream the vector omits.** **The base-key count on the six streams reads 22, and not the 4 that #289 estimated.** **#306 repairs the `## [Unreleased]` preamble of `CHANGELOG.md`, and it builds the guard that holds the paragraph true.** That paragraph was false at the end of three consecutive batches, because it states a whole-tree count and each member moves it by a slice. #270 holds the first instance, and round 32 above holds the second. **The enumeration read `150 + 35 + 13 + 4` against 203 keys for four batches, and 202 is not 203.** The register held 14 `#197` entries at `34b6715`, so the `13` was wrong on the day it was written. The paragraph now reads `248 + 35 + 14 + 4 = 301`. **`changelog_counts_freshness_test.go` reads the four counts and the enumeration, and it compares each one against `docs/audit/conformance.md` and `testdata/deviations.json`.** **The guard adds a failure, and it reads no exit status of the conformance run**, so an honest exit 2 still reports a deviation. It reads the tracked report and never the corpus, so it needs no corpus and it skips for no reason. The #227 staleness step makes that report current at every commit, and this guard rests on that chain. **Four mutants prove the guard.** A moved match count, a moved ruling count, the historical `13` and a ruling the register does not hold each produce a named failure. **The `## Terms` table gains `guard`**, because rounds 21, 25 and 27 above each use the word and no row defined it. **The paragraph keeps its counts, and the guard is the reason.** A sentence that names no number cannot go stale, and it also tells a reader nothing. **Measured on `issue/306-changelog-guard` with the corpus present: the run reports 1602 matches, 783 deviations, 301 accepted deviations and 301 register keys before and after.** The batch ran from `34b6715` to `db53765`, and it moves the four counts from 1545, 940, 203 and 203. **This round changes no fingerprinter and no `internal/parser/` file, and it moves no fingerprint value.** **The change opens no issue in the port**, because it moves no fingerprint value and no exported name. Recorded by #306. |
| 34 | 2026-08-12 | **Batch #321 changes no fingerprinter, and it moves no fingerprint value.** **The four counts read 1623 matches, 680 deviations, 409 accepted deviations and 409 register keys before the batch and after it.** The run reports 0 stale register entries. **#307 gives the conformance engine a stale-entry check, and the `## Terms` table above gains the row `stale entry`.** `compareConformance` read the presence of a register key alone, at `conformance_engine_test.go:99`. An entry therefore stayed accepted after a later change moved the value that entry records. The check now reads the recorded `ours` value, and it fails `make conformance` with one line for each stale entry. `docs/audit/conformance.md` gains the summary row `Stale register entries` and the section `## Stale register entries`. That section states the result of every run. **No entry of the 409 trips the check.** **The check covers the value half alone, and never the orphan half.** A register key that the run never reaches falls outside the key loop of the engine. An orphan check therefore needs a second pass over the register. **`docs/specs/features/04-conformance-harness.md:115-125` numbers the check as FR-conformance-33a through FR-conformance-33g.** The behavior reached no requirement when `6a03362` merged, and `4a30c33` numbered it. The ordinal carries a letter, because `.github/workflows/ci.yml`, `corpus_fetch_test.go`, `conformance_skip_marker_test.go` and this file cite FR-conformance-34 through FR-conformance-39. **#264 numbers `CloseConnectionWindow` in four locations, and the exported surface does not change.** `docs/specs/features/08-python-parity.md:136-154` holds FR-parity-33a through FR-parity-33h, and `:287` holds the eviction row. The `## Parity with ja4plus` register above gains the row for the method, and the `Cleared by` cell of `JA4SSHFingerprinter` names it. **The ordinal carries a letter, because FR-parity-34 through FR-parity-60 already exist.** A renumber would break the references that ten test files and four documents hold. **The premise of #303 is false on this tree, and the member measured that before it wrote one line.** The issue reports two JA4H values for `http-empty-useragent.pcap`. 10 consecutive runs of `make conformance` on the fork point `d7b01d0` produced one deviation key set and one report, and 3 more runs agreed. `ja4h.go:315` is the one map range that the JA4H value reads, and `ja4h.go:318` sorts that result by the cookie name. **#286 is the probable closer**, because `internal/parser/http.go:90` now returns nil for a header block that has not ended. `ja4h_determinism_test.go` holds two guard tests, and each one failed with that sort removed. **#305 forbids `git stash` in a worktree of this repository.** `CLAUDE.md` `## Conventions` holds the rule, and `.claude/rules/worktrees.md` holds the reason, the three alternatives and two readings. **A worktree cannot hold a stash of its own**, because `git-stash(1)` writes `refs/stash` and `git-worktree(1)` shares every ref under `refs/` except three. **`.githooks/reference-transaction` refuses a hand-written stash from a linked worktree, and it lets an autostash store through.** **The first hook of #305 refused the autostash store too, and that refusal destroyed the work git had just saved.** `git merge --autostash`, `merge.autostash`, `rebase.autostash` and `git pull --rebase --autostash` each fall back to `git stash store` when the entry cannot re-apply. git removes the autostash file after the store, and it removes that file whether the store succeeded or failed. The refusal therefore left the saved state as an object id on one `error:` line. **The cross-member review of this batch measured that loss, and `4a30c33` repaired the hook inside the batch.** The ref name, the old value and the new value separate nothing, and `GIT_REFLOG_ACTION` is unset. The hook therefore reads the state file that git writes. `.githooks/reference-transaction:27-31` tests `MERGE_AUTOSTASH`, `rebase-merge/autostash` and `rebase-apply/autostash` under the git directory of the worktree. **The repaired hook was watched on five cases**, and `.claude/rules/worktrees.md` records the output of each one. **`.claude/rules/worktrees.md` records two limits of the repaired hook.** `:262` holds the one case that misses a refusal: a hand-written stash while a rebase autostash is pending exits 0. That case destroys nothing. `:245` holds one over-refusal: the hook refuses `git stash drop` on the entry it allowed. The `Dropped` line prints the object id first, so the entry stays reachable. The rule at `CLAUDE.md:115` binds every agent whether or not the hook runs. **The hook is inert until the maintainer runs `git config core.hooksPath .githooks`.** The `## Terms` table above gains the row `autostash`. **#300 and #289 record a reading each, and both stay open for the maintainer.** #300 asks whether a HelloRetryRequest is a ServerHello for JA4S, and #289 asks the shared cause of the 83 surplus raw and original-order keys. Neither one writes a register entry, and neither one is shipped work. **#326 stays open, and this round repairs no part of it.** The `State it holds` cell at `docs/specs/spec.md:474` names a `results` field, and `ja4ssh.go:61-67` declares `connections`, `packetCount` and `arrivals`. **`make conformance` exits 2 on this branch, and Epic 5 owns that exit.** `conformance_test.go:873` reports the 680 deviations that the register does not hold. **This round writes no register entry, and `testdata/deviations.json` gains no entry.** **The port needs no change**, because the batch moves no fingerprint value and no exported name. Recorded by #321. |
| 35 | 2026-08-12 | **Batch #331 changes no fingerprinter, and it moves no fingerprint value.** **The four counts read 1623 matches, 680 deviations, 409 accepted deviations and 409 register keys before the batch and after it.** The run reports 0 stale register entries and 0 orphan register entries. **The batch merged four members and one repair.** #261, #328, #254 and #260 are the members, and `db52e4a` repairs the four findings of the cross-member review. **#328 builds the orphan half that #307 left open, and the `## Terms` table above gains the row `orphan entry` at line 130.** The stale check of #307 visits the keys of one comparison alone, so a register key that no capture produces falls outside every branch of it. `conformanceComparison.Reached` now names every register key that one comparison visits, and `conformanceOrphanEntries` subtracts the reached keys of the whole run from the register. That second pass runs after the last capture, because one comparison reads one capture and one vector set. An orphan entry fails the suite, as a stale entry does. `docs/audit/conformance.md` gains the summary row `Orphan register entries` and the section `## Orphan register entries`, and that section states the result of every run. **`docs/specs/features/04-conformance-harness.md:126-138` numbers the check as FR-conformance-33h through FR-conformance-33o**, and #328 carries the requirements from its first commit rather than after a review. **The check was watched failing.** The worker added the temporary entry `tls12.pcap/1/JA4ORPHANWATCH`, ran the suite, and removed the entry, and the run reported `the run reaches 409 register keys and reports 1 orphan register entries`. `conformance_test.go:931` writes that line on the merged tree. **`Accepted == 409 == \|register\|` was the whole proof that no register entry had drifted, and #307 and #328 together replace it with two named checks.** That identity fails the moment one batch adds an entry and removes another in one commit, and `changelog_counts_freshness_test.go:135-137` already concedes the asymmetry. **#254 changes no citation, and that is the right answer.** The issue asks for a `reference/` prefix on every citation of `docs/specs/foxio/`. The measurement proves that prefix wrong, and the member measured before it wrote one line. **`make corpus` writes two corpus roots, and not one.** `scripts/fetch-corpus.sh:149` moves `pcap`, `python/test/testdata` and `wireshark/test/testdata` out of the staged tree, and `scripts/fetch-corpus.sh:167` then writes the rest to `testdata/foxio/reference/`. Four citations of the nine method pages name `python/test/testdata/browsers-x509.pcapng.json`, which `make corpus` writes under `testdata/foxio/python/`, so a blanket prefix breaks a citation that a reader can follow today. **The short citation form is also a repository-wide convention, and a Go change alone moves it.** `.claude/rules/rulings.md` states the form, `ja4l.go:141`, `ja4t.go:132`, `ja4ssh.go:158` and `ja4x_raw_test.go:101` carry it, and five assertions of `foxio_transcription_tcp_test.go` anchor on it at `:230-232` and `:251-252`. A first draft repointed the four vector citations, and `TestTLSTranscriptionRulesThatNameAnImplementationCarryAFileAndALine` then failed. **The member states the bases once instead**, at `docs/specs/foxio/README.md:31-49`, and the nine method pages and `zeek.md` each carry one worked example. **The member repairs two false statements as well.** `zeek.md` stated that a path is relative to the `zeek/` directory of the FoxIO repository, while every citation on the page already carries that prefix. `README.md` stated that the script fetches no part of `technical_details/`, and `testdata/foxio/reference/technical_details/` holds each file of the inventory table at `docs/specs/foxio/README.md:166`. **#254 stated its rule on a page whose own citations break it, and the cross-member review of this batch found that.** The rule named two bases, and the tree holds seven, so it sent a reader of `docs/specs/foxio/zeek.md:76` to a path that no tree holds. **That is the silent failure #254 exists to stop, on the page that states the rule.** **`db52e4a` repairs it inside the batch, and `docs/specs/foxio/README.md:41-49` now holds one table of seven bases.** The page states no count of pages, because a count of pages goes stale at the next edit, and that is how the defect arose. **Base 2 is evidence that no member named.** A page cites a bare image name such as `JA4D.png`, and that file sits at `testdata/foxio/reference/technical_details/JA4D.png`. The FoxIO repository root holds no file of that name, so base 1 reaches it at no path. `docs/specs/foxio/README.md:110` counts 27 spans at base 2. **`docs/specs/foxio/README.md:96-131` records the measurement of the repaired rule.** A resolver read the thirteen pages of the directory and 822 path-shaped code spans. 621 resolve at base 1, 15 at the short form of base 1, 27 at base 2, 26 at base 3, 19 at base 4, 83 at base 5, 10 at base 6, 18 at base 7, and 3 under no base. **No citation names a line past the end of the file it names.** **One citation resolves under no base, and the page reports it rather than repointing it**: `ja4l.py`, at `docs/specs/foxio/port-register.md:80`. The FoxIO `python/` directory at the pin holds `common.py`, `ja4.py`, `ja4h.py`, `ja4ssh.py` and `ja4x.py`, and no `ja4l.py`. **The repair changes `README.md` alone, so #254 still changes no citation.** **No test holds the repair**, because the acceptance criteria of #254 bar a Go file change, and #335 records the guard. **#261 adds four rows to the `## Terms` table above, at lines 122 to 125**: `maintainer`, `project manager`, `schema violation` and `delegated ruling`. **The issue names three of them.** The boundary sentence of the `delegated ruling` meaning uses `schema violation`, `.claude/rules/ste.md` bars a domain word the table does not hold, and the project manager accepted the fourth row for that reason. Every row records a meaning that `.claude/rules/rulings.md` or `.claude/rules/parity.md` already fixed, and the member invents none. **#260 adds `.claude/rules/cross-member-review.md`, one bullet at `CLAUDE.md:119` and six rows at lines 192 to 197 of the `## Terms` table above.** The six are `batch`, `member`, `cross-member review`, `mailbox`, `idle signal` and `spawn path`, and they append at the end of the table, so they touch no row that #261 wrote. **A cross-member review returned an idle signal and no text in eight consecutive attempts, across session 5 and session 6.** **The worker of #260 spawned three agents on 2026-08-12 to separate the cause.** Arm A carried no `name` and the review brief, and it reached the in-process path. Arm B carried a `name` and the review brief, and it reached the cross-session path. Arm C carried a `name`, `isolation` set to `"worktree"` and a one-command brief, and it reached the in-process path. Arm A and arm C each reported to the spawner without a request, and arm B reported to the project manager and never to the spawner. **Arm A against arm B is the one contrast of the measurement that varies a single parameter**, because arm C moves `isolation` and the brief together. **Arm B and arm C both carry a name and reach different paths, so the name alone does not decide the path.** That conclusion reads the path of each arm, and it reads no duration. The session-6 record names the `name` parameter as the cause, and that names the wrong variable. **The cross-member review of this batch found that the first draft read as a one-parameter measurement, and `db52e4a` repairs it inside the batch.** `.claude/rules/cross-member-review.md:47-51` gains a `Brief` column, and `:53-55` states that the two durations measure the two briefs and nothing about the spawn path. **The file records three open questions at `:103-116`, and the third is a fourth arm that nobody has run.** That arm sets `isolation` and carries the review brief, and it separates the two variables that arm C holds together. **The rule states the discriminator that survives a harness change**: the in-process path names an output file at the spawn, and the cross-session path names a mailbox. **The review also repaired two of the six `## Terms` rows.** The `cross-member review` row stated that the rule file names the term in full at the head of each section, and the file names it in full at one heading of eleven. The `mailbox` row and the `spawn path` row each declined `channel`, so a reader could not tell whether the word is permitted, and the `mailbox` row now keeps the decline alone. **This row is the dated `## Changelog` row that the body of #261 asks for, and no member of this batch wrote one.** **A member that writes its own round produces the defect #278 records**, so the batch documentation owns the round. **This round describes the tree that its own commit produces.** The branch merges `batch/331-integrity` at `db52e4a` first, and the commit then writes this row and the four `## [Unreleased]` entries of `CHANGELOG.md` and nothing else. **The tree moved under this round once**, because `db52e4a` landed after the first draft, and the round was rewritten rather than left to describe a tree that no longer stands. **`CHANGELOG.md:10-15` keeps its four counts without a change**, because the batch moves none of them, and `changelog_counts_freshness_test.go` holds that paragraph true. **`make conformance` exits 2 on this branch, and Epic 5 owns that exit.** `conformance_test.go:935` writes `the run reports %d deviations that the register does not hold, and Epic 5 closes them`, and the run of this commit reports 680 of them. **Round 34 above cites that failure at `conformance_test.go:873`, and that citation is false.** `conformance_test.go:873` at the fork point `57dcb24` reads `totals.Stream.Matches, totals.Stream.Deviations, totals.Stream.AcceptedDeviants`, which is the second line of a summary call. **`conformanceReportTotals` calls `t.Helper()` at `conformance_test.go:910`, so the run attributes every one of its lines to the caller at `conformance_test.go:680`.** A citation of a `t.Helper()` function therefore reads two ways, and this round cites the statement rather than the output prefix. **This round writes no register entry**, and `testdata/deviations.json` gains none. **The port needs no change**, because the batch moves no fingerprint value and no exported name. Recorded by #331. |
| 36 | 2026-08-12 | **Batch #342 changes no fingerprinter, and it moves no fingerprint value.** **The four counts read 1623 matches, 680 deviations, 409 accepted deviations and 409 register keys before the batch and after it.** The run reports 0 stale register entries and 0 orphan register entries. **The batch merged four members and one repair.** #334, #335, #319 and #255 are the members, and `c5e2aad` repairs the two findings of the cross-member review. **#334 makes the users table cite a `## Terms` row rather than restate it.** The `Maintainer` cell at `docs/specs/spec.md:262` reads `The person that row maintainer of ## Terms names.`, and it stated a second meaning before. `.claude/rules/ste.md` rule 6 gives one word one meaning, and the `maintainer` row at `docs/specs/spec.md:122` bars `owner`, which the old cell used. **The member checked all 4 rows of the users table against the 100 rows of the `## Terms` table at `docs/specs/spec.md:98-197`.** **It found a second collision on the word `monitor`, and it repaired nothing there.** The `monitor` row at `docs/specs/spec.md:171` names the `ja4plus watch` command, and four sites name a program the caller writes: `docs/specs/spec.md:76`, `docs/specs/spec.md:253`, `docs/specs/spec.md:259` and `docs/specs/features/06-fuzz-testing.md:12`. The collision reaches three sections and a second file, so it sits outside the table #334 owns. **#344 holds it, and it is open.** **#335 builds `foxio_citation_base_test.go`, which holds 849 lines and seven tests.** The file reads every path-shaped code span of `docs/specs/foxio/` against the seven bases of the table at `docs/specs/foxio/README.md:41-49`. `docs/specs/features/11-foxio-reference.md:106-120` numbers the check as FR-reference-18a through FR-reference-18l. **The shape check of FR-reference-18b needs no corpus, and no skip of the file is silent.** `foxio_citation_base_test.go:530` declares `foxioCorpusAbsentMessage`, and every skip writes that sentence. **The line-bound check of FR-reference-18h reads the file the citation names, and it fails a line past the end of it.** **A counted exception table holds two entries and three spans**, and each entry names one page, one span, one count and one reason. `docs/specs/foxio/README.md` carries 2 spans of `ja4l.py` and `docs/specs/foxio/port-register.md` carries 1. An exception that now resolves fails `TestTheCitationExceptionTableHoldsNoSpanThatNowResolves`. **The member watched four failure modes**: a citation under no base, that same citation with the corpus moved aside, a citation that names a line past the end of its file, and a stale exception count. **The line-bound check found a defect that no reader had seen.** `docs/specs/foxio/port-register.md:81` cites `.claude/rules/external-apis.md:95` and `.claude/rules/external-apis.md:101`. **This repository holds a file at that path, and it holds 62 lines**, so a resolver that reads base 5 before base 7 answers from the wrong repository and reports nothing. `docs/specs/foxio/README.md:90-91` states that the copy's citations name paths of the port, so base 7 outranks base 5 on that page. **No page changed, and the resolver reads the recorded order.** **#319 repairs two mockup values, two missing port citations and one wrong field citation.** `docs/specs/mockups/02-cli-output.html:59-60` now write `65535_2-1-3-1-1-8-4-0-0_16344_6` on the JA4T line and on the JA4TS line, which is the form ruling #297 delivers. **The sweep found a second dead value, and no ruling moved that one.** `docs/specs/mockups/03-watch-output.html:88` held `ge20cn10enus_9da5c4e0ba8f_e3b0c44298fc_e3b0c44298fc`. `e3b0c44298fc` is the truncated SHA-256 of the empty string, and `internal/parser/hash.go:8` writes `000000000000` for an empty list, **so the library reaches neither value**. The value also contradicts itself, because part a carries the cookie flag `c` and a cookie list that is not empty hashes to no empty value. `testdata/foxio/python/http1-with-cookies.pcapng.json` supplied the repair, and the line now writes `ge11cr04da00_8ddaef5d77af_280f366eaa04_c2fb0fe53442`. **A mockup value that a person composed by hand never matched the library**, so a later member checks a mockup value against a corpus vector and never against a ruling alone. **The port half of #297 is `Crank-Git/ja4plus#215`, and the port needs no change**, because `ja4plus/utils/tcp_options.py:51-56` already writes one entry for each option byte. **The port holds no issue for #285**, and `ja4h_raw_test.go:81-82` records that reading with the port lines that carry the three-field shape. **The field citation at `ja4t_option_byte_count_test.go:76-79` named the wrong field.** Frame 31 of `browsers-x509.pcapng` is a SYN-ACK, so the per-packet vector holds `ja4.ja4ts` and not `ja4.ja4t`. The comment now names frame 30 as the SYN of the same connection. **#319 found one more defect and repaired nothing.** `docs/specs/mockups/02-cli-output.html:56` shows `$ ja4plus testdata/foxio/pcap/tls12.pcap` and six fingerprint lines. `cmd/ja4plus/main.go:74` and `cmd/ja4plus/main.go:100` show that the program takes `ja4plus analyze <pcap-file>`, and `tls12.pcap` reaches one JA4 value alone, so no capture reproduces the example. **#350 holds it, and it is open.** **The body of #350 and the finding comment of this batch each name that line as `:57`, and `:57` is blank.** The second reader of this round measured the line, and the round writes `:56`. **A worker of #350 reads `:56`**, so it does not chase a blank line. **#255 ships one rule, and not eighteen edits.** `.claude/rules/rulings.md:35-49` holds `## A citation names its repository`, and `:37` states that a bare `#N` names an issue of this repository. **The rule adopts a form the tree already uses.** Measured at `c5e2aad` over the same file set, the tree writes `Crank-Git/ja4plus#N` 5 times, `port issue #N` 5 times, `` `Crank-Git/ja4plus` issue #N `` 3 times and `the port's issue #N` once. **Two pages carry a local rule, and the section names both.** The `Ruling` column of the register above names the port, and `docs/specs/spec.md:380` now states that. `docs/specs/foxio/port-register.md` is a verbatim copy of the port's specification, and a bare number in it names the port. **The member changed three citations.** `docs/specs/spec.md:446` now reads `This repository's #216`, and `ja4l_test.go:1272-1273` now names both halves of one question. **The JA4L part count is #127 here and issue #225 in the port**, and the member confirmed the port title `Decide the JA4L form: the part count, the protocol part and the duplicate server value` on 2026-08-12. **This batch broke the rule it added, and it repaired the breach inside itself.** #319 wrote a bare `#215` that names the port, in the same batch that #255 wrote the rule. **#215 of this repository is `The JA4L-C occurrences of one QUIC stream are shifted by one against the vector`**, which no part of the JA4T question reaches. `c5e2aad` repairs it: `ja4t_option_byte_count_test.go:28-32` now quotes the port docstring as a code span and it names the port's issue. **Read this batch as one that produced a defect and caught it, and never as a clean one.** **The cross-member review named the structural cause.** FR-reference-18a scopes the guard of #335 to `docs/specs/foxio/`, and every citation this batch wrote landed outside that directory. **#351 holds that gap, and it is open.** **The second review finding is a comment that named a guard which guarded nothing.** `foxio_citation_base_test.go:527-529` names `TestTheCISkipDetectorMatchesNoUntaggedSkipMessage`, and the extractor of `conformance_skip_marker_test.go` read a string literal alone. `foxioCorpusAbsentMessage` concatenates a package constant and two literals, so that guard read no skip of the file that names it. **`c5e2aad` makes the claim true rather than correct the comment.** The extractor now reads the skip argument through `go/ast`, and it resolves a literal, a constant of the package and a concatenation of the two. **A skip argument it cannot read now fails the test**, so the silent gap cannot return. **`.claude/rules/cross-member-review.md` gains category 6, and `CLAUDE.md` counts six categories with it.** Category 6 names an unsupported claim about the merged result, and category 3 keeps its one mechanism of a target that a member moved, renamed or never created. Both findings of this batch sat on a surface that exactly one member edited, so the rule also gains a sweep of every rule the batch adds against the whole merged tree. **A pull-request body is in scope**, because the batch round copies its measurement into this file. **Three counts of the body of pull request #349 fail a re-measurement, and this round writes the measured ones.** **Each count below names `c5e2aad` as its base, which is the merged batch before this commit.** This round writes both numbers into the tree, so a count measured after it counts itself. The grep reads `*.go`, `*.md`, `*.html`, `*.json` and `*.yml`, outside the untracked `testdata/foxio/`. **`#127` occurs 23 times, on 18 lines, in 8 files at `c5e2aad`.** The body states `18 bare #127 citations found`, and 18 is the count of lines that name this repository. **It is not the occurrence count.** The review reported that the digit should read 19, and the repairing member re-measured and reproduced 18 under that reading. **The second count is wrong in the number and in the location.** The body states `4 write Crank-Git/ja4plus#N in a table cell`. **`c5e2aad` holds 5 citations of that form, and 2 of the 5 sit in a table cell**: `CHANGELOG.md:250`, `CHANGELOG.md:341`, `docs/specs/spec.md:446`, `docs/specs/spec.md:447` and `ja4t_option_byte_count_test.go:28`, of which the two `docs/specs/spec.md` sites are the table cells. #255 measured before #319 landed the fifth. `.claude/rules/rulings.md:38` holds a sixth occurrence, which is the rule's own example of the form, and it names no issue. **The third count reproduces under no reading this round could build.** The body states `The tree holds 39 citations of an issue of the port` and `32 of them name the port in a sentence`. The four forms the body names total 14 at `c5e2aad`, and the round writes the four measured numbers above rather than a fourth digit of that body. **State the unit with every count, and name the base that produced it**, because a grep that counts lines and a grep that counts occurrences differ by 5 on this one number. **The batch also carries a ruling triage, which closed no code.** A reading of 2026-08-12 checked #125 through #130 against the tree at `2acf313`, the issue threads, the register, `testdata/deviations.json` and the Go tests. **All six are ruled, and none of the six holds an open question.** **Four are carried into the tree in full, and the project manager closed them**: #125, #127, #128 and #130. **Two are carried in part, and the maintainer holds them**: #126 and #129 each carry the label `status:needs-feedback`. The RST half of #126 reaches no line, and #56 owns that work. **Step 2 of #129, the TCP handshake originator, reaches no line.** Step 1 sits at `ja4ssh.go:125-133` and step 3 at `ja4ssh.go:136-145`, and no open issue owned step 2 before this batch. **#346 now owns it.** **#345 holds the second gap the triage found.** `.claude/rules/rulings.md` names three durable records for a ruling, and #126, #128, #129 and #130 reach none of the three. **The triage changed no code, and it removed six unbuildable issues from the ready pool.** **`CHANGELOG.md:10-15` keeps its four counts without a change**, because the batch moves none of them, and `changelog_counts_freshness_test.go` holds that paragraph true. **`make conformance` exits 2 on this branch, and Epic 5 owns that exit.** `conformance_test.go:935` writes `the run reports %d deviations that the register does not hold, and Epic 5 closes them`, and the run of this commit reports 680 of them. **The run attributes that line to `conformance_test.go:680`, because `conformanceReportTotals` calls `t.Helper()`.** Round 35 above states the rule, and this round cites the statement. **Three numbers have been written for that one line, and this round adds no fourth.** **This round describes the tree that its own commit produces.** The branch carries `c5e2aad` first, and the commit then writes this row and the four `## [Unreleased]` entries of `CHANGELOG.md` and nothing else. **This round writes no register entry**, and `testdata/deviations.json` gains none. **The port needs no change**, because the batch moves no fingerprint value and no exported name. Recorded by #342. |
| 37 | 2026-08-12 | **The maintainer ruled the ALPN condition, and the requirement adopted the printable ASCII range `0x20-0x7E`. The code did not move.** FR-parity-8 and FR-parity-9 each read `not alphanumeric`, and the register row `ALPN value for a first byte that is not alphanumeric` read the same condition. **The FoxIO measurement contradicts that test.** Both FoxIO implementations pass a printable ASCII byte through, whether or not that byte is alphanumeric, so `\x20\x61` reads ` a` in each one and `\x30\xab\xcd\x31` reads `01` in each one. The port ran both implementations at the pinned commit `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`, and `Crank-Git/ja4plus#141` holds the commands and the table. **Parity rule 1 gives the behavior to FoxIO, so the requirement was wrong and the code was right.** The maintainer ruled on 2026-08-12 that the requirement text and the register row adopt the range. FR-parity-8 now names the first byte and FR-parity-9 now names the last byte, and FR-parity-9 states that the field reads no byte between them. **The second register row adopts the same title**, because its old title read `a byte outside 0x20-0x7E in a position other than the first` and that wording also covered a middle byte. **FR-parity-10 keeps the alphanumeric test**, because the two implementations dispute every one-byte value, and a one-byte value outside the alphanumeric ranges writes `99`. **`Crank-Git/ja4plus#601` closes the same wording on the port side.** The port already implements the range at `ja4plus/fingerprinters/ja4.py:56-58`, and two of its register rows still read `not alphanumeric`. **The two implementations agree on behavior, and they differed in wording alone.** **R9 question 1 is closed, and the maintainer closed it on 2026-08-12.** The row stated that no FoxIO source states what an empty first ALPN value writes. `technical_details/JA4.md:93` does state it, and it gives `00`, which is the value `internal/parser/tls.go` already wrote. **That closure changes no behavior**, and `docs/specs/foxio/JA4.md` R17 records the reading. **The one fingerprint repair of #50 is the value the FoxIO vector holds.** `ALPNValue` wrote the first and last character of the hexadecimal form of the whole first ALPN value, which no FoxIO implementation writes. `docs/specs/foxio/JA4.md` R19 records that the prose rule computes `bd` for the first ALPN value `0xba 0xad` and that the vector holds `99`, and that no function of the bytes gives both. **Four fingerprint values moved, on one capture.** Each one is `tls-non-ascii-alpn.pcapng` stream 0, and the four are `JA4.1`, `JA4_o.1`, `JA4_r.1` and `JA4_ro.1`. **The four counts read 1623 matches, 680 deviations, 409 accepted deviations and 409 register keys before. They read 1627 matches, 676 deviations, 409 accepted deviations and 409 register keys after.** The register gains no entry and loses none, because the four values now match the vector. Measured on `issue/50-alpn-form` against the base `b489ed2` with the corpus present. **`docs/specs/foxio/JA4.md` R18 gains `wireshark/source/packet-ja4.c:552-554`, and the count of three results holds**, because the dissector agrees with the prose and with Zeek. That code sits in `ja4s_r` and renders JA4S, so R18 states that it corroborates the JA4 rule through the shared ALPN store at `wireshark/source/packet-ja4.c:1023-1034`. Reading 2 of that page states that the dissector registers no JA4 field. **The self-review of #50 corrected one line number of this round before it landed.** The empty-value sentence sits at `technical_details/JA4.md:93` and not at `:92`, which is a blank line. Line 93 holds two sentences, so R17 and R18 of `docs/specs/foxio/JA4.md` cite one line for two rules, and each rule names the sentence it reads. **This round repairs no part of R9 question 2**, which #279 owns. Recorded by #50. |
| 38 | 2026-08-13 | **Batch Epic 8b merges seven members and one repair, and it is the first batch of this project to close a requirement by changing a parser.** #58, #56, #57, #361, #126, #356 and #375 are the members, and one commit repairs the six findings of the cross-member review. **Thirty-one fingerprint values moved, and every one moved from a deviation to a match.** **The four counts read 1627 matches, 676 deviations, 409 accepted deviations and 409 register keys before the batch. They read 1658 matches, 645 deviations, 409 accepted deviations and 449 register keys after it.** The run reports 202 unaccepted uncovered values, 40 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **The arithmetic is exact and it is evidence: `1627 + 1 + 30 = 1658`, where #56 moved one value and #57 moved thirty.** The two changes are independent and neither disturbed the other. **The maintainer made seven rulings on 2026-08-13, and one of them governs the other six.** **A ruling that the port's divergence register already records lands in this repository as a reading, and not as a fresh ruling.** The reason is `.claude/rules/parity.md`, which states that a ruling lands in both repositories or in neither: the port half of each of these questions landed on 2026-08-07 or 2026-08-08, and the Go half never did. **That asymmetry is the one the parity rules exist to prevent.** Each such row of the port's register ends with a sentence of the form `The port must ... or the two implementations disagree`. A worker cites the port register row, the port issue number and a file and a line, implements to match, and records the citation in a test comment. **The boundary holds without a change: a split that the port's register does not record still reaches the maintainer**, and #369 is one such question that this batch raised and did not settle. **Six rows of Epic 8b are settled by that one ruling**: the RST flag combination, the RST that carries no delay, the client RST, the three JA4X values of the SOCKS4 tunnel, the JA4H method code, the repeated DHCP option 57 and the DHCPv6 relay type. **#361 answer 1: the conformance run compares a value whose reference file publishes no key for its method, and it reports that value as an uncovered value.** An uncovered value is neither a match nor a deviation, and the register may hold a decline for it. **That ruling changes the form of an invariant this project has stated since #42.** `Accepted == \|register\|` read 409 == 409, and it now reads **`accepted comparisons == \|register\|`**, at 449 == 449, where 449 is 409 accepted deviations plus 40 accepted uncovered values. **The change is a consequence of the ruling and not a second decision**: an accepted uncovered value names one register entry and it is not a deviation, so it cannot be counted as an accepted deviation. `docs/specs/features/04-conformance-harness.md` records the new form as FR-conformance-33v, and FR-conformance-33p through FR-conformance-33u state the category. **The JA4TS part e delay rounds to the nearest second, half away from zero**, which is the Wireshark reading over the split that `docs/specs/foxio/JA4T.md` records as R24 and R25. **The port already ships that rule and its register does not record it**, at `ja4plus/fingerprinters/ja4ts.py` of tag `v1.1.0`, so a truncation here would have moved a shipped port value. **`Crank-Git/ja4plus#602` asks the port to record the row.** **JA4T reads any SYN whose ACK bit is clear**, which is the Rust reading over the split that R29 records. **`ja4t.go:32` already carried that rule**, because `gopacket` exposes each TCP flag as its own boolean, so #126 built two tests and changed no behavior. **`Crank-Git/ja4plus#603` asks the port to record that row.** **The header block terminator matches one line ending followed by another**, and `Crank-Git/ja4plus#604` carries the port half; #298 holds the Go half and this batch built neither, because a ruling lands in both repositories together. **`generate_ja4l` and `generate_ja4ssh` are recorded as `not applicable`**, and this library exports no `ComputeJA4L` and no `ComputeJA4SSH`. `JA4LFingerprinter.ProcessPacket` and `JA4SSHFingerprinter.ProcessPacket` are the Go form of a multi-packet method, and an exported one-shot function would export connection state that `.claude/rules/concurrency.md` keeps unexported. **The freeze at `v1.0.0` makes that decision permanent, so #356 is the last chance to choose it.** **#129 needed no ruling, because the maintainer had already ruled it on 2026-08-11**, and the label alone was stale; the project manager closed it. **#57 found two false premises, and each one was a real defect that a locate pass had reported as met.** **FR-parity-47 was reported met and it was not.** `internal/parser/http.go:22` held a closed list of nine method names, so a `PROPFIND` request, a `MKCOL` request, a `PROPPATCH` request, a `REPORT` request and an `UNLOCK` request each produced **no JA4H value at all**. The repair reads the method token character set that RFC 9110 section 5.6.2 names, and it bounds the match at 8192 bytes, which is the value `ja4plus/utils/http_utils.py:182` of the port holds. **The bound matters beyond JA4H, because `ja4l.go:270` reads `IsHTTPRequest`**, so a payload that function admits by mistake moves a JA4L measurement point. **FR-parity-50 was framed as a test to write, and the library produced zero JA4X values on `socks4-https.pcap` where the port produces three.** Two defects that name no tunnel caused it: a concatenation in arrival order, and a scan that read the first handshake message of a TLS record alone. `parser.TCPStreamReassembler` is the mechanism `ja4h.go:18` already uses, so JA4X now reads one reassembler and never a second mechanism. **The self-review of #57 found that the swap had silently removed the per-stream byte cap**, because the reassembler bounds the run it returns and bounds no stored segment, and `streamBytes` restores it. **The project manager wrote three wrong counts into three briefs, and a worker measured rather than adopted every one of them.** **The first: the #361 brief and the batch cross-check each state that #361 writes five register entries and raises the key count to 414. The true rise is 28, to 437.** Five is the port's key form `<capture>/<method>`, and this repository keys an entry `<capture>/<stream>/<method>`; `deviations_test.go:183` and FR-reference-21 reject a two-part key, so five port rows cover 28 comparisons here. **Writing five was impossible, and not merely different.** **The second: the #375 issue body states six comparisons for the three FR-parity-50 values, and the true number is twelve**, because the run compares `JA4X` and `JA4X_r` in the per-stream set and in the per-packet set. **The third: the #126 plan states that no capture of the corpus reaches an ECN-marked SYN, and 2 of 38 captures carry one across 4 packets** — `gre-sample.pcap` frames 11 and 12, and `macos_tcp_flags.pcap` frames 1 and 2. **Read those three together as the case for the instruction that every brief now carries**, which asks a worker to probe each premise and to report where it found the brief wrong. **The ruling comment of #126 also cited `rust/ja4/src/tcp.rs:153` for an assertion that sits at `:154`**, and `:153` holds the comment above it; #126 carries the correction. **#375 declined to write `#138` into the `ruling` field of twelve register entries, and that decline is right.** `deviations_test.go:40` declares `^#[1-9][0-9]*$`, and `.claude/rules/rulings.md` states that a bare `#N` names an issue of this repository, where #138 is an unrelated issue. The field holds `#375`, and each `reason` names `Crank-Git/ja4plus#138` in full. **The cross-member review found five defects, and the project manager found a sixth that the review listed as unchecked. It is now ten for ten.** **Finding 1: `conformance_report_test.go` states the counts 192 and 220, which were #361's numbers on its own branch**, and #57 and #375 moved them to 202 and 242. **Finding 2: `ja4ts_part_e_test.go` cites `docs/specs/foxio/port-register.md:87` and `:89` for the two rows #56 adopted**, and #58 inserted 25 lines above the copy, so those rows sit at `:112` and `:114` and line 87 is now the table header row. **Finding 3: `conformance_adapters_test.go` states the harness rule that #361 reversed**, in the doc comment of the `Covered` field, and no member edited that file. **Finding 4: the fingerprinter state table of this document named `streams`, a field #57 deleted.** **Finding 5: `renderUncovered` claimed FR-conformance-33r, which names the summary and which `renderSummary` holds.** **Finding 6, which the project manager found: `docs/specs/spec.html` read as pure future work for JA4T and JA4TS while this document read `Closed by #56`.** #57 and #375 each updated both files and #56 updated one, so the two documents disagreed. **`docs/specs/spec.html` is hand-written and no generator produces it**, and this is the third consecutive batch in which it carried a stale sentence. **Four of the six findings sat on a surface that exactly one member edited**, which is the shape `.claude/rules/cross-member-review.md` already records. **The batch filed eight issues that it did not build.** #369 holds two JA4TS reference splits past the ten-delay cap, where Zeek writes ten delays and Wireshark writes nine, and it is the maintainer's. #370 holds the DHCPv6 relay inner message, which #57 measured as three parts wide rather than the one part its brief named. #371 holds the JA4D FQDN flag, which this library sets on the presence of option 81 where the port requires a name. #373, #378 and #379 each repair a sentence that this batch made false or left false. #376 holds the reading of why the per-packet vectors publish no JA4L key for two captures, which #361 declined to register because `python/ja4.py:340` states nothing about the Wireshark generator. #375 was filed by the batch and built by it. **`make conformance` exits 2 on this branch, and Epic 5 owns that exit.** `conformance_test.go` writes `the run reports %d deviations that the register does not hold, and Epic 5 closes them`, and the run of this commit reports 645 of them, where the fork point reported 676. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is therefore unmeetable, and every verdict of this batch says so rather than reporting a green gate.** **This round describes the tree that its own commit produces.** The branch carries the repair commit first, and the commit then writes this row and the `## [Unreleased]` entries of `CHANGELOG.md` and nothing else. Recorded by the batch of Epic 8b. |
| 39 | 2026-08-13 | **Batch Epic 12 merges four members and one repair, and it implements the eleventh method without changing one line of a fingerprinter.** #60, #61, #62 and #63 are the members, and one commit repairs the eleven findings of the cross-member review. **No fingerprint value moved.** The counts read 1658 matches, 645 deviations, 409 accepted deviations, 449 register keys, 202 unaccepted uncovered values, 40 accepted uncovered values, 0 stale register entries and 0 orphan register entries, before the batch and after it. **`accepted comparisons == \|register\|` holds at 449.** **The reason the epic changes no fingerprinter is that the library already emitted the value.** `ja4l.go:183` and `ja4l.go:381` each write a `JA4L-S=` label, and they did so before this epic. **What was missing was a type token, and the maintainer ruled that the token must not exist.** **The maintainer made five rulings on 2026-08-13, and one of them reverses a requirement that this project wrote.** **A JA4LS result keeps `Type: "ja4l"`.** FR-ja4ls-1 asked for `ja4ls`, and the `## Behaviour rules` of the same feature file already stated that JA4LS gets no type of its own, because a second type would split one connection across two owners. **The port agrees with the code**: `ja4plus/processor.py` registers ten typed fingerprinters with `("ja4l", JA4LFingerprinter)`, and the port's `docs/methods/ja4ls.md` tells a reader to read the `JA4L-S=` prefix. **The conformance harness already recovered `JA4LS` from that label**, at `conformance_test.go:347-362`. **`Type` is a field of the frozen exported surface, so emitting `ja4ls` would have frozen a disagreement with the port at `v1.0.0`.** The ruling is recorded at issue #60, comment 5275948279. **`ja4ls` becomes an extra `--types` token, and `ja4l` still selects both.** That closes R9 question 3 of this document, which asked whether `--types ja4l` alone prints the JA4LS value. **The token is a superset over the port and not a divergence**, because the values a caller receives do not change and `ja4l` keeps selecting both. **The filter reads the `JA4L-S=` label rather than the type**, because a JA4LS result carries `Type: "ja4l"`. `Crank-Git/ja4plus#605` proposes the same token for the port, so the two converge rather than drift. **FR-ja4ls-7 and FR-ja4ls-8 adopt the unanimous FoxIO reading, and the code moves not at all.** FR-ja4ls-7 asked for a hop count, and `docs/specs/foxio/JA4L.md` R13 states that part b holds the value the packet carries and that no implementation subtracts it from an initial time-to-live, while R18 states that no reference implementation writes an estimated hop count into a JA4L value. FR-ja4ls-8 asked for a propagation factor, and R6 states that four implementations divide by 2 while R22 states that no reference implementation computes a distance. **`.claude/rules/parity.md` `## Where a difference comes from` row 1 governs both**: the reference is unanimous, the code already agrees with it, and the requirement is what differs. **Both amendments are provisional and each names #60 as its reversal path.** **FR-ja4ls-11 is amended because `Processor` holds no type filter at all.** The requirement asked that `Processor` accept `ja4ls` in a type filter, and the one filter of this repository lives in `cmd/ja4plus/main.go`. **A filter on `Processor` would add an exported method that `v1.0.0` freezes**, and `sync_processor_test.go:59` would have required a matching wrapper. The amendment is provisional and it names #61. **FR-ja4ls-22 names the per-stream set alone, and that is a scope decision of the project manager rather than a ruling.** The requirement asked that an uncovered JA4LS value carry a register entry, and #376 states `Write no register entry until the cause is read.` **Both cannot hold.** The per-stream set is explained, because `python/ja4.py:340` deletes every JA4L key when the run names another method, and ruling #361 registered 24 of them on 2026-08-13. **The per-packet residue stays with #376**, which is open. **The project manager declined the answer that would decline a value whose cause nobody has read**, because `.claude/rules/rulings.md` bars it and #376 says so in the same words. **#61 closed a defect that no requirement named.** `--types` validated no token: a token that named no method printed an empty table and exited 0, so a caller who misspelled one read the absence of output as the absence of fingerprints. It now names every valid token and exits 1, following `ja4plus/cli.py:82-92` of the port. **That is a user-visible behavior change, and `CHANGELOG.md` records it under `### Changed`.** **#62 built the test that FR-ja4ls-18 has required since round 3 and that `.claude/skills/release/SKILL.md:34` already cited as though it existed.** `TestMethodCount` reads every tracked document, it names the file and the line of each violation, and **it reported nine on the fork point**: `README.md:3`, `docs/specs/spec.md:224`, `docs/specs/spec.html:200`, `docs/specs/features/00-foundation.md:167`, `docs/specs/features/01-licensing.md:47`, `docs/specs/features/12-ja4ls.md:86` and `:174`, `docs/specs/features/15-mutation-sweep.md:161` and `.claude/skills/release/SKILL.md:34`. **`README.md:3` read `Implements ten JA4+ methods`, which is the exact form `CLAUDE.md` names as wrong, in the first paragraph of the file a reader meets first.** **The adversarial review of #62 found the first exemption design blocking, and the member repaired it inside its own pull request.** That design paired each delimiter with the next one, so a stray backtick anywhere on a line hid a real violation. **The second design reads exactly two bytes**, the one before the phrase and the one after it, and `TestMethodCountScanReportsAFormThatAStrayDelimiterPrecedes` holds the repair. **A scanner that a stray character defeats is worse than no scanner**, because it reports zero and a reader believes it. **`NOTICE` names ten methods where it named nine**, and a chain of six records moved with it, including two test names that became false the moment JA4LS joined the list. **#63 proved that every published JA4LS value reaches a comparison**: 102 of them, 58 per-stream under the vector key `JA4L-S` and 44 per-packet under the field `ja4.ja4ls`, and **no vector file anywhere holds the string `JA4LS`**. **Each of its guards reads one vector file twice**, the published count from the raw JSON and the compared count from the adapter, so the test cannot pass against a broken adapter. **The cross-member review found eleven defects, and it is eleven for eleven batches.** **Six of the eleven are the records #60 reported and did not own**, and a seventh is the hand-written HTML twin of one of them that #60 did not find. `docs/specs/features/12-ja4ls.md` `## Purpose` stated `This project does not implement it` and cited `ja4l.go:198`, which holds `return nil, nil`; `docs/specs/spec.html:169` held the same citation; `docs/specs/spec.md:472` asserted that the fingerprinter writes `ja4ls`; `docs/specs/spec.md:771` held the exit criterion the batch's own ruling contradicts; the `## Terms` row of `docs/specs/spec.md:155` defined the propagation factor as the JA4L divisor, which FR-ja4ls-8 now denies; and two hop-count records of the feature file survived the FR-ja4ls-7 amendment in the same file. **The eighth is a duplicated rule.** `concurrency_doc_test.go` held a second method-count check with a narrower scope and no quotation exemption, so a page that legitimately quotes the forbidden form passed the new scanner and failed the old one, with no line number. **`.claude/rules/ste.md` rule 6 gives one word one meaning, and two checks gave one rule two meanings.** The repair removes the older check and records what it read that the newer one does not: the whole text of a Go file, including a string literal, where the newer one masks a Go file to its comments through `go/parser`. **The ninth is a citation that a sibling moved twice.** `ja4ls_emission_test.go` cited `12-ja4ls.md:143-144`, the rule sat at `:156` when #60 wrote the citation, and #63 then inserted 24 lines above it. **The repair cites the section rather than the line.** **The tenth is a behavior change with no changelog entry**, which FR-foundation-21 requires. **The eleventh is a promise the feature file made and no member kept**: it stated that Epic 12 updates `mockups/02-cli-output.html` to hold a `ja4ls` row, and the mockup held none. **The repair adds the row.** **The review also read the scanner adversarially and reported it sound.** It dumped every match the scanner makes, exempt or not, found exactly three and all three genuine quotations, and it confirmed that no real violation is accidentally exempted and no legitimate quotation passes by luck. **It reproduced the count of nine on the fork point independently.** **One repair of this batch was itself caught by the linter.** A Go comment quoted a Markdown heading exactly, and that heading spells a word the British way where the maintainer writes US English. **The repair cites the section by name instead, and #388 holds the sweep**, which the measurement puts at 25 files. **`misspell` reads Go files alone, so no check reaches a document.** **A second defect of the gate is recorded here because it is silent.** The project manager measured `make conformance` in a worktree that `git worktree add` created, and it **exited 0 and printed no summary**. That is the suite skipping for want of a corpus, and not a pass. **`.worktreeinclude` copies the corpus into a worktree the harness creates, and a plain `git worktree add` copies nothing.** The measurement was retaken with the corpus linked, and it reports the numbers this round states. **Read an exit code of 0 from `make conformance` as a skip until the summary line is read.** **Four questions left the maintainer's desk on the same night, and the readings that emptied it are recorded on their issues.** #289 and #300 closed as readings and needed no ruling. #346 applied a ruling the maintainer had already made on 2026-08-11 and that #53 closed without carrying. #369 question 1 is settled by a FoxIO text specification, and question 2 is parked as a genuine reference split. **#387 is new, and it is the one question that neither reading could settle alone.** **`make conformance` exits 2 on this branch, and Epic 5 owns that exit.** The run reports 645 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is therefore unmeetable, and every verdict of this batch says so rather than reporting a green gate.** **This round describes the tree that its own commit produces.** Recorded by the batch of Epic 12. |
| 40 | 2026-08-13 | **Batch #391 merges four documentation repairs and one gate repair, and it moves no fingerprint value.** #373, #378, #379 and #386 are the members. **The counts read 1658 matches, 645 deviations, 409 accepted deviations, 449 register keys, 202 unaccepted uncovered values and 40 accepted uncovered values before the batch and after it.** **Each member repairs a sentence that session 9 made false or left false**, and each was held out of the Epic 8b batch and the Epic 12 batch on purpose, because each one touched a page a live member was editing. **#373 amended FR-ja4ls-10 so that it stops naming `ComputeJA4L` as an existing function, and it declined to withdraw the requirement.** Withdrawing it answers open question 2 of `docs/specs/features/12-ja4ls.md`, which the maintainer owns. **#378 repaired the JA4X count sentence of `docs/parity.md`, and it swept the whole page rather than the one sentence.** The page states seven counts about its own contents, the member verified each one by command, and **exactly one was false**. It added `parity_ja4x_name_count_test.go` so the sentence cannot drift again, written first and watched failing. **#379 added two rows to the `## Terms` table**, `one-shot function` and `multi-packet method`, and it declined the term the brief named. The brief asked for `multi-packet state`, and **the state names a state rather than a method**, which `docs/parity.md` and round 38 already write as a method. **#386 repaired R18 of `docs/specs/foxio/JA4T.md`, which stated one count of ten and cited two implementations that write ten and nine.** It states both counts now, it states that Zeek bounds the list of delays and Wireshark bounds the array of timestamps, and it gives the arithmetic so a reader reproduces nine and ten without reading the C. **#386 declined the word the acceptance criteria used.** The criteria said `delay vector`, and the `## Terms` table reserves `vector` for one FoxIO capture and its expected-output file. **The member kept `vector` inside a verbatim code span alone**, where it names the Zeek type. **The cross-member review found six defects, and it is twelve for twelve batches.** **The serious one is a citation of a rule that the tree does not hold.** R18 read `#369 applied ten delays ... under the delegation that `.claude/rules/rulings.md` states`, and that file grants no such delegation: its condition 1 requires a source of rank 1 or rank 2 and the deciding source ranks at 3, and its condition 2 requires every FoxIO implementation to enforce the rule while Wireshark departs. **The maintainer widened the delegation on 2026-08-13 and recorded the grant on issue #108, and the tree states the widening nowhere.** **The ruling was right and the citation was wrong.** The repair cites the grant, and **#398 lands the widening in `.claude/rules/rulings.md`**, so a later reader finds the rule in the tree. **The second is a rule the tree breaks in eleven places, on surfaces no member edited.** The `one-shot function` row declines `convenience function`, and eight exported functions and three other sites write it, **including `ja4t.go:174`, which is the function the new row cites as its own exemplar**. **The repair files #399 rather than sweeping**, because three of the eleven need a judgment rather than a substitution: `ParseKEXINITFromPacket` computes no fingerprint, so the generic English sense may be the right one there. **The third is two guards that forbid one thing with two reversal instructions**, which is the shape the review of batch #59 found and round 39 records. `parity_one_shot_not_applicable_test.go` holds the ruling of #356 and `terms_one_shot_function_test.go` derives its list from the `## Terms` row, and both fired on `ComputeJA4L` with contradictory advice. **The repair narrows the second guard to the one name that #356 never reached.** #356 named `generate_ja4l` and `generate_ja4ssh`, so it named JA4L and JA4SSH and it named no JA4LS. **A probe proved the split**: `ComputeJA4L` and `ComputeJA4SSH` now fail the #356 guard alone, and `ComputeJA4LS` fails the Terms guard alone. **One name, one home, one reversal instruction.** **A standing tension is recorded and not resolved.** FR-ja4ls-10 promises `ComputeJA4LS`, and `terms_one_shot_function_test.go` makes that name a red build. **The ruling of #356 never named JA4LS**, and open question 2 asks whether to add it. **The tree now states the tension in both places rather than in neither**, and the maintainer answers it by changing the `multi-packet method` row and that test. **Five defects of this batch were introduced by the project manager, and a worker caught every one.** The `crossCheck` URL of all four briefs returned 404, because the project manager printed a confirmation instead of capturing the URL the forge returned and then wrote an identifier from memory. A brief cited `docs/parity.md:21` where the row sits at `:23`. A brief cited `wireshark/source/packet-ja4.c:687` where the loop head sits at `:686`. A brief named a word the `## Terms` table declines. **A correction message stated that #373 had sub-merged before its gate had run**, and the worker of #379 read `git rev-parse` and found the fork point unchanged. **Each worker reported the defect rather than working around it in silence**, which is why each one is recorded here. **The rules these earn: capture the URL the forge returns rather than construct one, and never state an action as done before its gate has run.** **A defect of the gate is recorded a second time, because it recurred.** The project manager measured `make conformance` in a worktree that `git worktree add` created, and the suite skipped for want of a corpus and exited 0. **A symlink of the corpus into such a worktree then breaks `method_count_test.go`**, which reads `testdata/foxio` as a file. **The scanner never meets that shape in a real checkout**, where the corpus is a directory, so this is a property of the workaround and not of the tree. **Copy the corpus, or run the suite from the main checkout.** **`make conformance` exits 2 on this branch, and Epic 5 owns that exit.** The run reports 645 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this batch reports no green gate.** **This round describes the tree that its own commit produces.** Recorded by batch #391. |
| 41 | 2026-08-13 | **Batch #402 merges nine members and one repair, and it moves no fingerprint value.** #398, #395, #397, #387, #399, #346, #355, #409 and #410 are the members. **#398 landed the widened delegation of 2026-08-13 in `.claude/rules/rulings.md`.** **#395 repaired the ten-delay citation of the JA4TS part e test comment, and it named #369 in the reset-delay test.** **#397 repaired the one-shot note of `README.md`**, which named two multi-packet methods where three exist. **#387 wrote the maintainer ruling of 2026-08-13 into the register as five entries.** **#399 wrote `one-shot function` on the fourteen sites that carried a declined synonym.** They are seven exported one-shot doc comments, `internal/parser/ssh.go`, three test comments, one test name and two sites of `docs/audit/findings.md`. **It added `terms_one_shot_function_test.go`, which reads the `Do not use` column of the `## Terms` table.** **#346 recorded the four readings of the JA4SSH client direction, and the delegated ruling that #413 builds.** **#355 added the `git stash` deny entry to `.claude/settings.json`, with the guard that holds it.** **#409 extended ruling #387 to the five hashed JA4S keys, as five more register entries.** **#410 repaired R18 of `docs/specs/foxio/JA4T.md`, which reported a silence that #398 ended.** **This batch carries three rulings, and the maintainer made two of them.** **The maintainer ruled #387 on 2026-08-13.** The library keeps two JA4S values on a stream that carries a HelloRetryRequest, and the register records the per-stream surplus. **The maintainer extended that ruling to the five hashed keys on the same date.** The #387 worker measured ten deviations where the ruling named five. **The maintainer ruled #369 question 2 on 2026-08-13.** The JA4TS reset delay reads the eleventh SYN-ACK timestamp past the ten-delay cap, and the library keeps the value it writes today. **The project manager ruled #346 under the widened delegation, and that ruling is provisional.** The library carries step 2 of the #129 client direction ruling, under decisive case 3 of the grant. **#413 builds it, and no line of the tree carries step 2 today.** **#369 needs no member of its own**, because #395 wrote the one line of the tree that the ruling needs. **The measurement reads `origin/batch/402-delegation-and-citations` at `895cefa` with the corpus present, against `origin/dev` at `2736873`.** **The run reports 1658 matches before and after.** **The deviations that the register does not hold fall from 645 to 635.** **The register holds 449 keys before and 459 after**, and it gains ten entries under ruling #387. **The accepted deviations rise from 409 to 419**, and the accepted uncovered values read 40 before and after. **The run reports 0 stale entries and 0 orphan entries on both sides.** **The accepted comparisons equal the register key count on both sides**: 449 against 449, and 459 against 459. **Eight rulings share the 459 entries.** The counts read 248 under #42, 108 under #285 and 35 under #196. They read 28 under #361, 14 under #197, 12 under #375, 10 under #387 and 4 under #223. **`make conformance` exits 2 on this branch, and Epic 5 owns that exit.** **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable today, and this batch reports no green gate.** **CI passed on the batch branch, and a passing job is not a zero-deviation result.** Run 31709959112 at commit `895cefa` reports `lint: success`, `conformance: success` and `test (1.24, ubuntu-latest): success`. **The `conformance` job holds the exit status of the suite back by design.** `.github/workflows/ci.yml:106-111` runs `make conformance 2>&1 | tee conformance.log || status=$?`, and it prints that status rather than exiting on it. FR-conformance-34 and the maintainer ruling of 2026-08-11 state the reason at `.github/workflows/ci.yml:36-40`. **A later step fails the job on a build failure, and on every `--- FAIL:` line that does not name `TestConformance`**, at `.github/workflows/ci.yml:116-130`. **So the green check reports that the suite ran and that nothing but a deviation failed.** It reports no closed backlog, and 635 deviations stand. **Seven issues came out of this batch, and each one came from a finding of the batch itself.** #403 lists every one-shot function in the README code block. #406 repairs the #369 open-question comment in `ja4ts.go`, which is production code that #395 does not own. #409 extends ruling #387 to the five hashed JA4S keys, and it merged into this batch. #410 repairs R18 of `docs/specs/foxio/JA4T.md`, and it merged into this batch. #413 carries step 2 of the JA4SSH client direction ruling into the code, and it is `status:blocked` on #239. #417 adds four delegation words to the `## Terms` table. #419 records that the `git stash` deny entry refuses a recovery path that `.claude/rules/worktrees.md` instructs. **A project manager number was wrong four times in this batch, and a worker measured every one.** **The project manager wrote 76 register entries under ruling #42, and the register holds 248.** **It wrote five JA4S keys where ten deviations share one cause**, and the maintainer then extended the ruling to the other five. **It wrote eight exported doc comments that carry the declined synonym, and seven of them are one-shot functions.** **It named #239 as built, and #239 is open, `status:ready` and `priority:high`.** **The rule holds: never hand a worker a number it can measure itself.** **The 404 `crossCheck` defect repeated, and round 40 above already records the rule that stops it.** Round 40 states that rule as `capture the URL the forge returns rather than construct one`. **The project manager wrote that rule and then did not apply it.** It posted the cross-check with `gh issue comment 402 --body-file /tmp/cc402f.md >/dev/null`, which discards the URL that `gh` returns, and the identifier then came from memory. **The brief of #409 and the brief of #410 each carried a comment id that the API reports as `Not Found`.** **The worker of #410 measured the 404, recovered without stopping, and reported it.** It listed the comments of #402 and it found the real cross-check. **A worker that treats a 404 `crossCheck` as a hard block returns `blocked` and does no work.** That is the correct behavior under the worker contract. **Finding 3.1 of the cross-member review is a record, and this round proposes no edit of the body.** **Pull request #404 cites R18 of `docs/specs/foxio/JA4T.md` at `:114-129`, and #410 grew R18 in the same batch.** **R18 runs from `:114` to `:154` in the merged tree.** **A merged body is a record of what its author measured.** `.claude/rules/cross-member-review.md` states that a finding against a body names no repair of the body. **The repair commit of this round carries two findings, and each one is a rule breach.** **Finding 1.1 repairs two citations of one table cell.** `.claude/rules/rulings.md:271` sent the reader to #346 for the step 2 work, and #413 holds it. The same cell cited comment `5276173568`, which is the reading of session 9, and the delegated ruling is comment `5281493518`. The cell now cites the ruling, and it names #413. **Finding 4.2 repairs one line of `README.md`.** The line read `For stateless fingerprinting without maintaining state:`. **That is a breach of rule 7 of `.claude/rules/ste.md`, and it is not a breach of the `Do not use` column.** The column of the `one-shot function` row declines the string `stateless function`, and that string is absent from the line. **Rule 7 states one concept and one word**, and the line named the one-shot function concept in other words. The line now reads `Each one-shot function below computes one fingerprint, and it holds no connection state:`. **Four sentences of this file that the merged result makes false reach two different repairs, and the shape follows this file's own precedent.** **This file repairs a round in place when the round was false on the day it was written.** Round 23 above repairs two sentences of round 22, and round 30 above repairs round 29, which `described the tree that stood before its own commit`. Each of those repairs landed in the batch that wrote the round. **This file corrects a round in a later round when a later fact makes the round false, and it leaves the earlier round intact.** Round 18 above records that `Round 16 above cited the wrong Python`, and the round 16 row is byte-identical to the row that commit `0489f7f` wrote. **Round 34 and round 40 each state what their author measured on its own date, and this batch made each one false.** **So round 34 and round 40 keep their text, and this round states each correction.** **This round repairs the `delegated ruling` row of the `## Terms` table in place, because that row is a definition and not a record.** It stated that the project manager rules a schema violation and never a reference split. **`.claude/rules/rulings.md` now holds two delegations**, and its `### How the two delegations relate` states that a question which fails the five narrow conditions can still meet a decisive case. Neither #346 nor #369 question 1 is a schema violation, and `.claude/rules/rulings.md:271-272` records both as delegated rulings. **The row now names both delegations, and it leaves a genuine reference split with the maintainer.** **The row writes `narrow delegation`, `widened delegation` and `decisive case`, and the `## Terms` table holds a row for none of them.** **#417 owns that gap, and this round adds no row for them.** **This round repairs `### Issues that wait on a ruling` above in place, because that section is live and holds no round number.** It named six issues that carry `status:needs-feedback`. **One issue carries that label, and it is #364.** `gh issue list --label status:needs-feedback --state all --limit 300` measured that on 2026-08-13. #126, #129, #289 and #300 are closed, #298 carries `status:ready`, and #346 carries `status:batched`. **#279 still owns the repair of the rows of that table.** **Round 34 above states `The hook is inert until the maintainer runs `git config core.hooksPath .githooks`.`, and the hook is installed.** `git config --get core.hooksPath` reports `/Users/christiancrank/Development/Personal/ja4plus-go/.githooks` on 2026-08-13. **The installed value is absolute, and the sentence names a relative path.** `.claude/rules/worktrees.md` records that a relative value resolves per worktree, so a relative install reaches no linked worktree. **#419 owns `.claude/rules/worktrees.md`, and this round changes no line of it.** **Round 40 above states that eight exported functions and three other sites write the declined synonym, and the merged tree holds zero of either.** #399 wrote `one-shot function` on every one of them. **The round 40 count reads the exact string, and the declined concept reaches six other sites at `origin/dev`**: `audit_panic_test.go:833`, `audit_panic_test.go:916`, `docs/audit/findings.md:598`, `docs/audit/findings.md:606`, `ja4_plausibility_guard_test.go:176` and `ja4s_test.go:303`. **The merged tree writes that string on two lines alone**: the `Do not use` column of the `one-shot function` row at `docs/specs/spec.md:199`, and round 40 above. **This round writes it on no line**, so the count stays at two. **`docs/specs/spec.html` needs no change.** It holds no `## Terms` table, no changelog round, no one-shot text and no `status:needs-feedback` list, so no sentence of it reads on this round. **This batch moves no fingerprint value, and it opens no issue in the port.** The #346 ruling brings this library to the rule that the port already ships at `ja4plus/fingerprinters/ja4ssh.py:355-361`. **The #387 ruling and the #369 question 2 ruling each keep the value that the port writes today.** **This round describes the tree that its own commit produces.** Recorded by batch #402. |
| 42 | 2026-08-13 | **Batch #421 merges fifteen members and one repair, and it is the first batch of this session to raise the match count.** #239, #413, #446, #447, #442, #443, #70, #434, #406, #403, #428, #417, #419, #426 and #429 are the members. **#239 bounded the JA4SSH state table by entry count and by age, and it unblocked #413.** **#413 built step 2 of the JA4SSH client direction ruling, under a second bounded table for the handshake originator.** **#446 gave JA4H one value for a repeated TCP segment, and it closed 50 deviations.** **#447 moved `JA4L-S` to the point D frame, and it closed 14 deviations and removed 21 register entries.** **#442 read the JA4H deviation cluster as five causes, each with a measured yield.** **#443 read the JA4L and JA4LS deviation cluster as five causes, three of them measured.** **#70 recorded the `gopacket` dependency reading, under a measurement the maintainer authorized.** **#434 ran that measurement, and no fingerprint value moves under the fork.** **#406 wrote the #369 ruling into the `recordSynAck` comment of `ja4ts.go`.** **#403 made the one-shot code block of `README.md` name all ten exported functions, under a guard that reads the exported set.** **#428 made a malformed `## Terms` row fail the suite, by cell count.** **#417 added `delegation`, `grant`, `narrow condition` and `decisive case` to the `## Terms` table.** **#419 named the maintainer as the actor of each `git stash` recovery path in `.claude/rules/worktrees.md`.** **#426 made the Epic 7 file table name the creator of every file it names.** **#429 gave Epic 7 the `.coverage-floor` file, and it closed a gap that would have failed at the Epic 7 gate.** **The measurement reads `origin/batch/421-state-bound-and-repairs` at `106b2e9` with the corpus present, against `origin/dev` at `76659bb`.** **The matches rise from 1658 to 1664**, and that is the first rise of session 10. **The deviations that the register does not hold fall from 635 to 571**, so this batch closes 64 of them. **#446 closes 50 and #447 closes 14.** **Pull request #450 states that #447 closes 15, and the two numbers count different things.** Fifteen comparisons move from a deviation to a match, and the removal of the one stale register entry returns one comparison to the unaccepted count, so the total falls by 14. **The accepted deviations fall from 419 to 418 for the same reason**, and **the register falls from 459 keys to 438**. **The unaccepted uncovered values fall from 202 to 172, and the accepted uncovered values fall from 40 to 20.** **The run reports 0 stale entries and 0 orphan entries on both sides.** **418 and 20 add up to 438, so `accepted comparisons == \|register\|` holds.** **This round measured every one of those counts rather than adopting them**, and `conformance_test.go:683` states them on the merged tree. **`make conformance` exits 2, and Epic #441 owns that exit.** **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this batch reports no green gate.** **Epic 5 is closed and every one of its members is closed, and the 635 deviations then had no open owner.** **#441 is that owner**, and it holds a cluster breakdown with a measured yield for each cause. **The maintainer adopted two policies on 2026-08-13.** **A documentation-consistency defect no longer earns a tracker issue**, and the batch documentation round repairs it instead. #439 carries the fourteen that were already filed. **An internal cross-reference cites a section heading, and an evidence citation keeps `file:line`**, because an evidence citation names a pinned commit. #440 carries that convention. **This round corrects three earlier rounds, and it edits no earlier round in place.** The shape follows the precedent that round 18 and round 23 set: a round that a later fact makes false is corrected in a later round, and a round that was false on the day it was written is repaired in place. **Round 41 above states `no line of the tree carries step 2 today`, and `decideEndpoints` of `ja4ssh.go` now carries it.** #413 built it, and the second step of that function reads the handshake table, between the port 22 test and the lower port test. **Round 41 above assigns the `narrow delegation` and `widened delegation` gap of the `## Terms` table to #417, and #417 closed neither.** It added `delegation`, `grant`, `narrow condition` and `decisive case`. **This round adds a row for `narrow delegation` and a row for `widened delegation`**, because rule 6 of `.claude/rules/ste.md` gives one word one meaning and neither meaning follows from the `delegation` row. The `narrow condition` row and the `decisive case` row each name one of the two delegations, so a reader reaches the two names before any definition of them. **Round 39 above cites `ja4l.go:183` and `ja4l.go:381` as the two `JA4L-S` writes, and #447 moved the second write to `ja4l.go:413`.** `ja4l.go:381` now holds `if _, ok := conn.timestamps["A"]; ok {`. **Round 39 was true on the day it was written, so it keeps its text.** **The repair commit of this round carries five findings, and the cross-member review measured every one.** **Finding A is a behavior change with no changelog entry, and FR-foundation-21 requires the Keep a Changelog format.** Neither #446 nor #447 reached `### Added`, `### Fixed` or `### Changed`, and each one moved what the library emits. **A user of `v0.3.0` who upgrades reads one JA4H value where two arrived, and a `JA4L-S` on a later frame.** `### Fixed` of `CHANGELOG.md` now holds one entry for each, with the upgrade effect. **Round 39 above records the same defect class, so this is the second occurrence.** **Finding B is a double count of the register in the `## [Unreleased]` preamble of `CHANGELOG.md`.** #447 lowered two counts of the enumeration to the post-removal holdings, and it added the sentence `Issue #447 removed 21 entries`. A reader who read the enumeration as a history reached `438 + 21 = 459`, which the paragraph never states. **The preamble now states that the enumeration reads the present and never a history**, and it states that the enumeration already subtracts the removal. It names the two counts the removal lowered: #197 from 14 entries to 13, and #361 from 28 entries to 8. `changelog_counts_freshness_test.go` still holds the sum at 438. **Finding C is a stale line citation, and this batch moved each target.** The JA4LS row of the parity register cited `ja4l.go:183` and `ja4l.go:381`, and the `Three defects` box of `docs/specs/spec.html` cited `ja4ssh.go:176-180` for a `min(packetCount, 10)` cap that no line of `ja4ssh.go` holds. **Each repair cites a function and a file rather than a line**, under the #440 convention. The rows now name `processTCP`, `processUDP`, `emitResult`, `checkWindow` and `generateTCPFingerprint`. **The `Three defects` box named three defects and the code holds none of them.** The JA4SSH cap and the JA4T padding were each repaired before this batch, and no reader of that box could tell. **Finding D is the `### Fingerprinter state` table above, which named none of the state this batch added.** `JA4HFingerprinter` now holds `ranges`, and `JA4SSHFingerprinter` now holds `arrivals`, `order`, `packets`, `handshakes` and `handshakeOrder`. **Batch #342 found the same table naming a deleted field, so this is the second consecutive occurrence**, and the text above says so. **The `results` field of every row is separately false, #326 owns that repair, and this round leaves it alone.** **Finding E is the R5 sentence above, which read `no release since v1.1.19 in 2022`.** The Go module proxy reports `{"Version":"v1.1.19","Time":"2020-10-19T16:12:32Z"}`, and #434 recorded it in `docs/audit/dependency-decision.md`. **The gap is five years, nine months and 25 days on 2026-08-13, and not four years.** `docs/specs/spec.html` held the same sentence, and this round repairs both. **`docs/audit/dependency-decision.md:46` states the gap as five years and ten months, and that figure rounds up.** Five years and ten months after 2020-10-19 falls on 2026-08-19, which is 6 days after the measurement date. **This round writes the exact span and it edits no audit file**, because #434 owns `docs/audit/dependency-decision.md`. **The self-review of this round found that rounding**, and it found three sentences above the 25-word limit of rule 1 of `.claude/rules/ste.md`. Each of the four is repaired. **The project manager's own defects are recorded, and a worker measured every one.** **Six project manager numbers were refuted by measurement in this batch.** **The per-method deviation counts were undercounted in every cluster**, because the pattern that read them required a `.N` suffix that a per-stream key does not carry. **The 404 `crossCheck` defect repeated twice more**, from a `>/dev/null` that discards the URL the forge returns. **The mechanism is now a shell variable**, and round 40 above and round 41 above each record the rule that the project manager wrote and did not apply. **#429 sat claimed with no worker for a long period.** The project manager planned it, posted its cross-check and claimed it, and it launched no worker. **One state of the merged tree is recorded here and repaired nowhere, and it is a naming difference and never a defect.** **#239 and #413 evict with a `container/list` order list and the constant prefix `maxSSH`, and #446 evicts with a full map scan and the constant prefix `ja4hMax`.** **Both age bounds are 600 seconds**, which the port sets at `ja4plus/utils/state_table.py:58` and at `ja4plus/utils/tcp_stream.py:50`. **Rule 7 of `.claude/rules/ste.md` states one concept and one word**, and two eviction shapes under two prefixes give one concept two names. **This round repairs no code**, and no `.go` file of this batch changes under it. **The maintainer authorized four concurrent workers on 2026-08-13**, and `.issue-flow.json` sets `"concurrency": 4`. The standing limit of three lives outside this repository, so this round changes no line of that file. **This batch moves two fingerprint paths, and the two move parity in opposite directions.** **#446 closes a parity difference**, because the port already holds the consumed-sequence rule at `ja4plus/fingerprinters/ja4h.py`. **#447 opens one, and the port carries no matching half today.** `ja4plus/fingerprinters/ja4l.py:549-551` returns the `JA4L-S` value from `_quic_server_initial`, so the port emits the server value at the point B packet exactly as this library did. **Pull request #450 states `A port issue is needed`, and no port issue holds that question on 2026-08-13.** `Crank-Git/ja4plus#595` asks whether the QUIC path evicts its JA4L points at point D, and that is a different question. **This round records the gap and files nothing**, because a port issue is the project manager's to open. **This round describes the tree that its own commit produces.** Recorded by batch #421. |
| 43 | 2026-08-13 | **Batch #457 merges three members and one repair, and it moves one fingerprint path.** #454, #440 and #455 are the members, and #465 is this round. **#454 wrote policy 1 and policy 2 into the rule files.** Policy 1 names the five cases that earn a tracker issue, and it sits in `CLAUDE.md`. Policy 2 states the citation convention, and it sits in `.claude/rules/ste.md`. `rule_file_policies_test.go` holds both. **#440 converted the line citations of the rule files to stable targets**, across 14 spans. **#455 moved the JA4H emission frame to the packet that completes the request.** **The measurement reads `origin/batch/455-ja4h-emission-and-policies` at `af67ce9` with the corpus present, against `origin/dev` at `6027d9e`.** **The matches rise from 1664 to 1680, and the deviations that the register does not hold fall from 571 to 475.** **The accepted deviations rise from 418 to 450, and the register grows from 438 keys to 470.** **The run reports 0 stale entries and 0 orphan entries on both sides**, and `450 + 20 = 470` holds on the merged tree. **This round measured every one of those counts rather than adopting them.** **The per-packet set moved alone, and no per-stream count changed.** The per-packet matches rise from 561 to 577. The per-packet deviations fall from 508 to 412. The per-packet accepted deviations rise from 143 to 175. **96 unaccepted deviations become 48 comparisons**, because each frame pair of cause 2 of `docs/audit/ja4h-deviation-cluster.md` holds one value. **16 of the 48 are matches, and 32 are accepted deviations.** **That count of 48 reads the frame pairs, and the yield of the change is 64.** The next sentences state the difference. **The cross-check comment of pull request #464 states `571 - 507 = 64` and `507 - 32 = 475`, and both hold.** **The fall of 96 in the unaccepted count carries two mechanisms.** The body gate closed 64 comparisons, and the register absorbed 32 under ruling #285. **A deviation comparison is one that the run counts as unaccepted or as accepted.** The two counts read `571 + 418 = 989` at `dev`, and `475 + 450 = 925` at the batch head. **`989 - 925 = 64`, and that is the yield of the body gate.** `450 - 418 = 32` is the count the register absorbed. **507 is the unaccepted count on a tree that holds the code change and leaves the register unchanged.** **This round cannot observe 507 directly, because its tree already holds the 32 entries.** That is a limit of this measurement, and it corrects no number. **The yield of cause 2 is 64, and it is not 96.** Epic #441 buys yield, so a round that reports 96 as the yield mis-scales every remaining cause. **The 64 and the 32 are checkable from the final tree alone**, through the 989 and the 925. **The maintainer ruled the JA4H body gate on 2026-08-13, and the answer is the strict gate.** The library holds the value until the payload after the header block reaches the byte count that `Content-Length` names. **A request whose body never completes reaches no value.** The library emitted such a request before. **No vector of the corpus separates the two answers**, so `ja4h_body_gate_test.go` records the ruling and no register entry does. `TestJA4H_ProducesNoValueForARequestWhoseBodyNeverCompletes` holds it. **That ruling opened a parity difference, and this round writes the register row.** **The port produces the JA4H value at the packet that ends the header block**, at `ja4plus/fingerprinters/ja4h.py:149-166`. **This round read the port at the tag `v1.1.0`, which `.claude/rules/parity.md` names, and not at the default branch.** The cross-member review read `master` at `9e4c5789`. **`ja4plus/fingerprinters/ja4h.py` is byte-identical at `v1.1.0` and at `master`**, so the reading holds at the tag. `Crank-Git/ja4plus#607` carries the port half, and it is open. **The register row is bookkeeping and it is not a new ruling**, because the maintainer already ruled the question. **The cross-member review found three defects, and it met the eight-item return contract.** **This round states no streak count.** Round 40 above reads `twelve for twelve batches`. Round 41 and round 42 each state none. **So this round cannot extend the chain without re-reading two batch gates it did not run.** **All three findings come from the second pass, which reads each rule the batch adds against the whole merged tree.** No file of this batch was touched by two members. **Finding 1 is category 4, and it is a member that makes a sibling non-conformant.** The internal row of `## How a citation names its target` named `The section heading` alone. **#440 legitimately cited `R13` and `R18` of `docs/specs/foxio/JA4T.md`.** It also cited the requirement number `FR-ja4ls-11`. **Neither form is a section heading, and each one is stabler than a line number.** **`docs/specs/foxio/*.md` holds numbered rules and few headings**, so a reader who applied the table there reached no permitted form at all. **The row now names a section heading, a rule number, a requirement number, or an identifier.** **It states the property they share.** Each one names the text it points at, so it survives an edit above it. **`## Terms` gains a `stable target` row for that word**, under rule 6 of `.claude/rules/ste.md`. **The guard asserted the narrow wording, and this round widened it.** `TestTheInternalCitationRowNamesEveryStableTarget` now reads the four forms. **The defect is the rule text, and it is not the conversion.** **Finding 2 is category 6, and it is a page that states behavior the tree no longer holds.** `docs/audit/ja4h-deviation-cluster.md` read `The library emits when the header block ends.`, and #455 landed the opposite in the same batch. **The page is a reading dated 2026-08-13, and this round separates its record from its present tense.** A new `## Two causes are closed since this reading` section states what the library does today. Each cause section keeps the count that #442 measured. The falsified sentences move to the past tense. **The risk that the reading named and did not settle is now settled**, and the page cites the ruling and its test. **Finding 3 is category 6, and it is a register count that this batch moved.** The page read `The register holds 108 entries for it today`, and the register holds 140. **This round measured 140 rather than adopting it**, and it measured 108 at commit `dbbad89`, which is the tree the reading ran against. **Every JA4H key of the register names ruling #285, and every #285 entry names a JA4H key**, so the page's second clause still holds. **The page cited this library's own code at ten distinct line spans, in twelve places, and nine of those spans were stale.** The review named `ja4h.go:88`, which the page cites twice. The other stale spans are `ja4h.go:47`, `ja4h.go:50-53`, `ja4h.go:95-96`, `ja4h.go:125`, `ja4h.go:202-205`, `ja4h.go:284`, `internal/parser/http.go:35`, which the page also cites twice, and `internal/parser/http.go:116-119`. **Each of those nine spans now holds something else**, so eleven sentences stated behavior of a line that does not carry it. **`internal/parser/hash.go:14-16` was the one span that still held what the page claimed.** This round converted it with the rest, rather than leave one line citation behind. **This round converted all twelve places to the identifier form** that the third row of `## How a citation names its target` names. They are `ProcessPacket`, `segmentCarriesNoNewRequest`, `rememberTheConsumedRequest`, `ja4hPartA` and `computeJA4HFromRequest` of `ja4h.go`, and `requestLineRe` and `HTTPMessageIsComplete` of `internal/parser/http.go`, and `TruncatedHash` of `internal/parser/hash.go`. **Every evidence citation of that page is byte-identical.** **This round measured that rather than asserting it.** The sorted set of `testdata/foxio`, `ja4plus` and `packet-ja4.c` spans is unchanged. **The project manager decided that #454 owns the rule text and #440 converts.** One rule, one owner. **A rule and its conversions land in one batch, and the rule is the surface that a later reader reads.** **The Epic #441 member list named two pull requests, and this round records the repair.** It named `#444` and `#445`, which are the pull requests of #443 and #442. **So the JA4X cluster and the JA4T, JA4TS, JA4SSH and JA4S clusters had no owner at all.** **#458 and #459 are those owners**, and each one is open. **A bare number that names a pull request in a member list is one defect class with a bare number that names the port.** `.claude/rules/rulings.md` `## A citation names its repository` already records that class. **#463 is the one issue this batch filed, and policy 1 admits it because it is behavior.** It produces a JA4H value for a pipelined second request. **The review confirmed that it satisfies two of the five cases**, because it states behavior and a fingerprint value. **No other finding of this batch earned an issue**, and this round repairs each one. **This round moves no fingerprint value.** `testdata/deviations.json` is unchanged under it. **The four counts read the same before this round and after it**: 1680 matches, 475 deviations, 450 accepted deviations and 470 register keys. **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 475 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this batch reports no green gate.** **Cause 1, cause 4 and cause 5 of the JA4H cluster were open at this round.** **Batch #478 repaired the two sentences that stood here**, because each one named a question that the maintainer has since settled. The first read `Cause 1, cause 4 and cause 5 of the JA4H cluster are open.`, and the second read `The maintainer rules split S1 and split S2 of cause 4.` **The maintainer ruled split S1 of cause 4 on 2026-08-13, and comment 5286085639 of #441 holds the ruling.** The library reads an HTTP request over TCP alone, so it produces no JA4H value for an SSDP request over UDP. **Split S2 is unreachable under that ruling**, because `M-SEARCH` reaches this library only through an SSDP request over UDP. **The reversal path is a read of the UDP payload**, which reopens split S2 and needs a matching change in the port. **#467 recorded the ruling as 108 capability declines in `testdata/deviations.json`.** **Cause 1 and cause 5 are open, and #441 owns each one.** **This round describes the tree that its own commit produces.** Recorded by batch #457, and repaired by batch #478. |
| 44 | 2026-08-13 | **Epic 7 merged at `6681d3e` and it carried no documentation round, because session 11 ended at the merge.** Batch #478 is that round, and #477 writes this entry. **`dev` held no record of Epic 7 in this document until now.** **Epic 7 shipped six members.** #65 gates a pull request on `govulncheck`. #66 configures Dependabot for `gomod` and `github-actions`. #67 pins every action reference to a 40-character commit hash. #68 enforces a coverage floor. #69 runs the benchmarks in CI. #70 decides the `gopacket` question. **#426 and #429 each closed as a scope decision of the same epic.** **This round changes no Go file, and it moves no fingerprint value.** `testdata/deviations.json` is unchanged under it. **The four counts read the same before this round and after it**: 1680 matches, 475 deviations, 450 accepted deviations and 470 register keys. The same run reports 172 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **#477 measured every one of those counts with the corpus present, and it adopted none of them from an issue body.** **That run records the branch of batch #478 before #467 sub-merged, and #477 wrote this entry first of the five members.** **#467 then moved three of the four**, so the branch head holds 1680 matches, 367 deviations, 558 accepted deviations and 578 register keys. **Round 45 below states the counts of the merged branch**, and batch #478 measured each one. **The matches hold at 1680 across both runs**, so no member of batch #478 moved a fingerprint value. **The maintainer moved every CI job to Go 1.26 on 2026-08-13, and comment 5286440152 of #65 holds the decision.** On go1.24.13, the newest Go 1.24 patch, `govulncheck` v1.6.0 reports 9 called standard library vulnerabilities and exits 3. **Each one names a fix in a go1.25.x release, and none names a fix in a go1.24.x release**, so no Go 1.24 patch clears them. **FR-foundation-1 does not move**, because `go.mod` states a language version and never a toolchain, and #438 established that reading. **The workflow states the range `~1.26.6`, and it does not state `1.26`.** `actions/setup-go` reads the tool cache of the runner before it reads the release list. **So a runner that caches an earlier 1.26 patch resolves `1.26` to a toolchain that still calls a vulnerable function.** **Three requirements of `features/07-supply-chain.md` promised what the platform does not deliver.** **The member that built each one found the defect.** FR-supply-23 promised a pull-request comment on a benchmark regression. GitHub gives a fork pull request a read-only token whatever the `permissions` block states. **A wider `permissions` block therefore buys no comment.** The requirement now names a build summary and a warning annotation. FR-supply-19 promised that the floor rises when coverage rises. **A job cannot commit that rise under `contents: read`**, so #68 read the requirement as a rule for a person rather than widening the block. FR-supply-9 promised that a Dependabot pull request targets `dev`. **`target-branch` moves a version update alone**, and GitHub states the limit: `All options marked with a security updates icon also change how Dependabot creates pull requests for security updates, except where target-branch is used.` **#477 read that page rather than adopting the sentence**, at <https://docs.github.com/en/code-security/dependabot/working-with-dependabot/dependabot-options-reference>, retrieved 2026-08-13. **The default branch of this repository is `master`**, measured with `gh repo view` on 2026-08-13, so a security update reaches `master` and it reaches `dev` through a later merge. **The FR-supply-9 amendment is provisional, and issue #66 is its reversal path.** **No comment confirms the FR-supply-9 amendment, and #477 measured that rather than asserting it.** The body of #477 cites comment 5286085774 of #439 and comment 5286440152 of #65 as the confirmation. **Neither comment names FR-supply-9, Dependabot or `target-branch`**: the first decides the English question and the second decides the Go 1.26 move. **So the amendment is unconfirmed, and a later reader reads it as unconfirmed.** **This round states no total count of provisional decisions.** The issue body named ten and named nine of them confirmed. **#477 could not reproduce either number from the tree**, and a count it cannot measure is a count it does not write. `.claude/rules/rulings.md` `### Every delegated decision of session 9` holds the one enumerated list this repository carries. **The `vuln` gate goes red with no change to the tree, because the vulnerability database is live.** The `vuln` job passed at `~1.26.5` on go1.26.5 at commit `83f2127`, and it failed on the same toolchain at commit `7210e80` about one hour later. **No line of this repository changed between the two runs.** The comment above the `vuln` job of `.github/workflows/ci.yml` records the measurement. **The tree records that one occurrence, and #477 read no record of a second**, so this round states one rather than two. **The coverage floor disagreement was an environment mismatch, and the measurement settled it.** `make cover` reads the FoxIO corpus, and `.gitignore` holds the corpus out of the tree, so the same command reports two numbers: 75.0 with the corpus and 74.4 without it. **#68 measured 75.0 with the corpus, and the coverage job fetched no corpus, so the floor was unmeetable on the day it was written.** **#68 measured honestly, and the defect is the environment and not the number.** The Epic 7 gate repair added the four corpus steps to the `coverage` job and it left the floor at 75.0. `.coverage-floor` holds `75.0`, measured on 2026-08-13 at `6681d3e`. **A person who raises the floor under FR-supply-19 runs `make corpus` first**, because a measurement without the corpus reads 0.6 points low. **The `govulncheck` edge-case row of `features/07-supply-chain.md` stated two things the tool does not do, and this round repairs both.** It promised that the job fails when a vulnerability has no fixed version. **The calling path decides the exit status, and the fix status does not**: a called vulnerability exits 3 and an uncalled one exits 0. It promised a named exclusion with an expiry date. **The tool offers none**, and it states so: `There is no support for silencing vulnerability findings. See https://go.dev/issue/61211 for updates.` Verified against <https://pkg.go.dev/golang.org/x/vuln/cmd/govulncheck>, retrieved 2026-08-13. **Two file tables named a file that the tree now holds, and this round repairs both.** `features/07-supply-chain.md` `## Data touched` named three absent files, and #66 and #68 falsified two of the three. `git ls-tree -r --name-only HEAD -- .github` returns `.github/dependabot.yml`, `.github/workflows/ci.yml` and `.github/workflows/release.yml`, measured on 2026-08-13 at `6681d3e`. **`.github/workflows/fuzz.yml` is the one file of the three that the tree does not hold**, and issue #47 creates it. `features/00-foundation.md` read `Epic 7 creates that file, under issue #68.` in the present tense, and Epic 7 created it. **The `## What is missing` table of that file records a start state**, and this round says so rather than editing a historical row. **The `.github/workflows/ci.yml` row of its `## Data touched` table read `The matrix reduces to Go 1.24.`, which the FR-foundation-2 amendment contradicts.** The row now names no version. **The maintainer decided the English question on 2026-08-13, and `.claude/rules/ste.md` now states the rule.** Comment 5286085774 of #439 holds the decision: `This project writes US English, and the documentation round converts incrementally.` **Until this round the rule bound nobody**, because no rule file carried it. **The maintainer declined a repository-wide sweep**, because a sweep would edit a file that a live batch member also edits. **Issue #388 asked for the sweep, and the maintainer closed it on 2026-08-13**, so no issue holds the remaining files and each round takes the files it opens. **35 tracked files hold a British spelling**, measured on 2026-08-13 at `6681d3e`. **This round converted the prose of the three files it opened, and it left the `## Behaviour rules` heading.** 18 tracked files carry that heading, and one file cites the heading of another, so a partial conversion breaks a citation. **`misspell` reads a Go file alone**, so no check of this repository reads a document, and `.golangci.yml:32` states `# The maintainer writes US English.` **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 475 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **This round describes the tree that its own commit produces.** Recorded by batch #478. |
| 45 | 2026-08-13 | **Batch #478 merges five members, and it moves no fingerprint value.** #477, #467, #458, #459 and #483 are the members, and #483 is this round. **Each member forked `6681d3e`, and each one measured against the branch as it stood at its own start.** So this round re-measured every count at the branch head, and it adopted no number from an issue body and none from a member verdict. **#467 recorded the JA4H over UDP ruling in the register.** The maintainer ruled split S1 of cause 4 of the JA4H cluster on 2026-08-13, and comment 5286085639 of #441 holds the ruling. **The library reads an HTTP request over TCP alone, so it produces no JA4H value for an SSDP request over UDP.** **108 capability declines record that ruling**, and each one names ruling `#441`. This round read the register at `6681d3e` and at the branch head, and the difference is 108 added entries and 0 removed entries. **36 name a `JA4H` key, 36 name a `JA4H_r` key and 36 name a `JA4H_ro` key.** Every one of the 108 sets `capability` to `true`. **The register held no `capability: true` entry before this batch.** The register at `6681d3e` holds 470 entries and 0 of that kind, measured by this round. **So batch #478 introduced the first capability declines of this repository.** **The reversal path is a read of the UDP payload**, which reopens split S2 and needs a matching change in the port. **The four counts moved across the batch, and the matches did not.** The report that `6681d3e` holds reads 1680 matches, 475 deviations, 450 accepted deviations and 470 register keys. **One run of `make conformance` at the branch head reads 1680 matches, 367 deviations, 558 accepted deviations and 578 register keys.** The same run reports 172 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **Each of those four counts holds at both commits.** **`475 - 367 = 108` and `558 - 450 = 108`**, so the fall in the unaccepted count equals the rise in the accepted count. **The matches hold at 1680**, so no member of this batch moved a fingerprint value. **The four counts read the same before this round and after it**: 1680 matches, 367 deviations, 558 accepted deviations and 578 register keys. `testdata/deviations.json` is unchanged under this round. **#458 and #459 wrote two cluster readings, and they cover four rows of the cluster table of Epic #441.** #458 wrote `docs/audit/ja4x-deviation-cluster.md` for the `JA4X, JA4X_r` row, and #459 wrote `docs/audit/ja4t-ja4ssh-ja4s-deviation-cluster.md` for the `JA4T, JA4TS`, `JA4SSH` and `JA4S, JA4S_r` rows. **The body of #483 states three readings, and this round measured two pages over four rows rather than adopt that number.** **#458 measured 50 deviations, and this round re-measured 50 at the branch head.** They are 34 `JA4X` keys and 16 `JA4X_r` keys, in six captures: `tls-handshake.pcapng` 18, `socks-https-example.pcap` 12, `http2-with-cookies.pcapng` 9, `latest.pcapng` 6, `badcurveball.pcap` 3 and `chrome-cloudflare-quic-with-secrets.pcapng` 2. **The 108 entries of #467 name a JA4H key each, so they reach no JA4X key**, and the JA4X count holds across the two runs. **#459 measured 67, and Epic #441 states 65.** This round re-measured 67 at the branch head: `JA4T` 31, `JA4TS` 6, `JA4SSH` 16, `JA4S` 7 and `JA4S_r` 7. **The JA4S pair is the row that moved**, and the epic recorded 12 where the run records 14. **The epic measured 65 on 2026-08-13, before batch #421 and before #438.** So the difference is a moved tree, and it corrects neither number. **This round repaired three stale figures of `docs/audit/ja4x-deviation-cluster.md`.** #458 measured its whole-run context against the branch before #467 sub-merged, and the page stated 475 deviations with no run named. **The page now names each run**, and it states 475 for the first run and 367 for the second. **Every JA4X figure of the page holds at both runs**, and the page says so. **A third figure of that page was false at both runs.** It read `Every one of the 47 unaccepted keys names JA4X_r`, and the run reports 45 `JA4X_r` keys and 2 `JA4X` keys. **The 2 are `https-connect.pcap/0/JA4X` and `https-connect.pcap/0/JA4X.2`.** `testdata/foxio/python/https-connect.pcap.json` holds one stream entry, and that entry names `JA4H` and `JA4H_ro` and no JA4X key. **That is a disagreement about coverage and not about a value**, which is the reading the same page states for `chrome-cloudflare-quic-with-secrets.pcapng`. **This round repaired the `JA4XFingerprinter` row of the fingerprinter state table above.** The row read `guarded by mu`, and `ja4x.go` holds no mutex field. `JA4XFingerprinter` holds `reassembler`, `processedCerts`, `certsByStream`, `streamBytes` and `lastCleanup`. **`.claude/rules/concurrency.md` bars a mutex in a fingerprinter**, and no other row of that table names one. #458 reported the sentence and repaired nothing, because it owns no file of `docs/specs/`. **This round repaired the doc comment of `resetResults` in `ja4ts.go`, and it changed no other line of any Go file.** The comment stated that both FoxIO implementations nest the RST branch inside the delay branch. **That reading holds for the reset letter, and it does not hold for the four-part value.** `wireshark/source/packet-ja4.c:684` opens the branch on `syn_ack_count > 1`, and `wireshark/source/packet-ja4.c:693` reads `rst_time` inside it. `zeek/ja4t/main.zeek:229` opens the branch on a delay list that holds a value, and `zeek/ja4t/main.zeek:232` reads `rst_ts` inside it. **Each implementation writes the four-part value above that branch**, so a connection with one SYN-ACK still reaches a JA4TS value. **This round read both sources at the pin that `testdata/foxio.pin` holds**, which is `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. **The comment now cites `:684` for the guard and `:693` for the read**, because the guard and the read sit on two lines. **`## Terms` declines `yield`, the tree uses it in a second sense, and this round picks nothing.** The `emit` row names `yield` as a declined synonym of the verb `emit`, which returns a fingerprint from a fingerprinter. **Epic #441 and two changelog rounds use `yield` as a noun**, and that noun names the count of deviations a fix closes. **Rule 6 of `.claude/rules/ste.md` states one word, one meaning, one part of speech**, so the two senses cannot both stand. **This round measured the usage.** Nine noun uses sit in two tracked files: seven in round 42 and round 43 above, and two in `docs/audit/ja4x-deviation-cluster.md`. Two verb uses sit in `.claude/skills/conformance/SKILL.md` and in `docs/audit/conformance-exclusions.md`. **#459 already declines the word**, and `docs/audit/ja4t-ja4ssh-ja4s-deviation-cluster.md` writes `the count it closes` instead. **The two candidate answers are these.** **Answer 1 gives `yield` a `## Terms` row of its own, as a noun**, and the `emit` row keeps it as a declined verb. That answer records the second sense, and it gives one word two parts of speech, which rule 6 bars as written. **Answer 2 keeps `yield` declined in every part of speech**, and the open tree writes the count it closes instead. That answer keeps rule 6 whole, and it reaches the open tree alone. **The usage half is unrepairable under either answer.** Epic #441, several closed issues and two merged changelog rounds hold it, and each one is a record. **The maintainer rules this question, and this round picks neither answer.** **This round converted the prose of the two documents it opened, under the US English rule that #477 wrote into `.claude/rules/ste.md`.** It converted 29 uses of `behaviour` in `docs/specs/spec.md` and 1 in `docs/audit/ja4x-deviation-cluster.md`. **It left the two `## Behaviour rules` code spans**, because one file cites the heading of another. **It ran no repository-wide sweep**, which the maintainer declined. **17 tracked files carry that heading, and this round measured 17.** They are the 17 files of `docs/specs/features/`. **`.claude/rules/ste.md` states 18, and round 44 above states 18.** **The difference is a mention against a heading.** `git grep -l '## Behaviour rules'` reports 19 files, and 2 of them hold the string in a code span rather than as a heading. They are `.claude/rules/ste.md` and this file. **This round owns neither number it did not write**, so it repairs neither statement and it records the measurement. **Two tracked files moved outside the plan of every member, and #467 reported both.** The conformance suite regenerates `docs/audit/conformance.md`, and two guard tests of `CHANGELOG.md` fail when a register count moves. **This round changed neither file**, because it moves no count. **The conformance run of this round rewrote `docs/audit/conformance.md` byte-identically**, which is how this round measured that the committed report describes the branch head. **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 367 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **This round describes the tree that its own commit produces.** Recorded by batch #478. |
| 46 | 2026-08-13 | **Batch #478 ran one repair member after its gate, and #486 is that member.** The cross-member review of batch #478 found six defects and one observation, and comment 5287865776 of #478 holds the report. **Each one is a falsified sentence, a stale count or a spelling**, so policy 1 of `CLAUDE.md` sends every one to the round of the batch that found it, and #486 filed no issue. **#486 forked the merged branch at `b178cfd`, after all five members sub-merged.** It re-measured every count at that head, and it adopted no number from an issue body. **The `## Behaviour rules` count of `.claude/rules/ste.md` named the wrong form, and #486 repaired it.** The rule file read `18 tracked files hold it`. **The heading form reaches 17 and the string form reaches 19**, measured at `b178cfd`. `git grep -l -E '^## Behaviour rules'` reports 17 files, and they are the 17 feature files of `docs/specs/features/`. `git grep -l -F '## Behaviour rules'` reports 19 files, and 2 of them hold the string in a code span: `.claude/rules/ste.md` and this file. **The rule binds a heading, so the rule file now states 17, and it names the form and the two commands.** Round 45 above measured 17 and repaired nothing, because #483 owned no line of `.claude/rules/ste.md`. **Round 44 above states 18, and that row records the measurement #477 made.** This round edits no merged round, and it states the measured count here. **#486 converted 9 British spellings in the two documents that batch #478 left.** `docs/audit/ja4t-ja4ssh-ja4s-deviation-cluster.md` held 4 uses of `behaviour` and 2 of `labelled`, and `docs/audit/ja4h-deviation-cluster.md` held 3 uses of `behaviour`. **No conversion touched a quotation, and none touched the shared heading.** **This round converted no file outside the set that batch #478 touched**, which the maintainer requires. **`git grep -l -i` over every tracked file counts the files that hold `behaviour`.** 35 hold it at `6681d3e`. 36 hold it at `b178cfd`. **34 hold it after this round.** `.claude/rules/ste.md` states the 35 and it names `6681d3e`, so this round leaves that sentence and it records the two later counts here. **The `govulncheck` quotation of `features/07-supply-chain.md` carried two angle brackets that the source does not hold, and #486 removed them.** The tool writes `There is no support for silencing vulnerability findings. See https://go.dev/issue/61211 for updates.` **#486 read that sentence in the module source rather than from memory**, at `golang.org/x/vuln@v1.6.0/cmd/govulncheck/doc.go:95`. `.github/workflows/ci.yml` pins v1.6.0, and <https://pkg.go.dev/golang.org/x/vuln/cmd/govulncheck> renders that same file. **`.claude/rules/ste.md` bars a change to copied text**, and round 44 above already held the sentence without the brackets. **The two places now agree, and each one matches the tool.** **Two cluster pages asked for a repair that round 45 above already landed, and #486 repaired both sentences.** `docs/audit/ja4x-deviation-cluster.md` reported that the `JA4XFingerprinter` row reads `guarded by mu`. The merged row names `reassembler`, `processedCerts`, `certsByStream`, `streamBytes` and `lastCleanup`, and it names no mutex. `docs/audit/ja4t-ja4ssh-ja4s-deviation-cluster.md` asked for a repair of the doc comment of `resetResults` in `ja4ts.go`. The merged comment reads `Neither branch reaches the four-part value`. **Each page now records that round 45 carried the repair.** **`docs/audit/ja4t-ja4ssh-ja4s-deviation-cluster.md` carried four whole-run counts from the run that preceded #467, and it named no run.** #486 gave it the form that round 45 gave the sibling JA4X page. **The page now names two runs.** The first run reports 1680 matches, 475 deviations, 450 accepted deviations and 470 register keys. **The second run reads the branch head, and #486 produced it**: 1680 matches, 367 deviations, 558 accepted deviations and 578 register keys. **Every five-method figure of that page holds at both runs**, because each of the 108 entries of #467 names a `JA4H`, `JA4H_r` or `JA4H_ro` key. #486 re-measured each one at the branch head. **67 deviations name one of the five methods**, 61 in the per-packet set and 6 in the per-stream set. The split is JA4T 31, JA4TS 6, JA4SSH 16, JA4S 7 and JA4S_r 7. **Twelve captures hold the 67, and every row of the per-capture table holds.** **The register holds 70 entries that name one of the five methods, at both runs**, of 470 and of 578. **The JA4L register row above stated 28 value declines, and the register holds 8.** #486 counted 8 entries under ruling `#361` in `testdata/deviations.json`, at `6681d3e` and at the branch head. **#361 wrote 28 entries, and #447 then removed 20 of them**, and `CHANGELOG.md` records that removal. **No member of batch #478 touched that row**, so the cross-member review reported it as an observation, and policy 1 sent it to this round. **This round changes no Go file, and it changes no byte of `testdata/deviations.json`.** The register holds 578 entries before this round and 578 after it. **The four counts read the same before this round and after it**: 1680 matches, 367 deviations, 558 accepted deviations and 578 register keys. The same run reports 172 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **So this round moves no fingerprint value.** **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 367 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **This round describes the tree that its own commit produces.** Recorded by batch #478. |
| 47 | 2026-08-14 | **Batch #493 merges two members and one round, and #504 is this round.** #472 and #463 are the members, and both sub-merged before this round forked the integration branch. #472 is `c3d0ef9` and #463 is `89559ad`. **This round files no issue**, under policy 1 of `CLAUDE.md`. **The maintainer ruled two questions on 2026-08-14, and this round records both.** **#472 is the build toolchain question**, and the answer is candidate answer 1: `README.md` and `doc.go` each state a minimum build toolchain, and `go.mod` does not move. FR-foundation-23 of `docs/specs/features/00-foundation.md` holds the requirement, and `go_toolchain_statement_test.go` holds the tests. **The minimum build toolchain is `go1.25.13`**, and `minimumBuildToolchain` states it once. **FR-foundation-1 does not move**, and the language version stays at 1.24. **#463 is the pipelined pair question**, and the answer is one value: the library produces one JA4H value for a pipelined pair. **`ja4h.go` needs no change**, because the library already produces one value. `ja4h_pipelined_request_test.go` holds four tests. **No capture of the corpus reaches a pipelined pair.** A scan of all 38 captures reports 0 payloads that hold a byte past the first complete request, so `.claude/rules/rulings.md` `## Where a ruling is recorded` prescribes a test and `testdata/deviations.json` holds no entry. **Issue #472 and issue #463 are the two reversal paths.** **The `govulncheck` count of the Go 1.24 line moved in one day, and no line of this repository changed.** The measurement reads `govulncheck` v1.6.0 on 2026-08-14 at 01:24 UTC, against a database that <https://vuln.go.dev> published on 2026-08-13 at 21:43:54 UTC. go1.24.13 reports 13 and exits 3. go1.25.13 reports 0 and exits 0. go1.26.5 reports 4 and exits 3. go1.26.6 reports 0 and exits 0. **The clean pair is `go1.25.13` and `go1.26.6`.** **The issue body of #472 records 9 for go1.24.13 and 0 for go1.26.5, and both figures are stale.** The ruling comment of #472 carried the stale 0, and comment 5288420869 of #472 corrects it. **This round writes the measured figures with their date, and it copies no figure of an issue body.** **`.github/workflows/ci.yml` stated an undated 9 in two places, and this round dates both.** The #472 member was barred from that file. **This round changes no job, no version and no step of that workflow**, and `~1.26.6` still names the range for every job. **This round writes the rule that the correction earns.** `CLAUDE.md` `## Conventions` now states that a ruling record quotes no live measurement without its date. A live measurement reads a source that moves without a change to this repository, and the `govulncheck` count is one. `TestTheToolchainPagesDateTheVulnerabilityMeasurement` in `go_toolchain_statement_test.go` already holds that rule for `README.md` and for `doc.go`, and #472 wrote it. **This round adds no second guard**, because the plan of #504 names no Go file for a new assertion. **This round writes the clock rule, and that is a scope decision rather than a ruling.** Every date of this repository states the UTC calendar day. The environment reported 2026-08-13 local and 2026-08-14 UTC on one day, and each tracker timestamp reads UTC. A ruling record names a reversal path, so two clocks give one ruling two dates. `CLAUDE.md` `## Conventions` states the rule. **`## Terms` gains a `pipelined pair` row, and that is the second scope decision.** The word is a domain word, and rule 6 of `.claude/rules/ste.md` states one word, one meaning. The #463 member defined it inside `ja4h_pipelined_request_test.go`, because that member owned no line of this file. **The row now holds the meaning, and the test file cites the row.** The row states that two requests in two byte ranges are no pipelined pair, and `TestJA4H_ProducesTwoValuesForTwoRequestsInTwoSegments` holds that limit. **This round converted the prose of the seven files that batch #493 touched**, under `.claude/rules/ste.md` `## This project writes US English`. They are `CLAUDE.md`, `README.md`, `doc.go`, `docs/specs/features/00-foundation.md`, `docs/specs/features/10-release.md`, `go_toolchain_statement_test.go` and `ja4h_pipelined_request_test.go`. **Round 45 above converted the two documents it opened alone, and nine British spellings survived in two files that batch #478 touched.** #486 repaired those nine, and round 46 above records the repair. **So this round read all seven files rather than the files it opened.** **The seven hold 6 uses of `behaviour`, and this round converted 3 of them.** The two of `CLAUDE.md` sit in policy 1, and the one of `docs/specs/features/10-release.md` sits in FR-release-30. **The other 3 are the shared heading form, and the rule leaves every one.** `docs/specs/features/00-foundation.md` and `docs/specs/features/10-release.md` each carry the `## Behaviour rules` heading, and `docs/specs/features/00-foundation.md` cites that heading in a code span. **17 feature files carry the heading**, and one file cites the heading of another, so a partial conversion breaks a citation. **The found count and the converted count are unequal by design**, and the two sentences above state the difference. **`README.md`, `doc.go` and `go_toolchain_statement_test.go` hold no British spelling at all.** **This round converted no file outside the seven.** **One guard held the British spelling of `CLAUDE.md`, and this round moved that guard with the conversion.** `rule_file_policies_test.go` held `const theBehaviourWord = "Behavi" + "our"`, and the concatenation kept the British form out of `misspell`. The constant is now `theBehaviorWord`, and it holds `"Behavior"`. **The US form needs no concatenation.** `TestCLAUDEmdStatesTheFilingPolicy` and `TestCLAUDEmdNamesEveryCaseThatEarnsAnIssue` each read the converted sentence. **That file sits outside the seven, and this round changed it because the conversion falsifies it.** A conversion that leaves a failing guard behind is an unfinished conversion. **Two sentences of a merged record are wrong, and this round copies neither.** **The report of #490 states that #491 `has not landed` and that it `moves every total`. **A worker report is returned in a session and no page publishes it**, so a later reader reaches the sentence through comment 5288593682 of #441, which quotes it.** The maintainer ruled #491 after that report, and #491 wrote no line of code, so it moves no count at all. **The counts of the Epic #441 batch move through #484, #494 and #495**, and that batch runs on `epic/441-deviation-fixes`. **This round writes no count of that batch.** **The Wireshark count of the first #463 finding is withdrawn.** Comment 5288373578 of #493 states that `wireshark/source/packet-ja4.c:1756` reaches one value for a pipelined pair, and comment 5288427971 of #493 withdraws that count. No documentation this project reached states how often Wireshark calls a postdissector, and `.claude/rules/external-apis.md` bars a description of an external interface from memory. **The disagreement stands on two readings against one**, and the Zeek reading carries the Zeek documentation. **The ruling of #463 rests on no Wireshark row**, and `ja4h_pipelined_request_test.go` records that row as unverified. **Two pages of `docs/audit/` carry a falsified count, and this round writes no line of either one.** They are `docs/audit/ja4x-deviation-cluster.md` and `docs/audit/ja4t-ja4ssh-ja4s-deviation-cluster.md`. **The round of the Epic #441 batch owns both**, and both live on the other integration branch. **This round writes no line of `testdata/deviations.json`**, and #484 writes that file on that branch. **Two integration branches were live at this round**, and this `## Changelog` is the one file that both batches write. **This round writes the batch #493 row alone**, and the second batch to merge resolves the conflict at its gate. **This round moves no fingerprint value, and it changes no Go file that holds behavior.** `testdata/deviations.json` is unchanged under it. **The four counts read the same before this round and after it**: 1680 matches, 367 deviations, 558 accepted deviations and 578 register keys. The same run reports 172 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **Every one of those counts equals the count that round 46 above records.** **The conformance run of this round rewrote `docs/audit/conformance.md` byte-identically**, which is how this round measured that the committed report describes the branch head. **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 367 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **This round describes the tree that its own commit produces.** **The cross-member review of this batch returned three findings, and the project manager repaired all three before the batch merged.** Finding 1 is a count in the body of #513: it reads `Three members`, and this round states that batch #493 merges two members and one round. **A merged pull-request body is a record, so the project manager repaired the body before the merge and it names the true count.** Finding 2 is a rule this batch added that an untouched file breaks: `.github/workflows/release.yml` stated an undated 9 in the present tense, and `CLAUDE.md` `## Conventions` names a code comment as one of the four places that carries a date. **This round dates it, and the round that wrote the rule reached `ci.yml` alone.** Finding 3 is a barred word: the `## Terms` table lists `verdict` in its `Do not use` column of the `ruling` row, and this round wrote it twice. **Both uses now read `report`, and the sentence that quoted a worker report now names the comment that publishes the quotation.** **The review also established the mechanism of one green check that measures nothing.** `.github/workflows/ci.yml` runs `make conformance` and never exits on its status, and the next step fails only on a `--- FAIL:` line that is not `TestConformance`. **So the green `conformance` check of a pull request is no conformance gate while Epic #441 is open.** Recorded by batch #493. |
| 48 | 2026-08-14 | **The Epic #441 batch merges nine members and one round, and #512 is this round.** #489, #490, #496, #484, #494, #495 and #501 are the seven members that wrote a line, and they are `91a289d`, `8347afd`, `e0f7412`, `1d632a7`, `ecc2fa1`, `d637e66` and `74c8827`. **#491 closed as ruled on 2026-08-14, and it wrote no line of code.** **#503 closed, and #501 wrote the one register entry it asked for.** **#502 is parked with `status:needs-feedback`, and it blocks nothing.** **#492 left this batch**, and the ship-partial comment of #441 dated 2026-08-14 states why. **This round files no issue**, under policy 1 of `CLAUDE.md`. **This round writes round 48 and never round 47.** `origin/dev` holds round 47 at `8c6eb02`, which is the merge of batch #493, and `epic/441-deviation-fixes` forked `dev` before that merge. **The `## Changelog` table of this branch held rounds 1 through 46 and this row, and it held no round 47 until the batch gate.** Round 47 records the same collision from the other side, and it states that the second batch to merge resolves the conflict at its gate. **The project manager resolved this table at the batch gate: it merged `dev` into this branch and it kept both rows, round 47 first and round 48 second.** **The merged tree then reported the same counts as before that merge and left `git status` clean, which is the evidence that batch #493 moved no count of this measurement.** **The maintainer ruled four questions on 2026-08-14, and this round records every one.** **Split T1 — the library reads the TCP header that an ICMP error message quotes.** #494 built it, `internal/parser/icmp_quoted.go` holds the decode path, and the change closes 31 deviations on `ssh2.pcapng`. **Split T2 — the library publishes the stored four-part JA4TS value on a reset of a connection that holds one SYN-ACK.** #495 built it, and the change closes 3 comparisons and opens 1. **The JA4SSH counter reset — the library keeps the behavior it holds today**, which is candidate answer 2, and the register records 12 declines. **Cause 4 of the JA4SSH cluster — a register decline and no code change**, under row 3 of `.claude/rules/parity.md` `## Where a difference comes from`, and the register records 2 declines. **#484 is the reversal path of the first three, and #491 is the reversal path of the fourth.** **Two readings also reach the record, and neither one is a ruling.** FR-parity-44 of `docs/specs/features/08-python-parity.md` governs a client reset, and #502 holds that question. FR-parity-42 authorizes the entry for `browsers-x509.pcapng/119/JA4TS.1`, and #503 holds it. **The project manager settled three questions that reach the record, and no one of them is a ruling.** Candidate answer 1 of #501 is a design decision, and it reverses at #501. The decline of #492 from this batch is a scope decision. **The two writing decisions of this round are below.** **One run of `make conformance` on `issue/512-epic-441-round` at `74c8827` produced every count of this row.** That run reads the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. It reports 1753 matches, 278 deviations that the register does not hold, 575 accepted deviations and 595 register keys. The same run reports 184 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **`575 + 20 = 595`, so the accepted comparison count equals the register key count.** **The run rewrote no byte of `docs/audit/conformance.md`**, so the tracked report already described the branch head, and this round adopted no figure from a member report. **`dev` reported 1680 matches, 367 deviations, 558 accepted deviations and 578 register keys before this batch.** **73 comparisons closed, 1 opened, and 16 moved from an unaccepted deviation to an accepted one.** `1680 + 73 = 1753`. The 73 are 36 of #489, 3 of #490, 31 of #494 and 3 of #495. **The 1 that opened is `browsers-x509.pcapng/119/JA4TS.1`**, and the register accepts it under ruling #503. **The register gained 17 entries**, so `578 + 17 = 595` and `558 + 17 = 575`. **16 of the 17 name a comparison that the run reported as an unaccepted deviation before this batch**, and `367 - 73 - 16 = 278`. **The plan comment of #512 states that 17 moved from unaccepted to accepted, and 16 did.** The seventeenth entry accepts the comparison that #495 opened, so it moved nothing out of the unaccepted count. **The 17 read 14 under ruling #484, 2 under ruling #491 and 1 under ruling #503.** **The unaccepted uncovered value count rose by 12**, from 172 to 184. #489 predicted 11 and #490 predicted 1, and the two predictions compose exactly. **#501 closed 134 `cipher: message authentication failed` lines, and not one of them was a deviation.** 124 of the 134 name `tls-handshake.pcapng`, `tls3.pcapng` and `ssh2.pcapng`, three captures that no issue of this batch connected to the defect. **That is the largest thing this batch found that no count column of this row holds.** **This round repaired five findings that a member reported and no member owned.** **Finding 1 — FR-parity-43 read `A RST on a connection that holds no delay produces no value.`, and the split T2 ruling contradicts it.** The requirement now states that such a reset produces the stored four-part value, it names 2026-08-14 and it names #484 as the reversal path. **FR-parity-43a is new**, and it states that a reset which reaches no stored connection produces no value. The `## Edge cases & failures` table of that file now names FR-parity-43a for the reset before any SYN-ACK, and it gains one row for the reset of a one-SYN-ACK connection. **Finding 2 — `docs/audit/ja4t-ja4ssh-ja4s-deviation-cluster.md` carried stale whole-run counts and one false heading.** Its preamble now states that neither run of 2026-08-13 describes the tree today, and it names the five figures a reader must not carry forward. **A new `## The merged tree of the Epic #441 batch, 2026-08-14` section holds every re-measured figure**, per method, per cause and per register ruling. **The `### The reference agrees, so this cause needs no ruling` heading of cause 4 is false, and it now reads `### The reference is not unanimous, and the maintainer ruled this cause`.** Only the Wireshark plugin publishes `c20s12_c18s21_c10s1`; `testdata/foxio/python/sshv1.pcap.json` holds one stream entry with `ssh_extras` and no JA4SSH value, and the corpus holds no Zeek vector and no Rust vector for that capture. **The cause 4 count it closes read 2, and it now reads 0** with the label `Register`. **Finding 3 — the cause 2 count it closes read 6, and #495 measured 3.** 3 of the 6 frames are client resets of `https3-301-get.pcap`, FR-parity-44 declines a client reset, and #502 holds that question. **Finding 4 — `docs/audit/ja4x-deviation-cluster.md` carried six falsified counts, and this round re-measured every one.** The key-kind table reads 8 and 3 against 34 and 16. The direction table reads 11 and 0 against 47 and 3. The capture table names 2 of its 6 captures. **The unaccepted uncovered JA4X count reads 59, and #489 predicted 58**, because #490 adds `badcurveball.pcap/0/JA4X_r.2` on top of the 11 that #489 adds. **The uncovered totals read 204 and 71 against 192 and 59**, and 57 of the 59 unaccepted keys name `JA4X_r`. **The 12 accepted JA4X keys and the 12 JA4X register entries under ruling #375 hold unchanged**, and this round re-measured both. **Finding 5 — the word `decline` named two concepts, and the `## Terms` table held one of them.** **This round widened the row so that it names the second meaning and where that meaning lives**, and it converted no line of #501. The row now states that a reader which returns no value and no error for an input of the other role is a separate meaning, that `docs/specs/features/05-conformance-gaps.md` FR-gaps-27 carries that sense, and that the meaning writes no register entry. **Three rows of the same table already carry a second meaning in that shape**: `capability`, `grant` and `multi-packet method`. **The alternative repair added a term and converted every `decline` of #501**, and the `Do not use` column declines `reject`, `skip` and `ignore`, so no obvious replacement survives. **A row that gave one word two mechanisms with no separation would breach rule 6 of `.claude/rules/ste.md`, and a row that names the second meaning and its home does not.** **The second writing decision keeps a table row that reads 0.** The direction table of `docs/audit/ja4x-deviation-cluster.md` collapses to one row on the merged tree, and the `the two values differ` row now reads 0. **A row that states a measured 0 separates a direction that no deviation reaches from a direction that nobody measured**, and category 6 of `.claude/rules/cross-member-review.md` protects that distinction. **The `## Summary` table of `docs/audit/conformance.md` keeps a zero row for the same reason.** **This round found one breach that no member reported, and it repaired it.** `docs/audit/ja4x-deviation-cluster.md` wrote `yield` on two lines, and the `## Terms` table declines that word in the `Do not use` column of the `emit` row. **`docs/audit/ja4t-ja4ssh-ja4s-deviation-cluster.md` states that same rule about its own prose, and the sibling page broke it.** Both lines now read `the count it closes`. **The trap of batch #493 repeated in a different word**, and round 47 records the `verdict` instance of it. **The round that curates the vocabulary is the round most likely to breach it.** **This round converted the prose of the 23 files that this batch and this round opened**, under `.claude/rules/ste.md` `## This project writes US English`. **The 23 hold 38 British spellings, and this round converted 24.** They are 33 of `behaviour`, 4 of `acknowledgement` and 1 of `labelled`. **14 survive by rule.** 2 are the `## Behaviour rules` heading of `docs/specs/features/05-conformance-gaps.md` and of `docs/specs/features/08-python-parity.md`, and 17 feature files carry that heading, so a partial conversion breaks a citation. **The other 12 sit inside a code span of this file**, and each one either quotes that heading or names the word as the object of a measurement. **A conversion of one of the 12 would destroy the measurement that round 45 and round 46 above record.** **Every remaining British spelling of the 23 files sits inside a code span or in that heading**, measured after the conversion. **`CHANGELOG.md` holds 14 of the 24**, `docs/audit/findings.md` holds 2, `docs/specs/features/05-conformance-gaps.md` holds 2, `docs/specs/features/08-python-parity.md` holds 3 and this file holds 3. **No Go file that this batch touched holds a British spelling**, and `.golangci.yml` states `locale: US` for every Go file. **No guard of this repository failed under the conversion, and this round repaired none.** `rule_file_policies_test.go` holds `theBehaviourWord`, and it reads `CLAUDE.md`, which this batch does not touch. **Test-driven development does not apply to a documentation round**, and this round invented no test. **This round moves no fingerprint value, and it changes no Go file.** `testdata/deviations.json` is unchanged under it: #484, #501 and #503 wrote every entry of this batch, and the register was closed to this round. **The four counts read the same before this round and after it.** **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 278 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **The green `conformance` check of a pull request is no conformance gate while Epic #441 is open.** `.github/workflows/ci.yml` runs `make conformance 2>&1 | tee conformance.log || status=$?` and never exits on that status, and a later step fails the job on a `--- FAIL:` line that does not name `TestConformance`. **278 deviations still sit outside the register**, and the two largest clusters are JA4H and the JA4L pair. **#442 and #443 already read both**, and neither reading has a fix issue. **This round describes the tree that its own commit produces.** Recorded by the Epic #441 batch. |
| 49 | 2026-08-14 | **Batch #516 merges four members and one round, and #522 is this round.** #508, #510, #171 and #351 are the four members. **The batch moves no fingerprint value.** #508 deleted two dead nil guards, and it stated and tested the non-nil contract of `ja4t.go` and `ja4ts.go`. #510 wrote `docs/audit/gopacket-tcp-option-panic.md`, and it added a guard that no production file calls `DecodeFromBytes` directly. #171 split FR-gaps-17 into FR-gaps-17 through FR-gaps-17c, and it added two guards. #351 built `issue_citation_namespace_test.go`, which holds `.claude/rules/rulings.md` `## A citation names its repository` over the whole tree, and FR-reference-32 through FR-reference-53 number it. **This round repairs three findings, and policy 1 of `CLAUDE.md` sends each one here rather than to an issue.** **Finding 1 — round 19 above wrote a bare number, and that number names `Crank-Git/ja4plus#594`.** This round writes the explicit form of the citation, and it changes no measurement of that row. **#351 held the breach under one entry of `issueCitationException`, and it repaired no line of `docs/specs/spec.md`**, because #512 owns that file on the `epic/441-deviation-fixes` branch. **This round owns the file on this branch, so it carried the repair and it removed the entry.** The table is empty today. `TestTheIssueCitationExceptionTableHoldsNoBreachThatNoLongerReproduces` fails on an entry whose count moves either way, so the guard itself holds the removal. **Finding 2 — the `ruling` row of the `## Terms` table bars the word `decision`, and `docs/audit/gopacket-tcp-option-panic.md` wrote it.** The page read `it makes no decision`, and it now reads `it rules nothing`. The next sentence of that page already names the maintainer, so the repair states no new rule. **Three rounds before this one wrote a word that the `Do not use` column bars.** Round 47 above wrote `verdict`, `docs/audit/ja4x-deviation-cluster.md` wrote `yield`, and round 48 wrote `decisions`. **The round that curates the vocabulary is the round most likely to breach it.** So this round read every row of the `## Terms` table against its own prose before it committed, and it found no barred word. **Finding 3 — this round found 6 British spellings in the 10 files that batch #516 touched, and it converted 4.** `.claude/rules/rulings.md` held 1 use of `behaviour` and this round converted it. `docs/specs/features/05-conformance-gaps.md` held 3 and this round converted 2. `docs/specs/features/11-foxio-reference.md` held 2 and this round converted 1. **The 2 it left are the heading `## Behaviour rules`**, and `.claude/rules/ste.md` bars a partial conversion of that heading. `docs/audit/gopacket-tcp-option-panic.md` held none, and no Go file of the batch held one. **This round converted no file outside the 10 that batch #516 touched**, which the maintainer requires. **`git grep -l -i behaviour | wc -l` counts the files that hold the word.** It reports 32 on `origin/dev`, 32 on the head this round forked, and 31 after this round. `.claude/rules/ste.md` states 35 and it names commit `6681d3e`, so this round leaves that sentence and it records the later count here. **One conversion is a shared table header cell, and it is not the shared heading.** `| Case | Expected behaviour |` headed a table in 7 feature files before this round, and `docs/specs/features/11-foxio-reference.md` now reads `| Case | Expected behavior |`. **So 6 feature files hold the British form of that cell today.** **No file cites that cell.** The heading rule of `.claude/rules/ste.md` rests on a citation that a partial conversion breaks. **No citation of the cell exists.** So the incremental rule converts the prose of the file this batch opened. **One maintenance cost reached this round, and #351 named it.** `issueCitationHighWaterMark` read 520, and this round re-measured it as 522 with the command that the guard records. `gh api '/repos/Crank-Git/ja4plus-go/issues?state=all&per_page=1' --jq '.[0].number'` reported 522 on 2026-08-14. **This round cites #522, which is its own issue, and the inherited mark of 520 sits below that number.** **The mark is a lower bound, so a stale mark loosens the guard and it never reddens it.** A reader who meets that failure reads a stale constant, and never a real breach. **No document of the tree cited a number above 520 before this round.** The port citation of round 19 above was the one number the guard reported. This session created #521 and #522. **One question reached this round, and the maintainer holds the answer.** **The tree wrote an anaphoric port citation 19 times, in 15 files, on the head this round forked.** `ja4d.go:123` is one of them: `// reading. The port decided it as D4 of its issue #231 on 2026-08-08.` `git grep -o -i -E "its issues? #[0-9]+" | wc -l` reported 19 on that head. `git grep -l -i -E "its issues? #[0-9]+" | wc -l` reported 15. **The quotation above matches that pattern, so the command reports 20 after this round.** The file count holds at 15, because this row sits in a file that already held three of the 19. **One of the 19 is a test case of the guard itself**, in `issue_citation_namespace_test.go`. **`.claude/rules/rulings.md` `## A citation names its repository` names three forms, and the tree uses a fourth.** Every one of the 19 sits at or below the mark, so the guard reports none of them. **Whether the rule admits an anaphoric possessive is the maintainer's question, and this round repaired no line of the 19.** A repository-wide conversion is the change the maintainer already declined for the English question. **One of the 19 sits in `internal/parser/http.go`**, which #510 owns in this batch. **Two citations of this batch answer to a live branch that `dev` does not hold.** **#510 cites issue #494, and it cites no path.** `internal/parser/icmp_quoted.go` of #494 lives on `epic/441-deviation-fixes`, and `origin/dev` holds no file of that name. A path citation is therefore a broken cross-reference on this branch, and an issue number resolves before that branch merges and after it. Comment 5288850708 of #516 holds the measurement. **#351 wrote the entry of `issueCitationException` for the same reason**, because #512 owns `docs/specs/spec.md` on that branch. **This round writes round 49, and round 47 above is the highest round this branch inherited.** **`epic/441-deviation-fixes` carries round 48**, and that branch merges into `dev` before this batch does. **The project manager repairs this table when the two rounds collide.** **The counts of that branch read a different tree, and no figure of this round comes from it.** **This round changes no Go file that holds behavior, and it changes no byte of `testdata/deviations.json`.** It changes one Go test file, and it changes the constant and the empty table of `issue_citation_namespace_test.go` alone. **The four counts read the same before this round and after it**: 1680 matches, 367 deviations, 558 accepted deviations and 578 register keys. The same run reports 172 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **So this round moves no fingerprint value.** **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 367 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **One measurement of this round names a way to read a green gate that no run holds, and it corrects no round above.** `rtk` shortens what a command writes to a session, and it wrote the line `[exited with code 0]` under a `make conformance` run of this branch. `rtk proxy make conformance` reported the exit code 2 on the same tree, and the run holds `--- FAIL: TestConformance`. **So a session that reads that one line and no exit code reads a green gate**, and this round read the exit code. Recorded by batch #516. **The project manager merged `dev` into this branch on 2026-08-14 and it resolved two textual conflicts, and neither one is semantic.** This table now holds rounds 47, 48 and 49 in order, so the sentence above that names round 47 as the highest round this branch inherited reads the fork point and never the merged tree. `docs/specs/features/05-conformance-gaps.md` held the second conflict, in the `## Edge cases & failures` table: this branch added no row and `dev` added two QUIC rows, and both rows are kept. **The four counts of this round read the fork point of this branch, and the merged tree reads 1753 matches, 278 deviations that the register does not hold, 575 accepted deviations and 595 register keys.** The same run reports 184 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **Those are the figures `origin/dev` reports at `5d29a94`, so the merge moved no count and the property this batch claims holds.** `go test -race ./...` exits 0, `make lint` reports 0 issues, and `make cover` reports 77.7% against a floor of 75.0. **`make conformance` exits 2, and the 278 open deviations are the reason.** **The maintainer ruled the anaphoric port citation on 2026-08-14: the rule admits the form under a same-sentence antecedent constraint, and no line of the 19 is repaired.** #525 carries the rule and the guard, and it is the reversal path. Recorded by the batch #516 gate. **The cross-member review of this batch found one defect, and the project manager repaired it here.** The sentence above states that `git grep -l -i behaviour | wc -l` reports 31 after this round. **That figure is true at `31c265f`, which is the commit of this round, and the merged head reports 29.** The merge commit `43b19d4` brought in the copies of `CHANGELOG.md` and `docs/audit/findings.md` that `dev` had already converted, and each of those two files held the word at `31c265f`. **So the round moved 1 file and the merge moved 2 more, and no member of this batch converted either one.** **`.claude/rules/ste.md` states 35 and it names commit `6681d3e`**, so that sentence reads an older tree and this row records the later count. Recorded by the batch #516 gate. **The gate note above cites `#525`, and that citation reddened the guard this batch built.** #351 wrote `issueCitationHighWaterMark`, the round re-measured it as 522, and the gate note then cited a number above it. **The `test`, `coverage` and `conformance` jobs of CI each failed on the two guard tests**, because `issue_citation_namespace_test.go` sits in the root package and all three jobs run it. **The conformance job failed on the `--- FAIL:` line that is not `TestConformance`, which is the one gate of that job.** The gate re-measured the mark with the command the guard records and it read 530, and the constant now holds 530. **The guard reported a real breach of its own rule, and the repair is the one the failure message names.** Recorded by the batch #516 gate. |
| 50 | 2026-08-14 | **Batch #530 merges seven members and one round, and #541 is this round.** #484, #168, #370, #502, #533, #532 and #376 are the seven members, and they are `5c2f852`, `70f8a4a`, `adbd42a`, `8eab728`, `d10891b`, `aaf5bc3` and `0cff90a`. **#371 is parked, and it wrote no line of the tree.** **This round files no issue**, under policy 1 of `CLAUDE.md`. **The batch changes three Go files that hold behavior, and it moves no fingerprint value.** #370 gave JA4D6 the relay descent, #533 gave `ja4.go` a `dropConnection` call on the client hello error path, and #532 gave `ParseQUICInitial` one completeness rule. **#484 and #502 wrote every register entry of this batch**, #168 bounded every crypto reassembly path, and #376 wrote `docs/audit/per-packet-ja4l-absence.md` and no Go file. **One run of `make conformance` on `issue/541-batch-530-documentation-round` produced every count of this row.** That run reads the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. It reports 1753 matches, 273 deviations that the register does not hold, 580 accepted deviations and 600 register keys. The same run reports 184 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **`580 + 20 = 600`, so the accepted comparison count equals the register key count.** **The fork point of this batch reported 1753, 278, 575 and 595**, and round 49 above records those four figures. **The register gained 5 entries**, so `595 + 5 = 600`, `575 + 5 = 580` and `278 - 5 = 273`. **2 of the 5 carry the ruling #42 and #484 wrote them, and 3 carry the ruling #502 and #502 wrote them.** **The match count holds at 1753 through every member**, so 5 comparisons moved from an unaccepted deviation to an accepted one, and no comparison opened or closed. **Three member reports quote the figures of their own fork point, and this round quotes no one of them.** #168 reports 278, 575 and 595, and #370 and #376 each report 276, 577 and 597. **Every figure of this row comes from a run of the merged tree.** **This round repairs four findings and it records three, and policy 1 of `CLAUDE.md` sends every one of them here.** **Finding 1 — the register row `Subfield 1 of JA4D6 on a relay message` held three false statements, and #370 falsified each one.** The row read `Part b, part c and subfield 2 of part a read no option of the inner message, and the port reads them at any nesting depth.`, and this library now reads all three at every depth under a bound of 32. The row read `Done. No change to the code.`, and #370 changed `ja4d6.go`. The row stated that a one-level relay and a two-level relay give one value, and `TestJA4D6ReadsTheInnerMessageOfARelayMessage` reads `rlayf0014nn_9-1-6_23-24` for the first and `rlayf0014nn_9-9-1-6_23-24` for the second. **The cell also cited `ja4d6.go:138-141`, and this round names `ProcessPacket` of `ja4d6.go` instead**, under `.claude/rules/ste.md` `## How a citation names its target`. **Subfield 1 still writes the outer message type alone**, which `ProcessPacket` reads from `dhcp.MsgType`, so that half of the row holds. **#370 wrote no line of this file**, because the cross-check of this batch gives that file to one member, and comment 5289550995 of #530 records the finding. **Finding 2 — the register grew twice in this batch, so the identity needs the merged figures.** `580 + 20 = 600` above is that restatement, and no figure of it comes from a fork point. **Finding 3 — the maintainer ruled the anaphoric port citation on 2026-08-14.** `.claude/rules/rulings.md` `## A citation names its repository` admits the anaphoric possessive under a same-sentence antecedent constraint, and **no line of the 19 is repaired**. **#525 carries the rule and the guard, and it is the reversal path.** The gate note of round 49 above records the same ruling, and this round adds no repair of the 19. **Finding 4 — the JA4D6 change is wider than the title of #370.** The four subfield options now also read the `IA_NA`, `IA_TA`, `IA_PD`, IA Address and IA Prefix containers, and not option 9 alone. The register row states it, and `TestJA4D6ReadsASubfieldOptionOfANestedContainer` holds it. **Finding 5 — the count of #376 is wrong, and the member measured it rather than adopting it.** The body of #376 states 24 uncovered per-packet JA4L and JA4LS values on two captures. **The run reports 36, across 11 captures.** `CVE-2018-6794.pcap` holds 6, at frames 2, 3, 11, 12, 20 and 21. **`tls-handshake.pcapng` holds 0**, because that capture carries no SYN-ACK and this library therefore produces no JA4L value on it. **Finding 6 — #376 found a category 6 defect in its own page, and it repaired the page before the page landed.** The first draft wrote that the port register holds five JA4L entries across three captures. **That sentence counted the entries which support the argument and reported them as the whole register.** The measured truth is 21 JA4L entries across six captures, of which five record the absence of a key across three. Commit `e9d0b5c` separates the two counts. **The round that curates the counts is the round most likely to breach them**, and round 49 above states the same rule for the vocabulary. **Finding 7 — 30 of the 36 values stay unexplained, and #543 holds them.** Each one reaches the point C of the dissector, so the cause of the 6 is not the cause of the 30. **The 6 of `CVE-2018-6794.pcap` earn a value decline under ruling #196**, which the maintainer made on 2026-08-12, and never under ruling #361, whose 8 entries state a per-stream reason. **This round writes no register entry**, so the register holds no entry for the 6 today. **This round writes two requirements that #532 earned, and that member wrote no line of the file.** `docs/specs/features/05-conformance-gaps.md` `### QUIC reassembly` gains FR-gaps-31 and FR-gaps-32. **FR-gaps-31 states the completeness rule.** `ParseQUICInitial` reads the client hello of one datagram through `ClientHelloFromCryptoFragments`, and that function refuses a handshake message that the fragments do not cover from offset 0 with no gap. `ReassembleCryptoFrames` writes a zero byte over a gap, so a reader without the rule reports a cipher list that no client sent. **FR-gaps-32 states one behavior difference that no requirement named.** A handshake message of 1 to 3 bytes reached `ParseClientHello` under the reader that #532 replaced, and `ParseQUICInitial` then returned `ClientHello truncated: too short for version`. **It now returns no client hello and no error.** `ja4s.go` is the one caller that reaches the path and it discards the error, so no figure moved and nothing else carries the difference. **`TestParseQUICInitialProducesNoErrorForAHandshakeMessageOfThreeBytes` reddens when a later change reverses it**, and #532 is the reversal path. **The `## Edge cases & failures` table of that file gains one row for each of the two requirements.** **One writing question reached this round, and this round parks it.** `internal/parser/quic.go` writes `the completeness check` in the doc comment of `ClientHelloFromCryptoFragments`, and `internal/parser/quic_initial_completeness_test.go` writes `the completeness rule` for the same rule. **Rule 7 of `.claude/rules/ste.md` bars a synonym rotation, so one of the two forms is wrong.** **The `## Terms` table gains no row for the word.** A row names one form and bars the other, this round owns no Go file of #532, and a row would therefore leave a rule that the tree breaches on the day it lands. **The two requirements above write `completeness rule`**, which is the form the tests of #532 use. **One maintenance cost reached this round, and the guard reported it before this round wrote a line.** `issueCitationHighWaterMark` of `issue_citation_namespace_test.go` read 530, and this round re-measured it as 543 with the command that the guard records. `gh api '/repos/Crank-Git/ja4plus-go/issues?state=all&per_page=1' --jq '.[0].number'` reported 543 on 2026-08-14. **The batch merged `dev` into its integration branch at `d9deea8` after six members had landed**, so the guard that #351 built reached those commits only at that merge. **It reported 8 bare citations above 530, and 6 name #532 and 2 name #533.** They sit in `internal/parser/quic.go`, `internal/parser/quic_initial_completeness_test.go`, `ja4.go` and `quic_crypto_reassembly_test.go`. **Every one of the 8 names an issue of this repository, so the re-measurement is the whole repair.** `TestNoBareIssueCitationNamesANumberAboveTheHighWaterMark` and `TestTheIssueCitationGuardRecordsItsSourceAndReadsNoTracker` were both red on the base of this round, and both are green under it. **This round cites #532, #533, #541 and #543, and each number sits above the inherited mark of 530.** **The brief of this round stated that the guard is present and reads 530, and #376 reported that no file of the branch held the identifier while it ran.** Both sentences are true of two commits, and the merge at `d9deea8` sits between them. **This round read every row of the `## Terms` table against its own prose before it committed, and it found three barred words.** **It wrote `owner` for a member of a batch, and the `maintainer` row bars that word.** The sentence now gives the file to one member. **It wrote `copy` for the copy loop of `ReassembleCryptoFrames`, and the `transcription` row bars that word.** The sentence now names the loop. **It wrote `sweep` for its own read of the table, and the `sweep` row names one run of the mutation tool.** The sentence now names the read. **The three words above sit inside a code span, because this row names each one as the object of a measurement.** **Round 47, round 48 and round 49 above each record one such breach**, and this row records three. **This round converted no British spelling, because the two files it opens hold none that the rule reaches.** `docs/specs/features/05-conformance-gaps.md` holds 1, and it is the `## Behaviour rules` heading that `.claude/rules/ste.md` bars a partial conversion of. **`docs/specs/spec.md` holds 22, and every one sits inside a code span of a round above.** Each of the 22 quotes that heading or names the word as the object of a measurement, so a conversion would destroy the measurement that round 45, round 47, round 48 and round 49 record. **#371 is parked, and the maintainer holds the question.** It asks whether part a of JA4D reads the domain name inside option 81 or the presence of option 81. **Two stop conditions fire together**: the implementations differ, and the image differs from one of them. `zeek/ja4d/main.zeek:73` tests `options?$client_fqdn` and writes `d`, and `wireshark/source/packet-ja4.c:1521` tests `dhcp.fqdn.name` and writes `n`. **R16 of `docs/specs/foxio/JA4D.md` records the image label `Has a Domain name (d) or No domain (n)`.** **No capture of the corpus carries an option 81 at all**, and the program of #371 reported `captures=37 dhcpv4_messages=4 option81_occurrences=0 option81_no_name=0`. **So both candidate answers move zero fingerprint values today, and the answer still binds a live capture.** **The cause sits in the DHCP decoder of each program and never in its JA4 code**, and `.claude/rules/external-apis.md` pins neither the Wireshark core dissectors nor the Zeek analyzer. **#371 records that gap, and this round writes no line of that file.** **Two port findings left this repository, and `Crank-Git/ja4plus#611` holds them.** This round repairs no line of the port. **#168 found a live panic that no line of its own issue body names.** An offset of `1<<64-1` wraps the `uint64` sum, and the copy loop reported `slice bounds out of range [18446744073709551615:1]`. `cryptoFragmentPassesBound` tests the offset before it adds the length. **#168 also corrected the premise of its own issue**: the old guard read `totalLen > 1<<20`, the crafted allocation measured exactly 1048576 bytes, and the guard passed it. **This is the second panic on untrusted input that this project found in two batches**, and #510 recorded the first one. **#533 closes no part of #432, and the member stated that plainly.** The three QUIC tables still hold the entries of an incomplete connection until a caller calls `CleanupConnection`, and #432 asks for a bound on entry count and entry age. **#532 measured that the port holds no completeness rule on its own initial parse.** `ja4plus/utils/quic_utils.py:584` tests the length and the handshake type alone, at tag `v1.1.0`. **So this library is stricter than the port on that path, and no shared vector separates the two.** **#370 disclosed one incident.** It ran a `git checkout <sha> -- .` that reverted its own working tree. Every change was already committed, and `git reset --hard HEAD` inside its own worktree restored the tree. **No stash was written, and no file outside that worktree was touched.** **This round changes no Go file that holds behavior, and it changes no byte of `testdata/deviations.json`.** It changes one Go test file, and it changes the constant and the comment of that constant alone. **The eight figures read the same before this round and after it.** The two runs are the run of `d9deea8` and the run of this commit, and `docs/audit/conformance.md` is byte-identical after both. **`go test -race ./...` exits 0**, `make lint` reports 0 issues, and `make cover` reports 80.8% against a floor of 75.0. **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 273 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **This round describes the tree that its own commit produces.** Recorded by batch #530. |
| 51 | 2026-08-14 | **Batch #536 merges five members and one round, and #552 is this round.** #544, #449, #233, #543 and #527 are the five members, and they are `6b47541`, `996b6c7`, `2498ddf`, `683e08e` and `873048c`. Pull request #547 carried #544, #549 carried #449, #550 carried #233, #551 carried #543 and #548 carried #527. **#163 is parked, and it wrote no line of the tree.** **This round files no issue**, under policy 1 of `CLAUDE.md`. **The batch was formed as the first batch of the session whose members change what the library answers, and no member changed one.** **Three members reached a reference split and each one parked**, so the parks are the result of this batch and never a failure of it. **#527 parked rule 5b of JA4H.** The four FoxIO implementations split two against two on part b of an empty header list, and `docs/audit/ja4h-deviation-cluster.md` `### Rule 5b needs a ruling` states each reading. **#449 parked the coalesced QUIC datagram.** `wireshark/source/packet-ja4.c:969` and `wireshark/source/packet-ja4.c:1408` read every QUIC packet of a datagram, and `python/ja4.py:403-404` and `rust/ja4/src/time/udp.rs:278` read the first packet alone. **#543 parked one question of six values.** `docs/audit/per-packet-ja4l-thirty-uncovered.md` `## The one question for the maintainer` asks whether ruling #126 reaches JA4L, and it states three candidate answers and recommends no one of them. **#163 parked a dependency question**, because an RFC 8879 compressed certificate needs a brotli decoder and the maintainer holds that question. **#544 and #233 are the two members that changed a tracked file of the library.** #544 wrote 6 register entries under ruling #196, and it moved the counts of `CHANGELOG.md` and the report `docs/audit/conformance.md`. #233 wrote `ja4h_retransmitted_capture_test.go`, and `ja4h.go` changes no line. #449, #543 and #527 each wrote a reading and no `.go` file. **One run of `make conformance` on `issue/552-batch-536-documentation-round` produced every count of this row.** That run reads the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. It reports 1753 matches, 273 deviations that the register does not hold, 580 accepted deviations and 606 register keys. The same run reports 178 unaccepted uncovered values, 26 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **`580 + 26 = 606`, so the accepted comparison count equals the register key count.** **The fork point of this batch reported 1753, 273, 580 and 600**, and round 50 above records those four figures with 184 unaccepted uncovered values and 20 accepted uncovered values. **The register gained 6 entries**, so `600 + 6 = 606`, `184 - 6 = 178` and `20 + 6 = 26`. **The match count holds at 1753 and the unaccepted deviation count holds at 273**, so 6 values moved from an unaccepted uncovered value to an accepted one, and no comparison opened or closed. **All 6 are the per-packet JA4L and JA4LS values of `CVE-2018-6794.pcap`**, at frames 2, 3, 11, 12, 20 and 21, and #544 wrote them under ruling #196. **This round repairs three findings and it records three, and policy 1 of `CLAUDE.md` sends every one of them here.** **Finding 1 — `conformance_skip_marker_test.go` read `Three untagged tests`, and the tree holds six.** The count was already stale at five before this batch, and #233 added `ja4h_retransmitted_capture_test.go` as the sixth. ``grep -rln 'is absent, so run `make corpus` to fetch the FoxIO corpus' --include='*.go' .`` reports 7 files on this branch, and `conformance_test.go` is the one of the seven that carries the `conformance` build constraint. **The comment now states the count and the date, and it quotes that sentence nowhere.** A first draft of the comment held the command, and the quoted sentence then made `conformance_skip_marker_test.go` the eighth file the command reports. **A comment that quotes the sentence it counts changes the count it states**, and this row holds the command instead. The argument the comment makes holds at any count. **`TestTheCIWorkflowCountsTheConformanceTaggedFiles` counts the tagged files and never these**, so no guard was red. **Finding 2 — `conformance_scope_check_test.go` named `the second, third and fourth request values` of `CVE-2018-6794.pcap`, and each one is a retransmission value.** Each HTTP connection of that capture carries one request and five TCP retransmissions of it. **The 12 and the test itself are both correct, and the prose was the whole defect.** The comment now names the retransmission values, and it cites #233 and `ja4h_retransmitted_capture_test.go`. **Finding 3 — `docs/audit/per-packet-ja4l-absence.md` stated a register state that #544 moved.** The page read `The register already holds 35 per-packet JA4L entries under ruling #196` and `That is why the six sit outside the register while their 35 siblings sit inside it.`, and `testdata/deviations.json` holds 41 entries whose `ruling` reads `#196` today. **The page now names the 35 as the state of its own base**, it states the 41, and it names #544 as the member that wrote the six. **A third sentence reads `So the six values earn a value decline under ruling #196, and this page writes none.`, and that sentence stays true about the page.** The page now names the pull request that wrote the entries, so a reader reads it as the reading that earned them and never as open work. **Finding 4 — a repeated request of a FoxIO capture is usually a TCP retransmission, and `tshark` hides it from both references.** `testdata/foxio/reference/python/ja4.py:598` reads `if x['hl'] in ['http', 'http2']:`, so a retransmitted segment reports no HTTP highest layer. `testdata/foxio/reference/wireshark/source/packet-ja4.c:1634` reads `if (http_req != -100) {`, and `testdata/foxio/reference/wireshark/source/packet-ja4.c:1135` matches the field name `http.request.method`, and `testdata/foxio/reference/wireshark/source/packet-ja4.c:1149` sets `http_req` from that field. **The same trap reaches JA4 and JA4S, because a retransmitted ClientHello fails the same test.** **A member that reads a count difference against a per-stream vector counts the requests first, and it reads no candidate as a reference rule before that count.** #233 holds the measurement, and `ja4h_retransmitted_capture_test.go` records it beside the count. **Finding 5 — the body of #527 states that the FoxIO implementations agree on rule 5b, and they split two against two.** The project manager wrote that sentence from a reading of the reference Python and the Wireshark dissector alone, and `testdata/foxio/reference/rust/ja4/src/lib.rs:184` and `testdata/foxio/reference/zeek/utils/common.zeek:64` each write the zero sentinel for the empty string. **A claim that the FoxIO implementations agree needs all four implementations read**, and #548 repaired the same sentence of `docs/audit/ja4h-deviation-cluster.md` before this round ran. **Finding 6 — the body of #552 cites `d2b4c0a` for the squash of #544, and `git cat-file -t d2b4c0a` reports `fatal: Not a valid object name d2b4c0a`.** The squash of #544 on this integration branch is `6b47541`. **A commit hash of a member branch dies at the sub-merge**, and this row cites the squash of each member instead. **The same table of #552 records #527 with no squash**, and pull request #548 sub-merged a reading of #527 at `873048c`. **One maintenance cost reached this round, and #351 named it.** `issueCitationHighWaterMark` of `issue_citation_namespace_test.go` read 543, and this round re-measured it as 552 with the command that the guard records. `gh api '/repos/Crank-Git/ja4plus-go/issues?state=all&per_page=1' --jq '.[0].number'` reported 552 on 2026-08-14. **This round cites #544, #547, #548, #549, #550, #551 and #552, and each number sits above the inherited mark of 543.** Every one names an issue or a pull request of this repository. **This round read every row of the `## Terms` table against its own prose before it committed, and it found no barred word.** **The `part e` row of that table bars `retransmission part` as a name for part e of a JA4TS value, and no row of the table states the word `retransmission` itself.** This round adds no row for it, because 10 Go files of the tree already write the word with one meaning, and this round writes no barred form of it. **This round converted no British spelling.** **The 11 files that this batch opens outside `docs/specs/spec.md` hold none.** **The rows above this one hold 23 uses of `behaviour`, and every one of them sits in a row of this table.** Round 52 replaced a line range here, because #527 added two rows to the register above and moved every line of this table. Each of the 23 quotes the `## Behaviour rules` heading or names the word as the object of a measurement, so a conversion would destroy the measurement that round 45, round 47, round 48, round 49 and round 50 record. **This row states the count of the rows above and it states no total**, because a row that names the word raises the total it would state. **This round changes no Go file that holds behavior, and it changes no byte of `testdata/deviations.json`.** It changes two Go test comments, one constant, one audit page and this table. **The eight figures read the same before this round and after it**, and `docs/audit/conformance.md` is byte-identical after the run. **`go build ./...` exits 0**, `go test -race ./...` passes 1221 tests, `make lint` reports `0 issues.`, and `make cover` reports 80.8% against a floor of 75.0. **`make conformance` exits 2, and Epic #441 owns that exit.** The run reports 273 deviations that the register does not hold. **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **This round describes the tree that its own commit produces.** Recorded by batch #536. |
| 52 | 2026-08-14 | **Batch #555 merges five members and one round, and #561 is this round.** #371, #543, #537, #528 and #527 are the five members, and they are `f00c883`, `05a7566`, `7156c65`, `6cfe20a` and `69cc872`. Pull request #557 carried #371, #559 carried #543, #560 carried #537, #563 carried #528 and #575 carried #527. **The batch merged `origin/dev` at `2d675e7`**, after batch #536 landed there. **This round files no issue**, under policy 1 of `CLAUDE.md`. **This is the first round of session 15 that records a moved fingerprint value**, and #527 moved the one value. **The maintainer ruled four questions on 2026-08-14, and each ruling landed with a member.** **#527 — JA4H part b hashes an empty header list to `e3b0c44298fc`, and it writes no zero sentinel.** R28 of `docs/specs/foxio/JA4H.md` records the ruling, `TruncatedHashNoSentinel` of `internal/parser/hash.go` carries it, and `Crank-Git/ja4plus#612` holds the port half. **#527 also made the request line read a path that holds a space.** `requestLineRe` of `internal/parser/http.go` now holds a lazy group of any character in place of the non-space group. **The two rules close one comparison together, and neither one closes it alone.** **#371 — the domain name inside DHCP option 81 decides part a of JA4D, and the presence of the option decides nothing.** `ja4d.go` states the ruling and `ja4d_option81_domain_name_test.go` builds the separating packet. The corpus holds 4 DHCPv4 messages and no option 81, so no vector separates the two answers and no value of the corpus moved. `Crank-Git/ja4plus#615` holds the port half. **#543 — the SYN bit test of ruling #126 reaches JA4L.** R36 of `docs/specs/foxio/JA4L.md` records the ruling, and #543 wrote 6 value declines. **#528 — JA4L point C is the last QUIC server handshake packet, and the per-packet vector set reads the first one.** `docs/audit/ja4l-deviation-cluster.md` `### The ruling of 2026-08-14` records the ruling, and #528 wrote 3 value declines. **#537 wrote no ruling, no register entry and no fingerprinter line.** It recorded the upstream version that the FoxIO pin implies for each host: the Wireshark core at `v4.6.0` and the Zeek analyzer at `8.0.0`. `docs/audit/upstream-versions.md` holds the measurement, `upstream_versions_test.go` fails when a moved pin leaves a row behind, and `.claude/rules/external-apis.md` gained a row for each host. **The string `4.4.2` appears in no file of the FoxIO tree at the pin**, and the port names `Release 4.4.2` in its own interface table. **That is a two-repository disagreement and not a falsified sentence of this repository**, so this round repairs no line for it. **One run of `make conformance` on `issue/561-batch-555-documentation-round` produced every count of this row.** That run reads the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. It reports 1754 matches, 267 deviations that the register does not hold, 585 accepted deviations and 617 register keys. The same run reports 175 unaccepted uncovered values, 32 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **`585 + 32 = 617`, so the accepted comparison count equals the register key count.** **`origin/dev` reports 1753 matches, 273 deviations, 580 accepted deviations and 606 register keys**, with 178 unaccepted uncovered values and 26 accepted uncovered values, and the `## [Unreleased]` preamble of `CHANGELOG.md` on that branch states those six figures. **So the batch raised the match count by 1, it lowered the unaccepted deviation count by 6, and it added 11 register entries.** #543 wrote 6, #528 wrote 3 and #527 wrote 2. **The run rewrote no byte of `docs/audit/conformance.md`**, so the tracked report already described this head. **#527 raised the unaccepted uncovered count by 3, and no reading of this project predicted that.** The per-stream vector file `testdata/foxio/python/gre-erspan-vxlan.pcap.json` holds `JA4L-S` and `JA4L-C` for stream 0, and it holds no JA4H key. So the library's `gre-erspan-vxlan.pcap/0/JA4H`, `/JA4H_r` and `/JA4H_ro` reach no vector. **The JA4H value of that stream equals the per-packet vector of frame 4, so the three record a coverage difference of the FoxIO Python harness and never a value disagreement.** #527 wrote no register entry for them, because the register records an accepted difference from a FoxIO value and no FoxIO value exists for those three keys. **This round searched the tree for a document that states otherwise, and it found none.** `git grep -n gre-erspan-vxlan -- docs/ '*.go' CHANGELOG.md` names every place that reads the capture, and `docs/audit/ja4h-deviation-cluster.md` is the one that states the cause. **A member that writes a register entry moves a `CHANGELOG.md` count, and it needs no moved fingerprint value to do so.** The cross-check of this batch predicted that #527 alone would redden `TestTheChangelogPreambleStatesTheCountsTheTreeProduces`, and #543 disproved that prediction by running the gate. `changelog_counts_freshness_test.go` reads the register key count and the per-ruling enumeration, so any member that writes one entry reddens it. **The next cross-check reads the guard rather than the value.** **This round repairs the six findings of the batch #536 cross-member review, and every one of them still held at this head.** **Finding 1 — `docs/audit/ja4h-deviation-cluster.md` cited `python/ja4h.py:72` for the header hash in two places.** At the pin `:72` holds `cookies = sha_encode(x['cookie_fields']) if 'cookies' in x else '0'*12` and `:76` holds `headers = sha_encode(x['headers'])`. The page now cites `:76`, and it names `:72` as the cookie fields. R18 and R27 of `docs/specs/foxio/JA4H.md` already stated that separation. **Finding 2 — round 51 above joined a clause to a complete sentence with a dangling `and`.** The sentence is now two sentences, and both measurements survive. **Finding 3 — `conformance_skip_marker_test.go` counted tests and prescribed a measurement of files.** ``grep -rln 'is absent, so run `make corpus` to fetch the FoxIO corpus' --include='*.go' .`` reports 7 files on this branch, and `conformance_test.go` is the one of the seven that carries the `conformance` build constraint. **The comment now counts files, which is the noun its own measurement produces.** It also names the reason the two nouns differ: `tunnelSkipWithoutCapture` of `tunnel_keys_test.go` writes the sentence for 5 callers, so more tests write it than files hold it. **Finding 4 — the same page counted nine `TruncatedHash` call sites outside part b, and the root package holds ten.** `ja4hHashCallSites` of `ja4h_empty_header_list_test.go` names eleven call sites in all, and part b is the eleventh. `TestEveryTruncatedHashCallSiteOfTheRootPackageIsNamed` reads the syntax tree rather than a line number, so it is the measured truth. **Finding 5 — the same page cited `packet-ja4.c:628` for the unguarded hash.** At the pin `:628` holds `    wmem_strbuf_t *display = wmem_strbuf_new(scope, "");`, and the hash sits at `:629-630`. The page now cites `:629-630`, and `:641` was already correct. **Finding 6 — round 51 above cited `packet-ja4.c:1134` for the `http.request.method` match.** At the pin `:1134` is a comment and `:1135` holds the match. The row now cites `:1135`. **The review declined to settle one question, and this round settles it by measurement.** `conformance_scope_check_test.go` kept `CVE-2018-6794.pcap/0/JA4H` as its worked example of surplus per-stream values. **The example is historical, and the comment now says so.** `segmentCarriesNoNewRequest` of `ja4h.go` drops each retransmitted segment, so the library emits one JA4H value per HTTP connection of that capture, and `docs/audit/conformance.md` holds no `CVE-2018-6794.pcap/0/JA4H.2` key. **The comment also gains the live example that #233 named.** `testdata/foxio/python/http2-with-cookies.pcapng.json` holds 16 entries for stream 0, and 15 of them hold a JA4H key, so the harness numbers each one from `http2-with-cookies.pcapng/0/JA4H.1`. **This round repaired two more sentences that this batch falsified, and neither one reached the review.** **The doc comment of `TruncatedHashNoSentinel` stated that JA4H part b is the one caller**, and `TruncatedHash` in the same file calls it for a non-empty input. The comment now names both callers. **Round 51 above placed its 23 uses of `behaviour` on a line between 1193 and 1204**, and #527 added two register rows above this table and moved every line of it. The row now names the table rather than a line range, under the citation convention of `.claude/rules/ste.md`. **This round converted no British spelling.** The 5 files it opens outside `docs/specs/spec.md` hold none. **The rows above this one hold 25 uses of `behaviour`, and every one of them quotes the `## Behaviour rules` heading or names the word as the object of a measurement**, so a conversion would destroy the measurement that round 45, round 47, round 48, round 49, round 50 and round 51 record. `git grep -o -i behaviour -- docs/specs/spec.md` produced that count on 2026-08-14, and this row states no total, because a row that names the word raises the total it would state. **One maintenance cost reached this round, and #351 named it.** `issueCitationHighWaterMark` of `issue_citation_namespace_test.go` read 552, and this round re-measured it as 583 with the command that the guard records. `gh api '/repos/Crank-Git/ja4plus-go/issues?state=all&per_page=1' --jq '.[0].number'` reported 583 on 2026-08-14. **This round cites #555, #557, #559, #560, #561, #563 and #575, and each number sits above the inherited mark of 552.** Every one names an issue or a pull request of this repository. **This round changes no line that holds behavior, and it changes no byte of `testdata/deviations.json`.** It changes four Go comments, one constant, one audit page and this table. **The eight figures read the same before this round and after it**, and `docs/audit/conformance.md` is byte-identical after the run. **`go build ./...` exits 0**, `go test -race ./...` passes 1243 tests, `make lint` reports `0 issues.`, and `make cover` reports 80.8% against a floor of 75.0. **`make conformance` exits 2, and Epic #441 owns that exit.** `conformance_test.go` fails the run on any deviation that the register does not hold, and the run reports 267. **The count fell from 273, so a non-zero exit here records no regression.** **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable, and this round reports no green gate.** **This round describes the tree that its own commit produces.** Recorded by batch #555. |
| 53 | 2026-08-14 | **The Epic 9 batch merges four members and one round, and #581 is this round.** #72, #73, #74 and #75 are the four members, and they are `5d3fad7`, `91bb6ce`, `a7ea9bf` and `8004b2d`. Their pull requests are #562, #570, #571 and #578. **The epic moves no fingerprint value, and it writes no register entry.** **One run of `make conformance` on `issue/581-epic-9-documentation-round` produced every count of this row.** It reports 1753 matches, 273 deviations that the register does not hold, 580 accepted deviations and 600 register keys. The same run reports 184 unaccepted uncovered values, 20 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **Round 50 above records the same eight figures**, so no comparison of this epic moved. **The base of this row predates round 51 and round 52 above, and each of those two rounds moved figures of the eight.** **`epic/71-database-lookup` merged `origin/dev` twice, and the second merge states the figures a reader uses.** The first merge re-measured them on 2026-08-14 and it read 1753 matches, 273 deviations that the register does not hold, 580 accepted deviations, 178 unaccepted uncovered values, 26 accepted uncovered values, 606 register keys, 0 stale register entries and 0 orphan register entries. **Batch #555 then landed on `origin/dev` before this epic merged, and #527 of that batch moved one fingerprint value.** The second merge re-measured every figure on 2026-08-14, and one run of `make conformance` on the merged tree reports 1754 matches, 267 deviations that the register does not hold, 585 accepted deviations, 175 unaccepted uncovered values, 32 accepted uncovered values, 617 register keys, 0 stale register entries and 0 orphan register entries. **585 and 32 add up to the 617 register keys.** **No arithmetic on the two earlier rows produces these eight figures**, because one side of the merge holds an uncovered value where the other side holds a deviation. **A reader reads the eight merged figures here, and never the 600 register keys of this row.** **`make conformance` exits 2, and Epic #441 owns that exit.** Step 4 of the `## A change is done when` list of `CLAUDE.md` is therefore unmeetable, and this round reports no green gate. **`go build ./...` exits 0.** `go test -race ./...` reports 1267 tests passed in 6 packages. `make lint` reports `0 issues.` `make cover` reports 81.4% against a floor of 75.0. **This round adds no Go statement.** So it moves the coverage total by nothing that a measurement separates. **What the epic built.** #72 moved `RemoteLookupConfig` and `LookupFingerprintRemote` to the new package `github.com/Crank-Git/ja4plus-go/ja4db`, under the maintainer's ruling of 2026-08-14 that comment 5294420180 of #72 holds. `LookupResult`, `LookupFingerprint`, `CachedDatabasePath`, `DatabaseInfo` and `GetDatabaseInfo` stayed in the core package. **The core package imports no HTTP client**, and `network_boundary_test.go` holds five tests that fail on an import which breaks the rule. #73 met FR-lookup-8 through FR-lookup-17 inside `ja4db`, and it added no exported name. #74 replaced `sync.Once` with one `atomic.Pointer[lookupTable]`, and it removed the package-level variables `lookupDB`, `dbSource` and `dbCachePath`. #75 created `internal/dbcache`, which validates a downloaded database, holds the 16 MB bound of FR-lookup-25 and writes the cache file atomically. **`go list ./...` reports 6 packages, and the module held 4 before this epic.** **This round repairs eleven falsified sentences, and it files no issue for one**, under the filing policy of `CLAUDE.md`. **Repair 1 — four documents stated that the lookup path uses `sync.Once`, and #74 listed five.** The list this round re-derived with `grep -rn 'sync\.Once\|lookupOnce\|dbSource\|dbCachePath' --include='*.md' .` holds four, because #74 repaired the fifth itself. **The fifth is `.claude/rules/concurrency.md`**, whose `## The contract` section now states that one `atomic.Pointer` holds the table. The four are these. `docs/specs/spec.md` `### Lookup database`. `docs/specs/features/09-database-lookup.md` `## Purpose`. `docs/specs/features/02-correctness-audit.md` under S2 and under S3. `docs/audit/findings.md` at the suspected findings table, at F-24-13 through F-24-15 and at `### F-23-14`. **Every repair of an audit record writes the past tense and keeps the measurement.** An audit row records what its issue measured, and a rewrite of that row destroys the evidence. **Repair 2 — the `### DatabaseInfo` table named `Records` and `Updated`, and the code has never held either name.** The code holds `Entries` and `ModTime`. **#74 and #75 each found the difference and each left the code alone**, because #100 freezes the exported API at `v1.0.0`. **So this round repaired the table and no line of `lookup.go`**, and the page names #100 as the reversal path. **Repair 3 — no page of `docs/specs/` named `internal/dbcache/`.** The `### Files` table of `docs/specs/features/09-database-lookup.md` now names ten files, and it named five. **`lookup_remote.go` no longer exists.** The ruling moved the remote lookup to a package rather than behind a build tag, and the table stated the build-tag condition. **Repair 4 — the `## Layout` table of `CLAUDE.md` described the core package as the holder of the database lookup.** #72 and #75 had already added the `ja4db/` row and the `internal/dbcache/` row. **So this round repaired one cell**, and the root row now names the local database lookup. **The `internal/capture/` row of that table is wrong, and this round leaves it.** Epic 13 creates that package, and the round of Epic 13 owns the row. **Two rounds that repair one row is how a repair gets reverted.** **Repair 5 — the `### Components` table of this file named neither new package.** It gains a `Remote lookup` row and a `Database cache` row. **The `Public library` row now states that the core package imports no HTTP client.** **Repair 6 — Q1 of `docs/specs/features/09-database-lookup.md` was open, and the maintainer closed it on 2026-08-14.** `docs/audit/network-boundary.md` records the ruling, the three options and the reason. **Repair 7 — the corrupt-cache row of the `## Edge cases & failures` table named a field that no type holds.** The row stated that the library records the reason in `DatabaseInfo`. **That type holds no field for a reason.** The row now states what `GetDatabaseInfo` reports, which is the source `embedded`. **`TestTheLookupTable_FallsBackToTheEmbeddedCopyWhenTheCacheIsCorrupt` holds it.** The behavior rule above the table carried the same claim. It now separates the process that holds a previous table from the process that holds none. **Repair 8 — FR-lookup-21 named `dbSource`, which no line of the tree holds.** It now names the source, and a paragraph under FR-lookup-27 names the fields that replaced the two variables. **FR-lookup-19 still names `sync.Once`, and this round leaves it.** That requirement states what the library replaces, so it reads true, and `TestTheLookupSource_HoldsNoSyncOnce` cites it. **Repair 9 — `### F-23-14` of `docs/audit/findings.md` gave the reload to #75, and #74 built it.** That section also stated that `ja4plus db update` prints a restart note, and #74 removed the note from `cmd/ja4plus/main.go`. **Repair 10 — `docs/audit/dependency-decision.md` recorded that `go test -race ./...` passes in each of the four packages.** The row now dates that measurement to 2026-08-13, and a paragraph beside it states the six packages of today. **Repair 11 — `issueCitationHighWaterMark` of `issue_citation_namespace_test.go` read 543.** `gh api '/repos/Crank-Git/ja4plus-go/issues?state=all&per_page=1' --jq '.[0].number'` reported 581 on 2026-08-14, and the constant now reads 581. **This round cites #573, #577 and #581, and each number sits above the inherited mark.** **This round changes one Go file, and it changes the constant and the comment of that constant alone.** No `.go` file of this round changes behavior, and this round weakens no guard. **The 5 tests of `network_boundary_test.go`, the 6 tests of `lookup_reload_test.go` and the 14 tests of `internal/dbcache/dbcache_test.go` each stand unchanged.** The command is `grep -c '^func Test' <file>`. **This round reports one item and repairs it not.** The `Reason` cell of F-24-13, of F-24-14 and of F-24-15 gave the `unconfirmed` status a reason that the tree no longer holds. **Five tests of `lookup_reload_test.go` now reach the loader with a crafted cache file.** **A documentation round sets no audit status.** So the new section `## What Epic 9 changed under S2, S3 and F-24-13 through F-24-15` of `docs/audit/findings.md` names each test. It leaves the three statuses to the maintainer. **Two items of this epic already carry an issue, and this round repairs neither.** #573 holds the per-lookup `os.Stat` cost. The doc comment of `activeTable` records it as 6.6 ns before #74 and 614 ns after it, on macOS 25.6.0 on 2026-08-14. #577 holds the age question that needs a maintainer ruling, and it reaches the port. **This round converted six British spellings, in one file it opens.** They sit in `docs/audit/dependency-decision.md`, and each one is the word `behaviour` in that file's own prose. **`.claude/rules/parity.md` still writes the British form of the same word**, and a later round that opens that file converts it. **The two `## Behaviour rules` headings of the files this round opens stay**, which `.claude/rules/ste.md` `## This project writes US English` requires. **This round read its own prose against the `## Terms` table, and it found one barred word.** It wrote `owner` for an issue, and the `maintainer` row bars that word. The sentence now gives the reload to #75. **Round 50 above records three breaches of the same class**, and the round that curates the vocabulary is the round most likely to breach it. **A self-review lens reported twelve sentences above the 25-word limit of rule 1 of `.claude/rules/ste.md`, and this round split every one.** **This round describes the tree that its own commit produces.** Recorded by the Epic 9 batch. |
| 54 | 2026-08-14 | **The Epic 6 batch merges four members and one round, and #602 is this round.** #44, #45, #46 and #47 are the four members, and they are `b50f218`, `7cc7847`, `7a0d450` and `584df71`. Pull request #554 carried #44, #576 carried #45, #591 carried #46 and #600 carried #47. **This round is numbered 54 because rounds 52 and 53 exist on `origin/dev` and not on this integration branch.** The `docs/specs/spec.md` of `epic/43-fuzz-testing` ends at round 51, and the `docs/specs/spec.md` of `origin/dev` ends at round 53. **So the batch merge into `dev` brings rows 52 and 53 above this row**, and this round takes the next free number rather than the next visible one. **The batch moves no fingerprint value.** No member changed a line of any file that the library already held, so `testdata/deviations.json` gains no entry and `CHANGELOG.md` moves no count. **`git diff --stat origin/dev...` names one file that is not a test file, and that file is new: `internal/fuzzprop/fuzzprop.go`.** Ten `*_fuzz_test.go` files import it and no other file does, so no line of the library reaches it. **One run of `make conformance` on `issue/602-epic-6-documentation-round` produced every count of this row**, and it reads the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. It reports 1753 matches, 273 deviations that the register does not hold, 580 accepted deviations, 178 unaccepted uncovered values, 26 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **Every one of those figures equals the figure of the fork point**, because no member of this batch reads or writes a value the conformance suite compares. **`make conformance` exits 2, and Epic #441 owns that exit**, so step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable on this branch and this round reports no green gate. **This row read `Epic 5` until batch #617 repaired it.** Epic 5 is closed, its members are 37, 38, 39, 40, 41 and 42, and 441 is none of them. Round 50, round 51, round 52 and round 53 each name Epic #441. **The batch built 13 fuzz targets, five properties, 29 seed files and two CI paths.** **The tree holds 30 seed files, and the merged diff of this batch adds 29.** The 30th arrived on `dev` with the #556 hotfix at `ce2a296`, which added the repair and its crashing seed together. **Batch #617 repaired this count**, which read 30 for the batch and disagreed with the hotfix sentence three sentences above it. `grep -rn --include='*_test.go' '^func Fuzz' .` reports 15 targets in 12 files, and 13 of the 15 in 10 of the 12 are new. **The two the tree already held are `FuzzNoExportedFunctionPanicsOnAnyFrame` in `audit_panic_test.go` and `FuzzTCPOptionEntriesReadsAnyOptionRegion` in `ja4t_option_byte_count_test.go`**, and no member seeds either one. **`make fuzz` exits 0 on this branch, and it ran all 15 targets for 30 seconds each in about 8 minutes.** **The stall of #568 did not reproduce in that run.** **One member found a live panic in released code, and that is the result of this batch rather than a defect of it.** `FuzzParseServerHelloReadsAnyPayload` panicked in 1.14 seconds on its first run, on a record that declares a `supported_versions` length of 2 or more and that holds no extension data. **#556 repaired `internal/parser/tls.go` and merged to `dev` separately**, at `ce2a296`. **That one commit added the repair and the crashing seed together**, so no commit of this repository holds the seed and no repair, and the `## Behaviour rules` list of `docs/specs/features/06-fuzz-testing.md` now records that reading. **This round repairs twelve findings and it reports four, and policy 1 of `CLAUDE.md` sends every repair here.** **Finding 1 — the `## Purpose` section of `docs/specs/features/06-fuzz-testing.md` read `The library holds nine parser files. None has a fuzz target.`, and both halves are false.** `ls internal/parser/*.go` less the test files reports 13 files, and seven of them carry a fuzz target in a file beside them. **The section now states what the epic built, and it states the command that measured each count.** **Finding 2 — FR-fuzz-13 names a tunnel decoder that no line of the tree holds.** The `## Data touched` table of `docs/specs/features/05-conformance-gaps.md` names `internal/parser/tunnel.go` as a new file that holds the GRE, ERSPAN, VXLAN and Geneve decoders, and `ls internal/parser/tunnel*` reports `tunnel_test.go` and `tunnel_fuzz_test.go` alone. `isTunnelLayerType` in `internal/parser/packet.go` states the reason: `gopacket` registers a decoder for each tunnel, so this package adds none. **#44 wrote one target for the decapsulation code the tree does hold, and it invented no decoder.** **This round repairs the page this epic opens, and it edits `docs/specs/features/05-conformance-gaps.md` nowhere**, because that page belongs to another feature. **Finding 3 — FR-fuzz-15 is subsumed for one read and not for the other, and the page stated neither.** A read at or above the length panics, so FR-fuzz-14 catches it. **A read above the length and at or below the capacity panics for nothing**, and it returns the bytes that follow the input in memory. The fuzz engine hands the target the mutator's scratch slice at `internal/fuzz/mutator.go:113`, and `internal/fuzz/mutator.go:106` gives that slice a capacity of `maxPerVal` bytes, so the second read is reachable. **`ExactInput` of `internal/fuzzprop` returns a copy whose capacity equals its length**, which turns that read into a panic. **Finding 4 — FR-fuzz-17 names no mechanism, and the `## Interfaces` table named one that no line of the tree calls.** The table read `runtime.ReadMemStats`, and `grep -rn --include='*.go' ReadMemStats .` reports one comment that names it as the rejected candidate. #45 measured `runtime.ReadMemStats` at 24736 ns for one call and `runtime/metrics.Read` at 230.7 ns, on an Apple M4 at `go1.26.5` at `-benchtime 2000x`, and it took the second. **The table now names `runtime/metrics`.** **Finding 5 — the same table named `actions/github-script` at v7, and neither workflow uses it.** `.github/workflows/fuzz.yml` opens its issue with `gh issue create` and `--body-file`, and `grep -n 'uses:' .github/workflows/fuzz.yml` reports `actions/checkout`, `actions/setup-go` and `actions/upload-artifact` alone. **The table now names `gh issue create`.** **Finding 6 — the `## Data touched` table named two files that no member created.** It read `internal/parser/fuzz_test.go` and `fuzz_test.go`, and the tree holds neither. **#44 wrote one target file beside each source file instead**, which keeps a target beside the code it drives. The table now names the 13 files the batch created, and it names the member that wrote each one. **Finding 7 — FR-fuzz-19 names one seed corpus directory, and the tree holds two.** Go reads `testdata/fuzz/<TargetName>/` under the directory of the package that holds the target, so the root seeds live at `testdata/fuzz/` and the parser seeds live at `internal/parser/testdata/fuzz/`. **Go states the location, and no member chose it.** **Finding 8 — FR-fuzz-25 states that the fuzz job runs on every pull request, and this project runs CI once for each batch.** #47 gave the job no trigger of its own, so it takes the `pull_request` filter `[main, master, dev]` at the head of `.github/workflows/ci.yml`. **A member pull request targets an integration branch that no filter names, and every member commit ends with `[skip ci]`.** So the batch pull request into `dev` is the first run of the job. **Finding 9 — the nightly fuzz workflow starts no run until it reaches the default branch, and no page stated that.** The default branch is `master` and this epic merges to `dev`. The documentation states `This event will only trigger a workflow run if the workflow file exists on the default branch.`, verified against <https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows>, retrieved 2026-08-14. **A reader who watches the Actions page of `dev` for a nightly run sees nothing, and that is the documented behavior rather than a defect.** `.github/workflows/fuzz.yml` carries a `workflow_dispatch` trigger beside the schedule. **Finding 10 — `JA4PLUS_SEEDGEN=1` flips the seed corpus guard from compare to write, and `testdata/fuzz/README.md` stated how to set it and never stated where not to.** A CI job that sets it rewrites every seed to match whatever the code now does, and the guard can then never fail. **The rule now sits in `testdata/fuzz/README.md`, which is the file a person who changes a seed reads first**, and it sits in `docs/specs/features/06-fuzz-testing.md` beside the requirements it protects. **#47 had already built both checks**, and neither one is weakened: each fuzz job refuses a run that carries the variable, and `TestNoWorkflowSetsTheSeedGenerationVariable` and `TestEachFuzzJobRefusesASeedCorpusWrite` in `fuzz_job_test.go` hold them. **Finding 11 — `.github/workflows/fuzz.yml` wrote `sweep` five times for a fuzz run, and the `## Terms` table binds that word to the mutation tool.** The `sweep` row reads `One run of the mutation tool over a named package set.`, and the `audit` row bars `sweep` as a name for a review. **Rule 6 of `.claude/rules/ste.md` states one word, one meaning.** The five uses now read `nightly fuzz run`, which is the term the error messages of `fuzz_job_test.go` already used. **No test of this repository reads the word `sweep`**, so the repair weakens no guard. **This round adds six rows to the `## Terms` table.** `fuzz target`, `seed`, `seed corpus`, `property` and `nightly fuzz run` are new, and the `sweep` row now states that the word names a mutation run alone. **Epic 6 introduced five domain words and added none to the table**, which is the defect the six rows repair. **The three findings this round reports and does not repair are counts in a merged pull-request body.** `.claude/rules/cross-member-review.md` `## What a pull-request body is` states that a merged body is a record of what its author measured, and that the round writes the true number instead. **Finding A — the body of #600 states `It reported 91 passing fuzz seed subtests over the 15 targets.`, and the merged tree reports 109.** `go test -count=1 -run '^Fuzz' -v ./...` exits 0 and prints 109 lines that match `^    --- PASS: Fuzz`, over 15 top-level targets and with no failure. **`109 - 91 = 18`, and 18 is exactly the seed subtest count of `FuzzNoExportedFunctionPanicsOnAnyFrame`.** So the count of #600 reads 14 targets and its sentence names 15. **Finding B — the body of #576 states `The 97 seed replays pass`, and it measured a branch that held no file of #46.** #45 and #46 ran in parallel, so a seed count of either branch describes neither the other branch nor the merged result. **A count of seed subtests names the target set and the command, or it names nothing a reader can reproduce.** **Finding C — the doc comment of `FuzzParseServerHelloReadsAnyPayload` in `internal/parser/tls_fuzz_test.go` read `issueCitationHighWaterMark ... holds 543`, and the constant read 568.** The comment also stated that the target fails until #556 lands, and #556 landed at `ce2a296` before this round ran. **Both sentences are repaired, and the sentence that bars a weakening of the target stays.** **One maintenance cost reached this round, and #351 named it.** `issueCitationHighWaterMark` of `issue_citation_namespace_test.go` read 568, and this round re-measured it as 602 with the command that the guard records. `gh api '/repos/Crank-Git/ja4plus-go/issues?state=all&per_page=1' --jq '.[0].number'` reported 602 on 2026-08-14. **This round cites #591, #596, #600 and #602, and each number sits above the inherited mark of 568.** Every one names an issue or a pull request of this repository. **This round files no issue.** **Three issues were filed before it and stay open: #568 holds the unreproduced stall of `FuzzParseCryptoFramesReadsAnyPayload`, #596 holds the segment count that is bounded only through the byte bound, and #577 holds a timestamp eviction that needs a maintainer ruling.** Each one touches behavior, so policy 1 of `CLAUDE.md` earns it an issue and keeps it out of this round. **This round converted one British spelling.** `docs/specs/features/06-fuzz-testing.md` held one use of `behaviour` in prose at `## Interfaces`, and the `## Behaviour rules` heading of the same file stays under the rule of `.claude/rules/ste.md`. **This round weakens no guard.** #45 built five properties and ten tests that prove each one fires, #46 built a guard that fails on any seed no test builds, and #47 built three workflow guards. **The 15 targets, the 109 seed subtests and the 15 tests of `fuzz_job_test.go` all pass after this round.** **Finding D — the body of #600 states that 13 tests of `fuzz_job_test.go` pass, and `grep -c '^func Test' fuzz_job_test.go` reports 15.** A first draft of this row adopted the 13 from that body, and the round's own correctness review re-measured it. **A round that adopts a count from a body repeats the defect it exists to find**, and every count of this row now names the command that produced it. **Finding 12 — #46 wrote the barred word `verdict` in two doc comments and in `testdata/fuzz/README.md`, and `audit_record_test.go` already records that the `## Terms` table bars it.** The `ruling` row bars `verdict` as a synonym, and the doc comment of `auditSuspectedStatuses` states that the report writes `Status` for that reason **So one member breached a rule that another file of this repository states**, which is the cross-member defect that category 4 of `.claude/rules/cross-member-review.md` names. The four uses now read `result` or `reading`, and no `.go` file of this repository holds the word outside the sentence that names it as barred. **This round changes no Go file that holds behavior.** It changes 9 files: one constant and its doc comment, three test doc comments, five comments of one workflow, one feature page, one corpus README, `CLAUDE.md` and this table. **All four Go files it changes are test files.** **`go build ./...` exits 0**, `go test -race ./...` exits 0, `make lint` reports `0 issues.`, `make cover` reports 82.1% against the floor of 75.0 in `.coverage-floor`, and `make fuzz` exits 0 over 15 targets. **A prose round adds no Go statement, so the coverage total is the total of the base.** **This round describes the tree that its own commit produces.** Recorded by Epic 6. |
| 55 | 2026-08-14 | **The Epic 14 batch merges five members and one round, and #603 is this round.** #84, #85, #86, #87 and #88 are the five members, and their pull requests are #580, #587, #584, #597 and #601. **This round is a merge and a refresh, and it is larger than an ordinary round.** The epic forked from `dev` at `4d3db94`. Epic 9, batch #555 and batch #432 each landed after that fork. **So the site described a tree that no longer exists.** **The first merge of `origin/dev` reached one conflict, and it is `issue_citation_namespace_test.go`.** The epic read 595 and `dev` read 583. **A mark is a lower bound, so the resolution keeps both narratives and it re-measures the mark.** **The second merge of `origin/dev` reached four conflicts**, and they are `CLAUDE.md`, `docs/audit/network-boundary.md`, `docs/specs/spec.md` and `issue_citation_namespace_test.go`. `dev` had taken Epic 6 and batch #432 since the first merge, and the branch was 38 commits behind. **The `CLAUDE.md` resolution keeps both sides**: the `make fuzz` target count of Epic 6, and the sentence of this epic that names the `docs` target. **The `docs/audit/network-boundary.md` resolution keeps the reason this epic measured**, because `rebuildTable` and `updateCache` in `lookup.go` are the two callers of `CachedDatabasePath` and `loadDB` calls it never. **The mark met 604 and 602, and it keeps 604.** `gh api '/repos/Crank-Git/ja4plus-go/issues?state=all&per_page=1' --jq '.[0].number'` reported 604 on 2026-08-14, and this round cites #603. **The first merge renumbered no changelog row, and no citation moved.** Round 51, round 52 and round 53 each came from `dev` alone, and no member of this epic wrote a round. **The second merge of `origin/dev` reached the row-number collision that the first merge did not.** Epic 6 landed on `dev` after the first merge, and its round also wrote a row 54. **This row is 55, and the row of Epic 6 keeps 54**, because the Epic 6 row published on `dev` first. **No citation of the tree moved**, because `grep -rn -i 'round 5[0-9]'` reports citations of round 50, round 51, round 52 and round 53 alone, and it reports no citation of round 54 or of a higher row. **One run of `make conformance` on `issue/603-epic-14-merge-and-refresh` produced every count of this row.** That run reads the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. It reports 38 captures, 1754 matches, 267 deviations that the register does not hold, 585 accepted deviations and 617 accepted comparisons. The same run reports 175 unaccepted uncovered values, 32 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **`585 + 32 = 617`, so the accepted comparison count equals the register key count.** **`make conformance` exits 2 on this head, and Epic #441 owns that exit.** The run reports 267 deviations that the register does not hold. **So step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable**, and this round reports no green conformance gate. **Round 50 above records the same exit on `dev`**, and the exit is the state this round inherits rather than a state it creates. **`docs/audit/conformance.md` is byte-identical after that run**, so the merge resolved the generated file to the value the tree produces. **`testdata/deviations.json` holds 617 entries and 617 unique keys**, so the merge produced no duplicate key. **This round writes no byte of that file.** **This round changes no Go file that holds behavior.** It changes one Go test file, and it changes the mark constant and the comment of that constant alone. **The round repairs nine pages of the site, and every repair reads the merged tree.** **Epic 9 moved the remote lookup to `github.com/Crank-Git/ja4plus-go/ja4db`.** `RemoteLookupConfig` and `LookupFingerprintRemote` live there, and `LookupResult`, `LookupFingerprint`, `CachedDatabasePath`, `DatabaseInfo` and `GetDatabaseInfo` stay in the core package. `docs/reference/index.md`, `docs/reference/types.md` and `docs/usage.md` each named the pre-move surface, and each one now names the package. **`docs/reference/types.md` links a moved name to `https://pkg.go.dev/github.com/Crank-Git/ja4plus-go/ja4db`, and never to the core anchor.** **Epic 9 also replaced `sync.Once` with an `atomic.Pointer` snapshot, and no page of the site ever named `sync.Once`.** `docs/concurrency.md` gains a section that states the snapshot. **That section states the true property and never a wider one.** `activeTable` in `lookup.go` loads the pointer and returns the snapshot when the cache file is unchanged, and that steady-state read takes no lock. **A read that finds a changed cache file calls `rebuildTable`, and `rebuildTable` takes the mutex.** So the page states that a reader takes the mutex on the path that rebuilds, and it never states that no reader takes it. **Batch #555 moved a JA4H fingerprint value, and three more rulings landed with it.** `docs/methods/ja4h.md` stated that the library writes the zero sentinel in part b, and that the question stays open. **Both sentences are false on the merged tree.** Part b hashes an empty header list to `e3b0c44298fc`, `computeJA4HFromRequest` calls `parser.TruncatedHashNoSentinel`, and issue #527 is the reversal path. **`docs/methods/ja4d.md` stated that the library reads the presence of option 81.** **The maintainer ruled #371 on 2026-08-14**, and the domain name inside the option decides the flag. **`docs/methods/ja4l.md` carried two questions as open, and the maintainer ruled both on 2026-08-14.** A coalesced QUIC datagram does not fill point C. The SYN selection rule of ruling #126 reaches JA4L. **Batch #432 bounded nine state tables, and `docs/implementation-notes.md` named two bounded fingerprinters.** **Seven fingerprinters carry a bound on the merged tree**, and the page now names each one with its entry bound and its idle limit. **JA4T, JA4D and JA4D6 hold no per-connection state**, so none of the three needs a bound. The page also states the 1 MB per-stream bound of the TCP stream reassembler that #595 added. **The merge falsified five register counts of the method pages, and this round re-measured every one.** `docs/methods/ja4h.md` read 103 under `JA4H_r.1` and 109 under `JA4H_ro.1`, and the merged register holds 104 and 110. `docs/methods/ja4l.md` named one ruling for `JA4L.1`, and the merged register holds three: 38 under `#196`, 4 under `#126` and 3 under `#528`. `docs/methods/ja4ls.md` named one ruling for `JA4LS.1`, and the merged register holds 3 under `#196` and 2 under `#126`. **The counts of `docs/methods/ja4.md`, `docs/methods/ja4s.md`, `docs/methods/ja4ssh.md`, `docs/methods/ja4ts.md` and `docs/methods/ja4x.md` each hold on the merged tree**, and this round changed no one of them. **`docs/live-capture.md` stays true, and this round changed no sentence that states the absence.** **Epic 13 has not reached `dev`.** `ls internal/` reports `dbcache/`, `keylog/` and `parser/`, and it reports no `capture/`. `go run ./cmd/ja4plus watch` prints `unknown command: watch`, prints the usage text and exits 1, and `cmd/ja4plus/main.go` holds `--json`, `--csv`, `--types` and `--lookup` and no other option. **So the page describes the tree that built it**, and it describes no branch this tree does not hold. **One section of that page was falsified, and it is the capture filter.** The page recorded the filter as an open question. **The maintainer ruled it on 2026-08-14 under issue #564**, and the pure-Go backend applies no capture filter. `--bpf` names the `libpcap` build tag. **The ruling is a ruling of the project, and it is not a line of this tree.** So the section states the ruling, and it states that no `--bpf` option reaches the program of this branch. **#85 and #86 each declined to describe a build that its tree did not hold.** This round holds the same line. **The `## Layout` row that names `internal/capture/` in `CLAUDE.md` stays untouched**, because the Epic 13 round owns it. **One guard reddened on the first draft of this round, and the round repaired the page rather than the guard.** `TestEveryGoSampleOfTheSiteIsARunnableProgram` requires each fenced Go block of the site to parse as a whole program and to match a program of `examples/` by token sequence. The draft added a four-line fragment to `docs/usage.md`, and the guard reported `docs/usage.md:224 does not parse as a Go file`. **The repair states the call in prose**, and it adds no fenced block. **No guard of this epic is weakened**: `TestNoPublishedPageLinksIntoAnExcludedDirectory`, `TestEveryGoSampleOfTheSiteIsARunnableProgram`, the action-pin test of #88 and `TestNoBareIssueCitationNamesANumberAboveTheHighWaterMark` are each green on this head. **No page of this round links into `docs/specs/` or `docs/audit/`.** **`mkdocs build --strict` reports no such link**, because `path_to_url` of `mkdocs/structure/pages.py:509` caps the message at `INFO`. **The Go guard is the real check**, and this round ran `go test` as well as `make docs`. **The self-review of this round found one falsified sentence, and the round repairs it.** `docs/audit/network-boundary.md` gave the reason for `CachedDatabasePath` as `` `loadDB` of the core package calls it on every load. `` **The merged tree does not hold that.** `activeTable` in `lookup.go` returns the loaded snapshot when the cache file is unchanged, and it then calls `CachedDatabasePath` never. `rebuildTable` and `updateCache` are the two callers. **The cell names those two now.** **Policy 1 of `CLAUDE.md` sends a falsified sentence to the round that finds it, and it files no issue.** **Epic 9 wrote that cell**, and this round opened the file because the merge carried it. **The gate of this epic repaired two defects that publish to the site, and this paragraph records them.** **Defect A is a count of the bounded fingerprinters.** `docs/concurrency.md` stated `Two fingerprinters carry a bound of their own, and neither bound replaces the call.` and `docs/throughput.md` stated `Two fingerprinters carry a bound`. Each page listed JA4SSH and JA4H alone. **The merged tree holds seven**, and `docs/implementation-notes.md` already stated seven. **The gate re-measured the count on the merged tree**, with one `grep -lE` over the ten fingerprinter files of the root package. The pattern matches a bound constant: a name that ends `Connections`, `Streams`, `Handshakes` or `Certificates`, and a name that ends `ConnectionAge`, `StreamAge`, `FragmentAge` or `CleanupInterval`. It reported `ja4.go`, `ja4h.go`, `ja4l.go`, `ja4s.go`, `ja4ssh.go`, `ja4ts.go` and `ja4x.go`, and it reported no other file. **`ja4d.go`, `ja4d6.go` and `ja4t.go` hold no bound**, because each one reads a single packet. **Both pages now state seven, and each one names the bound and the idle limit of every fingerprinter.** **A wrong count here is a wrong belief about memory safety**, so the gate repaired it rather than a later round. **Defect B is a block quote attributed to a file that does not hold it.** `docs/reference/index.md` attributed `` The API reference is hand-written and links to `pkg.go.dev`. Do not add a generator; Go already publishes a canonical generated surface from the same doc comments. `` to `docs/specs/features/14-documentation.md`. **That file states the substance in other words, and it holds no such sentence.** `grep -rn 'Do not add a generator' .` reported the one line of `docs/reference/index.md` and no other line of the tree. `.claude/rules/ste.md` `## What is verbatim, and never rewritten` bars a reworded quotation, because a rewrite destroys the evidence. **The gate dropped the block quote and the attribution, and it states the design in the voice of the page.** It took that repair rather than a verbatim quotation, because the site excludes `docs/specs/`, so a reader of the site reaches no cited file. **The gate re-measured every conformance figure on the merged tree, and each one is unchanged.** The run reports 38 captures, 1754 matches, 267 deviations that the register does not hold, 585 accepted deviations, 175 unaccepted uncovered values, 32 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **`585 + 32 = 617`, and `testdata/deviations.json` holds 617 entries and 617 unique keys.** **`docs/audit/conformance.md` is byte-identical after two runs**, at `18f530243a89d61a9b8d3c6868e6325b`. **`make conformance` still exits 2 on the Epic #441 predicate**, and the merge neither created nor closed that exit. **This round describes the tree that its own commit produces.** Recorded by the Epic 14 batch. |
| 56 | 2026-08-14 | **The Epic 13 batch merges six members and one round, and #614 is this round.** #77, #78, #79, #80, #81 and #82 are the six members, and their pull requests are #569, #579, #586, #591, #599 and #608. **This is the first round of this project to run after its batch's cross-member review.** The maintainer changed the batch order on 2026-08-14, and the reason is that a round which runs before the review repairs nothing the review finds. **#594 is the accumulator that hid that defect for three batches.** **The review reported 15 findings, and the project manager split them under the five-case rule of `CLAUDE.md`.** **Five earned a tracker issue**, and they are #609, #610, #611, #612 and #613. #609 records that the pure-Go backend states Ethernet for every interface. #610 records that a read blocks forever on a quiet interface. #611 records that the audit classification guard reads no file of `internal/capture`. #612 records that `installWatchStopHandler` releases neither the registration nor its goroutine. **#613 is a maintainer ruling about the extent of the word `library`, and this round neither resolves it nor takes a side.** **Ten findings reached this round, and this round re-measured every one against the head it runs on.** That head carries a merge of `dev` at `8c8b198`. **All ten hold at that head, and this round repaired every one.** **Finding 1: the `## Interfaces` table of `docs/specs/features/13-live-capture.md` named `github.com/google/gopacket` at v1.1.19, and `go.mod` requires `github.com/gopacket/gopacket` v1.6.1.** #438 moved the module on 2026-08-13. **The round re-read each constraint at the version the tree requires, rather than carrying the old reading forward.** `gopacket@v1.6.1/pcapgo/capture.go:6` holds `//go:build linux`, and no file of v1.6.1 holds the term `go1.9`. **The same paragraph stated that `gopacket/afpacket` imports C, and no file of that package imports C at v1.6.1.** `gopacket@v1.6.1/afpacket/afpacket.go:7` and `gopacket@v1.6.1/afpacket/header.go:7` each hold `//go:build linux`, so `afpacket` reaches Linux alone. **The design conclusion survives the repair**, because a Linux-only package reaches macOS no better than `pcapgo` does. **Finding 2: the `## Data touched` table placed the statistics line in `cmd/ja4plus/watch.go`, and #81 put it in `cmd/ja4plus/statistics.go`.** The table gains a row for that file, and the `watch.go` row now names the command, the loop, the signals and the connection table. **Finding 3: two members added a stub of `capture.Handle` under two names.** `stubHandle` sits in `cmd/ja4plus/watch_test.go`, and `stubCaptureHandle` sits in `cmd/ja4plus/monitor_test.go`. **This round declined the fold and it wrote the comment**, because the fold is more than a mechanical change. Two facts separate the stubs. `stubHandle` records the close in a `closed` field that `TestWatchInterfaceClosesTheHandleThatItOpens` reads, and `stubCaptureHandle.Close` records nothing. `stubHandle` fails the first read with an error that is not `io.EOF`, and `newStubCaptureHandle` ends a packet list with `io.EOF`. **A fold needs a new field and a second end error on a type that every monitor test drives**, so it rewrites test support rather than moving two declarations. Each stub now names the other. **Finding 4: `monitorConnectionKey` in `cmd/ja4plus/watch.go` reproduces the body of `Processor.GetShardKey` in `processor.go`.** Each one reads `parser.GetGroupingIPInfo`, each one takes the same TCP branch and the same UDP branch, and each one applies the same sort. **They differ in the return shape alone.** **This round wrote a comment at each site and it moved no code**, because a shared helper changes the exported package that #100 freezes. `GetShardKey` is exported and it decides the rule; `monitorConnectionKey` follows it. **The duplication earns no tracker issue**, because it changes no behavior and it breaks no guard. **Finding 5: the feature file named `.goreleaser.yaml`, and this repository holds no such file on 2026-08-14.** `.github/workflows/release.yml` builds the five artifacts, and no build command of that file carries `-tags`. The file row and acceptance criterion 4 each name the workflow now, and criterion 4 records that #105 moves the release to GoReleaser later. **Finding 6: the doc comment of `packetSocketProtocol` in `internal/capture/permission_linux.go` claimed an equivalence that fails on a big-endian host.** The constant computes `0x0300` on every host, and `gopacket@v1.6.1/endian/endian.go:13-18` swaps on a little-endian host alone. **So `endian.Htons(unix.ETH_P_ALL)` reads `0x0003` on `s390x`, `ppc64` and `mips`, and the constant reads `0x0300`.** **The behavior is correct on every host, and this round repaired the comment and not the arithmetic.** `packet(7)` states `protocol is the IEEE 802.3 protocol number in network byte order.`, and it states that a value other than `ETH_P_ALL` selects one packet type. `captureRefusal` reads the errno of `socket(2)`, it binds the socket to no interface, and it closes the socket at once, so it reads no packet of any protocol. **A change of the value is a change of behavior, and this round writes none.** The project manager recorded this finding on #76. **Finding 7: `issue_citation_namespace_test.go` named a TODO comment that no file holds.** `grep -n "TODO" internal/capture/pcapgo_linux.go` reports nothing on 2026-08-14. **The three #564 citations sit at `:42`, `:112` and `:120`**: one ordinary comment inside the unexported `open`, and two lines of the doc comment of `compileFilter`. `.claude/rules/ste.md` `### A code comment` gives `TODO(#N)` a defined form, so a reader who grepped that marker found no target. The sentence now names what the file holds. **Finding 8: `internal/capture/permission_darwin.go` attributed a `man 4 bpf` reading to macOS 15.** `sw_vers` on the host reports `ProductVersion: 26.6.1` and `BuildVersion: 25G76`, and Darwin 25 is macOS 26. **The two quotations of that comment are verbatim correct, and the platform number is the part the tree does not support.** The comment now states 26.6.1 and the build. **The body of pull request #608 carries the same sentence, and this round repairs no merged body.** **Finding 9: the body of pull request #569 states that `internal/capture` exports three names, and the merged package exports four.** `Options`, `Handle`, `Open` and `PermissionDenied` are the four, and `PermissionDenied` sits at `internal/capture/capture.go:96`. **`Handle` also gained a fourth method**, `DropCount() (uint64, bool)` at `internal/capture/capture.go:54`, which #81 added. **The second sentence of that passage is a second defect.** It states that #100 freezes the exported API at v1.0.0. **`internal/capture` is an internal package, so #100 freezes no name of it.** The bodies of #599 and of #608 each state that correctly. **This round states the true count and the true freeze reading, and it repairs no merged body.** **Finding 10: the body of pull request #599 states that the row count of `docs/audit/findings.md` moves from five to six, and the merged file states seven.** `docs/audit/findings.md:112` reads `The table holds seven rows.` **The extra row is `state_bound.go` from #565, which arrived with the merge of `dev` at `8c8b198`.** **The tree is right and the body is the stale record, so this round copies seven and never six.** **The `## Layout` row that names `internal/capture/` in `CLAUDE.md` is the row this round owns outright.** Round 55 above records that three earlier rounds left it alone. It read `Opening a live interface. Holds the pure-Go backend and the libpcap backend.`, and it named neither the unsupported-platform fallback nor the three permission files. **The row now names the fallback and one permission probe for each of Linux, macOS and every other platform.** `ls internal/capture/` reports `capture.go`, `libpcap.go`, `pcapgo_linux.go`, `permission_darwin.go`, `permission_linux.go`, `permission_other.go` and `unsupported.go`, beside six test files. **This round resolves no part of #613 while it edits that row.** **The citation high-water mark moved from 604 to 614.** `gh api '/repos/Crank-Git/ja4plus-go/issues?state=all&per_page=1' --jq '.[0].number'` reported 614 on 2026-08-14. **This round cites #609, #610, #611, #612, #613 and #614, and each number sits above the inherited mark of 604.** **A round that runs after its review allocates six numbers rather than one**, which is the maintenance cost that #351 named. **This round changes exactly one line of Go that is not a comment, and that line is `issueCitationHighWaterMark` in `issue_citation_namespace_test.go`.** `git diff -- '*.go'` reports the pair `-const issueCitationHighWaterMark = 604` and `+const issueCitationHighWaterMark = 614`, and it reports no other line that is not a comment. **Every other Go change is a comment**: the doc comments of `packetSocketProtocol`, `bpfDeviceCount`, `monitorConnectionKey`, `Processor.GetShardKey`, `stubHandle` and `stubCaptureHandle`. **No statement, no signature and no control path moves.** **No fingerprint value moves, and no guard is weakened.** **The mark is a lower bound and this repository allocates upward**, so a re-measurement is the maintenance the guard was built to take, and round 55 above records the same step. **A stale mark loosens the guard and never reddens it**, and this round cites six numbers that the inherited mark of 604 does not cover. **One run of `make conformance` on `issue/614-epic-13-documentation-round` produced every count of this row.** That run reads the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. It reports 38 captures, 1754 matches, 267 deviations that the register does not hold, 585 accepted deviations and 617 accepted comparisons. The same run reports 175 unaccepted uncovered values, 32 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **`585 + 32 = 617`, so the accepted comparison count equals the register key count.** **Every one of the eight figures equals the figure of round 55 above, and `docs/audit/conformance.md` is unchanged after the run.** `git status --short` names no generated file, which is the proof that the round moved no value. **`TestConformance` fails on this head, and it fails on the base.** The failure line reads `the run reports 267 deviations that the register does not hold, and Epic 5 closes them`. **That exit is the state this round inherits, and it is not a state this round creates.** Recorded by the Epic 13 batch. |
| 57 | 2026-08-14 | **The batch #617 round carries three earlier reviews and its own, and #594 is this round.** #596, #568, #583, #577 and #582 are the five members of batch #617, and they are `879f0b1`, `65a12e5`, `ecfa003`, `d5aeafe` and `8497781`. **This round is the one unit of the batch permitted to write a `## Changelog` row**, and every member was told to write none. **This round files no issue**, under policy 1 of `CLAUDE.md`. **No `.go` file of this round changes behavior**, and this round weakens no guard. **It changes a code comment, two test names, one constant, one workflow message, one `.gitignore` entry, seven documents and this table.** **The round carries 19 findings from three earlier reviews and 9 items from the batch #617 review**, and the paragraphs below state each one. **The three earlier reviews are batch #555, Epic 9 and Epic 6**, and each one ran after the round of its own batch, so no round of those batches could carry it. **Two of the 19 do not hold at this head, and this round repairs neither.** **Finding 5 does not hold.** `docs/audit/network-boundary.md` names `rebuildTable` and `updateCache` as the two callers of `CachedDatabasePath`, and `lookup.go` holds exactly those two. The Epic 14 re-merge met that row as an add/add conflict and it kept the corrected reading. **The `internal/dbcache` half of finding 10 is a reading and not a duplication.** The CSV column set and the ja4db.com JSON field set are two keyspaces, and the package doc of `internal/dbcache` now says so. **Finding 1 — one heading counted two closed causes and the section named three.** `docs/audit/ja4h-deviation-cluster.md` `## Three causes are closed since this reading` is the repaired heading, its table gains the row for cause 5, and the three citations of that page move with it. **Round 43 of this table names the old heading `## Two causes are closed since this reading`, and this round does not rewrite round 43**, because that row records what #465 wrote on its own branch. The repaired section states the rename. **Finding 2 — R28 of `docs/specs/foxio/JA4H.md` cited a line that computes no hash.** `wireshark/source/packet-ja4.c:629-630` computes `hash1` and `wireshark/source/packet-ja4.c:641` writes it, measured on 2026-08-14 against the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. **The cluster page of the same member already separated the two**, so one member wrote the right reading on one page and the wrong one on another. **Finding 3 — `docs/audit/per-packet-ja4l-thirty-uncovered.md` asked a question that #543 answered in the same batch that wrote the page.** The page now holds `## The question this page asked, and the ruling of 2026-08-14`, in the shape that `docs/audit/ja4l-deviation-cluster.md` `### The ruling of 2026-08-14` uses: it states the ruling, and a table names each of the four sentences the ruling supersedes. **The reading that earned the ruling stays**, under a heading of its own. **The register holds 6 entries whose `ruling` reads `#126`, and every one carries a JA4L key or a JA4LS key**, measured on 2026-08-14. **Round 51 of this table names the old heading `## The one question for the maintainer`, and this round does not rewrite round 51.** **Finding 4 — row 5 of the cause table carried no closure marker**, and it now names #527 as rows 2 and 3 name their own issues. **Finding 6 — `.claude/rules/concurrency.md` stated that a build takes a mutex that no reader takes.** `activeTable` calls `rebuildTable` when the cache stamp changed, and `rebuildTable` takes `buildMutex`. **The rule file and `docs/specs/features/09-database-lookup.md` now state the property that `docs/concurrency.md` already stated**: the read path is lock-free in the steady state, and it is not lock-free at every call. **Two cross-member reviews raised that sentence**, Epic 9 and Epic 14. **Finding 7 — the update flow named an action that no code takes.** `runDBUpdate` of `cmd/ja4plus` writes the cache file and prints, and it marks nothing. `invalidateLookupTable` of `lookup.go` is unexported and no package outside the root package reaches it. **The flow now names the stat of the cache file**, which is the mechanism `activeTable` uses. **Finding 8 — FR-lookup-17 kept a British spelling, and it now writes the US form.** No other file this round opens holds a British spelling outside the heading exception of `.claude/rules/ste.md`. **Finding 9 — round 53 states 1267 tests and 81.4% coverage.** This round measures 1563 tests and 81.4% on its own head, and it does not rewrite round 53, which closes with the sentence that scopes it to its own commit. **Finding 10 — the `internal/dbcache` package doc claimed one column set for the whole repository.** The doc now names the CSV keyspace, and it names the ja4db.com JSON field set as the second keyspace that `ja4db/lookup.go` reads. **Finding 11 — the doc comment of `issueCitationHighWaterMark` and round 53 each name an inherited mark of 543.** The file already reconciles the two marks it met, 568 and 581, and it keeps the higher. The mark is a lower bound, so no guard is red. **Finding 12 — one member's instruction reddened another member's guard.** `.github/workflows/fuzz.yml` told an engineer to commit the crash file under the name the engine wrote, and `seedCorpusHoldsNoOtherFile` of `internal/parser/fuzz_seed_corpus_test.go` fails on that name. **The generated issue body now states the `issue-` naming rule and it names the guard.** Neither member was wrong alone, and no per-issue review reaches a pair. **Finding 13 — round 54 named Epic 5 as the owner of the conformance exit, and rounds 50 through 53 each name Epic #441.** Epic 5 is closed, its members are 37, 38, 39, 40, 41 and 42, and 441 is none of them. **Round 54 now names Epic #441.** **`conformance_test.go` writes a failure message that names Epic 5, and this round changes no line of it.** That message names the epic that closes the deviations, which is the claim `docs/specs/features/04-conformance-harness.md` makes, and it names no owner of the exit. **Finding 14 and finding 15 — two places cited two different lines for one fact, and the cited line was the test rather than the assignment.** Measured at go1.26.5 on 2026-08-14: `internal/fuzz/mutator.go:106` tests the capacity, and `internal/fuzz/mutator.go:107` is the assignment that gives the slice a capacity of `maxPerVal`. **`docs/specs/features/06-fuzz-testing.md` and `internal/fuzzprop/fuzzprop.go` now cite the same five lines**, which are `:56`, `:106`, `:107`, `:112` and `:113`. **The `len=5 cap=8` measurement reads a seed replay, and a seed replay reaches no mutator.** Both places now state what that measurement proves and what it does not, and the conclusion holds under either path. **Finding 16 — the new `seed corpus` row bars the bare word `corpus`, and `testdata/fuzz/README.md` wrote it for both meanings.** That file now writes `seed corpus` for the set of seeds and `FoxIO corpus` for the fetched captures, and it states the convention once. **Finding 17 — the new `property` row bars `assertion`, and the tree writes that word with a second meaning.** A Go type assertion is not a property, and a test assertion is not a property either. **26 tracked files write the word**, measured on 2026-08-14 with `git grep -l assertion -- ':!testdata/foxio'`, and one of the 26 is the `## Terms` row that bars it. **The row now bars `assertion` as a name for one of the five properties, and it bars no other use.** **`internal/fuzzprop` and `docs/specs/features/06-fuzz-testing.md` now write `property` where they named one of the five**, and two tests of `internal/fuzzprop/fuzzprop_test.go` are renamed to `TestTheDeterminismPropertyFiresOnATwoValueResult` and `TestTheDeterminismPropertyFiresForNoRepeatedResult`. **Finding 18 — round 54 credited the batch with 30 seed files, and the merged diff adds 29.** The tree holds 30, measured with `find testdata/fuzz internal/parser/testdata/fuzz -type f -not -name README.md` on 2026-08-14. **The 30th arrived on `dev` with the #556 hotfix at `ce2a296`**, and round 54 states that fact three sentences earlier. Round 54 now separates what the batch added from what the tree holds. **Finding 19 — `.gitignore` covered `.venv-docs/` and not `.venv/`.** It now covers both, and it states the reliance it removes: `python3 -m venv` writes a `.gitignore` holding `*` inside the directory it creates, so an environment ignores itself today. **Finding A — the body of pull request #621 states that the segment count bound is measured and not applied, and the tree applies it.** `DefaultMaxSegments` of `internal/parser/tcp_stream.go` holds 4096, and the store path refuses a further segment at that count. **The maintainer ruled the bound on 2026-08-14 after that body was written**, and issue #596 holds the ruling and the reversal path. **This round repairs no merged body**, and this sentence is the true reading. **Finding B — `docs/audit/findings.md` stated that JA4H part b is the one section without the sentinel, and the tree holds four such call sites.** `ja4hHashCallSites` of `ja4h_empty_header_list_test.go` names one in `computeJA4HFromRequest` and three in `computeJA4XWithRaw`, and `TestEveryTruncatedHashCallSiteOfTheRootPackageIsNamed` reads the syntax tree against that list. **The page now carries a table of the four**, and it states each ruling. **The same paragraph said that issue #57 of Epic 8b holds the decision for the JA4X empty list**, and the ruling of 2026-08-14 supersedes that sentence. Issue #582 is the reversal path, and `Crank-Git/ja4plus#619` is the port half. **Finding C — a published page stated a security property that the tree does not hold, and this is the one finding of the nine that reaches a user rather than a reader.** `docs/concurrency.md` stated that the age pass removes every entry, and `agedKeys` cannot remove the key of the packet that carries the crafted timestamp. `admit` runs the age pass and then calls `touch` for that key, so a later packet of the same key reads an age of zero. **`age_clock_ruling_test.go` asserts a count of 1 after the crafted packet at each of the four sites, and that assertion is correct.** **The prose was the defect, and never the test.** **All three sites now state the exception**: `state_bound.go`, `docs/concurrency.md` and the file comment of `age_clock_ruling_test.go`. **The heading of that section stated that one packet empties a bounded table, and no file cited that heading**, so this round repaired the heading too. **Finding D — the memory page named one reassembler bound, and the tree holds two.** `docs/implementation-notes.md` now states 1 MB of stored bytes and 4096 stored segments, and it states that the two refuse independently. **`docs/concurrency.md` names both bounds too**, because a reader of a memory page needs them. **`docs/throughput.md` needs neither**, because that page holds one benchmark measurement and no state bound. **This is the shared surface of the batch**: #583 edited the memory page, #596 added the bound, and no member connected the two. **Finding E — one sentence of `docs/concurrency.md` generalized over a list of seven fingerprinters, and JA4X reads the wall clock.** `ProcessPacket` of `ja4x.go` calls the wall clock. **The section now names the six that read the capture timestamp**, and it names JA4X for the wall clock alone. **Finding F — this repository and the port now collide on four issue numbers.** `Crank-Git/ja4plus#619` and this repository's #619 each concern the JA4X empty list, and `Crank-Git/ja4plus#620` and this repository's #620 concern two different questions. **No bare citation in that range names the port anywhere in the tree**, and every occurrence is written in the qualified form. The doc comment of `issueCitationHighWaterMark` now states the collision where a later reader meets it. **Finding G — the 788 attribution is this library's own measurement.** The port attributes 788 to `http2-with-cookies.pcapng`, and this library measured its largest stream on an SSH stream. **The count agrees and the attribution does not**, `internal/parser/tcp_stream.go` states the measured attribution and never the port's, and `Crank-Git/ja4plus#620` carries the port half. **No figure of this round moves.** **Finding H — FR-release-35a is unbuilt, and #105 is the issue that builds it.** Measured on 2026-08-14: `ls .goreleaser.yaml` reports no such file, and `.github/workflows/release.yml:121-129` holds an inline build matrix of five `go build` commands. **The defect predates this batch**, and it fails all five filing cases of `CLAUDE.md`, so `docs/specs/features/10-release.md` records it and this round files no issue. **Finding I — the port bounds the age of one TCP stream and this library bounds none.** The port holds a default stream age of 600 seconds at `ja4plus/utils/tcp_stream.py:50` of tag `v1.1.0`, measured on 2026-08-14, and `TCPStreamReassembler` holds no equivalent field. **That is a separate rule from the segment bound that #596 ruled**, and the type comment records the divergence so that a later reader finds it. **No issue holds that work today.** **This round re-measured `issueCitationHighWaterMark` and it read 622.** The tracker query that the guard records reported 622 on 2026-08-14, and the constant now reads 622 against an inherited mark of 614. **This round cites #594, #596, #617 and #619 through #622, and each number sits above the inherited mark.** **This round moves no fingerprint value, and it changes no byte of `testdata/deviations.json`.** **One run of `make conformance` on this branch produced every figure below**, against the corpus at `27f0cbf9fd3000c072f82a0f7d0361dc99acf6c8`. It read 38 captures, 3 of them not applicable, and 268 per-stream entries. It reports 1754 matches, 267 deviations that the register does not hold and 585 accepted deviations. It reports 175 unaccepted uncovered values, 32 accepted uncovered values, 0 stale register entries, 0 orphan register entries and 617 register keys. **`585 + 32 = 617`, so the accepted comparison count equals the register key count.** **Every one of the eight figures reads the same before this round and after it**, because this round changes no Go statement. **`make conformance` exits 2, and Epic #441 owns that exit.** **Step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable on this base, and this round reports no green gate.** **`go build ./...` exits 0**, `go test -race ./...` passes 1563 tests in 11 packages, `make lint` reports `0 issues.`, `make cover` reports 81.4% against the floor of 75.0 in `.coverage-floor`, and `make docs` builds the site under `--strict`. **A prose round adds no Go statement, so the coverage total is the total of the base.** **This round describes the tree that its own commit produces.** Recorded by batch #617. |
| 58 | 2026-08-14 | **The Epic 15 batch merges four members and one round, and #638 is this round.** #90, #91, #93 and #92 are the four members, and they are `c672cae`, `10f9c5b`, `19f1ee4` and `7a5ebe1`. **This round is the one unit of the batch permitted to write a `## Changelog` row**, and every member was told to write none. **This round files no issue**, under policy 1 of `CLAUDE.md`. **No `.go` file of this round changes behavior.** **It changes two test doc comments, one constant, the `mutate` comment of the `Makefile`, one comment of `mkdocs.yml`, `CLAUDE.md`, one settlement record and this table.** **This round weakens no guard, and it deletes no test case.** **The cross-member review of Epic 15 reported 7 findings, 6 of them reach this round, and 1 reaches #634 instead.** **F3 — the report renderer generates the pre-amendment rule.** `internal/mutationreport` writes `FR-mutation-11 states that every LIVED mutation is settled` into every report, and the maintainer amended that rule on 2026-08-14. **A round edits a comment and a test name, and a `b.WriteString` call is a statement**, so #634 carries it and **this round changes no line of `internal/mutationreport`**. **Finding 1 — one sweep of `./internal/parser` carried two elapsed times, and neither one is a defect.** `CLAUDE.md` and the `mutate` comment of the `Makefile` state `2m47s`, which is 167 s. `docs/mutation_reports/2026-08-14-internal-parser.md` states `289 s`, and `.github/workflows/mutation.yml` cites that figure. **The reading is that the two figures name two runs, and not one run read twice.** `Run` in `internal/engine/engine.go` of `gremlins` v0.6.0 sets `Elapsed` from `time.Since` around the mutation execution phase. `newReport` in `internal/report/report.go` parses that one value, `fullRunReport` prints `Mutation testing completed in %s`, and `fileReport` writes the `elapsed_time` field of the JSON. **`Duration` of `hako/durafmt` returns the parsed value without a rounding**, so the console line and the JSON report one number and a transcription of either one reaches the same figure. **So one run reports one number, and two numbers name two runs.** **The two commits corroborate the reading.** `c672cae` of #90 recorded `2m47s`, and `10f9c5b` of #91 tracked the generated report 31 minutes later on the same day. **The fixture of `main_test.go` in `internal/mutationreport` holds `"elapsed_time": 167.4`**, which is `2m47s`, so #91 knew that figure and the tracked report still reports 289 s. **A hand-written report would hold 167 s.** **The four verdict counts agree in both places because the mutation set and its verdicts are deterministic, and wall clock is not deterministic because the machine load moves it.** **Two derived claims depended on the smaller figure, and this round repairs both.** The `Makefile` stated `under three minutes`, and it now states between 2m47s and 289 s on a 10-core machine. It also names the criterion that question 1 of `docs/specs/features/15-mutation-sweep.md` `## Open questions` actually states, which is a run within the CI job limit, and `.github/workflows/mutation.yml` sets that limit at 60 minutes. **Both runs answer question 1.** `CLAUDE.md` and the `Makefile` stated `0.19` seconds for each mutation of `internal/parser`, and each now states between 0.19 and 0.33 seconds, which is 167 and 289 divided by 882. **The stated `ratio of about seven` against the root package now reads between about four and about seven.** **The `Wall clock` row of `2m47s` stays**, because it records what #90 measured, and `CLAUDE.md` now states that each cell names one run. **Finding 2 — `docs/mutation_settlements/2026-08-14-internal-parser.md` wrote `the eight reported figures` above a list of nine.** The ninth item was `617 register keys.`, and the established set of eight excludes it. The `## Parity with ja4plus` register of this file writes the eight out and then states the register key count separately, and the settlement record now follows that form. **`docs/mutation_sweep.md` was already correct**, and it states the eight without an enumeration. **This round repairs no merged pull-request body**, and three of them carry the same fold. **Finding 3 — two members assert the `exclude_docs` rule under two test names, and the tree keeps both.** `TestTheSiteExcludesTheMutationReports` in `mutation_sweep_test.go` names `mutation_reports` as a literal. `TestEveryExcludedDirectoryReachesTheSiteConfig` in `mkdocs_config_test.go` reads `excludedDocumentationDirs`, so it holds `mkdocs.yml` against that list and it holds the list against nothing. **So the second does not subsume the first.** One edit that removes `mutation_reports` from `excludedDocumentationDirs` and from `mkdocs.yml` together leaves the second test green, and `make docs` then fails on the first sweep. **The review permitted one deletion, and this round makes none**, because the deletion would weaken a guard. **The doc comment of each test now names the other one and states what it guards.** **Finding 4 — five citations of this library's own code named a line number.** `.claude/rules/ste.md` `## How a citation names its target` states that this library's own code, named in a document, cites the identifier and the file. The five sat in `docs/mutation_settlements/2026-08-14-internal-parser.md`, and each one now names `ProcessPacket` of `ja4t.go`, `ProcessPacket` of `ja4ts.go`, `ProcessPacket` of `ja4h.go` or `requestLineRe` in `internal/parser/http.go`. **A mutation position is a coordinate and not a reader citation, so this repair reaches none of them.** The record already stated that a position names the tree at commit `19f1ee4`, and it now states the citation rule beside it. **Finding 5 — ten settlements carried no issue number, and FR-mutation-14 states `Each settlement names the mutation and the issue that closed it.`** The only attribution was document-level. **#92 wrote every settlement of that record**, so the heading of each of S1, S2, E1, E2, E3 and E4 now carries `(#92)`, and the preamble states why a reader sees one number ten times. **Finding 6 — `mkdocs.yml` stated that the link guard fails on three directories, and it fails on four.** #92 added `docs/mutation_settlements/` to `excludedDocumentationDirs`, which drives `TestNoPublishedPageLinksIntoAnExcludedDirectory`, and it wrote its own paragraph below the stale enumeration. The comment now names the four, and it states that the list decides the count. **This round re-measured `issueCitationHighWaterMark` and it read 639.** The tracker query that the guard records reported 639 on 2026-08-14, and the constant now reads 639 against an inherited mark of 635. **#639 is the pull request of the Epic 16 documentation round, and #638 is this round.** This round cites #638, which sits above the inherited mark. **This round moves no fingerprint value, and it changes no byte of `testdata/deviations.json`.** **The eight conformance figures read the same before this round and after it**, because this round changes no Go statement: 38 captures, 1754 matches, 267 deviations that the register does not hold, 585 accepted deviations, 175 unaccepted uncovered values, 32 accepted uncovered values, 0 stale register entries and 0 orphan register entries. **The register key count is 617 before and after.** **`make conformance` exits 2, and Epic #441 owns that exit**, so step 4 of the `## A change is done when` list of `CLAUDE.md` is unmeetable on this base. **This round ran no mutation sweep**, because every count it needs sits in the tracked report. **This round describes the tree that its own commit produces.** Recorded by Epic 15. |
