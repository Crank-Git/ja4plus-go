name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master, dev]

jobs:
  test:
    strategy:
      matrix:
        go-version: ['1.24']
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Build
        run: go build ./...

      - name: Test
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Build CLI
        run: go build -o bin/ja4plus ./cmd/ja4plus

      - name: Verify CLI
        run: ./bin/ja4plus --version

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      # The pin holds the linter result steady. A linter release must never fail a pull
      # request that changes no code. Move this version and `.golangci.yml` together.
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.2
